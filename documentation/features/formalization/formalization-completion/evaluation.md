---
feature: "formalization/formalization-completion"
spec: ./spec.md
plan: ./plan.md
spec_revision: 6
status: ready
prd: https://plataformahms.atlassian.net/wiki/spaces/~712020e69febeaca304dffb2d8d156ea17d2c4/pages/24051713
jira_tickets:
  - SCRUM-145
updated_at: 2026-09-10
---

# Evaluation

Evaluation of Spec revision `6` against the current implementation.

Current result: `ready`; the full Core, Validation, Server, Inngest and Web evidence is green, the authenticated provider-backed browser captures are current, and the signed terminal state renders `2 de 2`/`100%`. The structural path classifications were reconciled against the selected `HEAD` baseline and the current gate passes all `239` declared paths.

## Acceptance matrix

| Criterion | Evidence | Status |
| --- | --- | --- |
| `AC-01` | `EV-BASE`; `MV-01`; `EV-WEB-SEND-ACTION`; `EV-WEB-SIGNATORY-AVATAR` | `passed_with_environment_note` |
| `AC-02` | `EV-BASE`; `MV-01` | `passed_with_environment_note` |
| `AC-03` | `EV-BASE`; `MV-03` | `passed_with_environment_note` |
| `AC-04` | `EV-BASE`; `MV-03` | `passed_with_environment_note` |
| `AC-05` | `EV-BASE`; `EV-LIVE-RESEND`; `MV-01` | `passed_with_environment_note` |
| `AC-06` | `EV-BASE`; `EV-LIVE-CLONE`; `MV-01` | `passed_with_environment_note` |
| `AC-07` | `EV-BASE`; `EV-VIS-CONFIRM`; `MV-01` | `passed_with_environment_note` |
| `AC-08` | `EV-BASE`; `EV-LIVE-CLONE`; `MV-01`, `MV-02` | `passed_with_environment_note` |
| `AC-09` | `EV-BASE`; `EV-LIVE-CLONE`; `MV-01` | `passed_with_environment_note` |
| `AC-10` | `EV-BASE`; `MV-02` | `passed_with_environment_note` |
| `AC-11` | `EV-BASE`; `MV-02` | `passed_with_environment_note` |
| `AC-12` | `EV-BASE`; `MV-01`–`MV-03`; `EV-WEB-SEND-ACTION`; `EV-WEB-SIGNATORY-AVATAR` | `passed_with_environment_note` |
| `AC-13` | `EV-BASE`; `EV-LIVE-CLONE`; `MV-01` | `passed_with_environment_note` |

## Automated and runtime evidence

| ID | Type | Layer | Command or scenario | Result | Status |
| --- | --- | --- | --- | --- | --- |
| `EV-BASE` | baseline | Cross-layer | Untouched implementation comparison against Spec revision 6 required tree, contracts, states, exclusions and validation exits | Baseline recorded before feature edits; current repository did not contain the required completion slice. | passed |
| `EV-PREFLIGHT-DOCKER` | runtime | Cross-layer | `docker compose ps -a` | Supabase DB/Auth, Inngest, Documenso, Gotenberg, Storage and Mailpit are up; DB/Auth are healthy. | passed |
| `EV-PREFLIGHT-AUTH` | runtime | REST | `curl -fsS http://localhost:8000/auth/v1/health` | GoTrue returned version `v2.189.0`. | passed |
| `EV-PREFLIGHT-SERVER` | runtime | REST | `curl -fsS http://localhost:5555/health` | Server returned `status: ok`, database and supabase `UP`. | passed |
| `EV-PREFLIGHT-SEED` | runtime | Provision | `rg -n "admin@hmsadvogados.com.br|HMS_USER_SEED_PASSWORD" apps/server/src/identity/database/identity-seeder.ts .env* apps/server/.env*` | Administrator email and `HMS_USER_SEED_PASSWORD` source were resolved; password value remains in local env and is not copied into this ledger. | passed |
| `EV-BUILDER-CORE-ACTIVATION` | workflow | Domain, Use cases | Activate `builder_core` Turing (`01a082ea-28fc-7722-9ba9-9e60dc71881c`) | Revision 6, F1-T1 then F2-T1, `packages/core/**` ownership, Rule Pack, design/PRD/Jira references and exits were supplied before feature edits. | passed |
| `EV-CORE-F1` | automated | Domain, Interfaces, Composition | `pnpm --filter @hms/core check:architecture && pnpm --filter @hms/core check-types` | Architecture passed with 1039 modules/3354 dependencies and 0 violations; type-check passed. | passed |
| `EV-CORE-F2` | automated | Use cases | `pnpm --filter @hms/core test` | 129 test files and 644 tests passed after the resend envelope correction. Covers tracking authorization/projection, resend/cancellation, contracting, Intake contracting, completion, Case summary, freeze/preview and transition guards. | stale |
| `EV-CORE-PROGRESS` | automated | Use cases | `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core exec vitest run src/formalization/use-cases/tests/get-formalization-signature-sending-status-use-case.test.ts`; `pnpm --filter @hms/core test` | The status use case now counts `submitted` and `confirmed` documents as completed for aggregate signature progress. Type-check passed; focused regression passed 1/1 file and 10/10 tests; the full Core suite passed 129/129 files and 645/645 tests. | passed |
| `EV-BUILDER-VALIDATION-ACTIVATION` | workflow | Validation | Activate `builder_validation` Averroes (`01a08307-6f49-72b0-be51-6191da7fefa6`) | Revision 6, F2-T2, `packages/validation/**` ownership, strict schema contract, Rule Pack, PRD/Jira references and validation exits were supplied before Validation edits. | passed |
| `EV-VALIDATION` | automated | Validation | `pnpm --filter @hms/validation check:architecture`; `pnpm --filter @hms/validation check-types`; `pnpm --filter @hms/validation lint` | All three passed; architecture reported 108 modules/254 dependencies, type-check passed, and Biome checked 97 files with no fixes. | passed |
| `EV-BUILDER-SERVER-ACTIVATION` | workflow | REST, Provision, Database, Messaging | Activate `builder_server` Halley (`01a08310-71f8-7030-ad90-87182a8b2b1d`) | Revision 6, F3-T1, canonical non-generated `apps/server/**` ownership, Rule Pack, PRD/Jira references, real curl and server/Inngest exits were supplied before Server edits; generated migration outputs are prohibited. | passed |
| `EV-BUILDER-WEB-ACTIVATION` | workflow | UI, REST | Activate `builder_web` Wegener (`01a08310-6639-7841-9d17-a28e3ad9693b`) | Revision 6, F4-T1, canonical `apps/web/**` ownership, Rule Pack, seven design references, supplemental decisions, Playwright and route exits were supplied before Web edits. | passed |
| `EV-SERVER` | automated | REST, Provision, Database, Messaging | `pnpm --filter server check:architecture`; `pnpm --filter server check:code`; `pnpm --filter server check:types`; `pnpm --filter server test`; `pnpm --filter server test:inngest` | Architecture/code/types passed. Final correction Server suite passed 114/114 files and 246/246 tests. The corrected full Inngest suite passed 10/10 files and 31/31 tests in 717.25s; the focused reconcile rerun passed 4/4. The fixture now retries transient host-port binding timeouts and cleans partially started containers. | passed |
| `EV-MIGRATION` | generated | Database | `pnpm --filter server db:migration:generate --name formalization_completion`; migration contract test; clean local database apply/idempotency | Generation produced 0043 SQL, snapshot and journal entry; Orchestrator added only the Spec-contracted guarded transitions, formatted the generated metadata and the migration suite passed 2 tests. The application schema in `postgres` was recreated, the complete journal including 0043 applied successfully, and the seed completed with every contracted Intake carrying `contractedAt`; Supabase `auth` and `storage` schemas were preserved, with `hms-bucket` confirmed as the canonical bucket for seeded stored files. | passed |
| `EV-LIVE-LOCAL-DB` | runtime | Auth, REST, UI, Database, Storage | Fresh Playwright login and `/intakes` → `/intakes/1426b63f-e42c-4f26-ab29-022284aecbf3` against Server/Web on the recreated `postgres` database with `hms-bucket` configured | Administrator reached `/home`; the seeded `INT-0001` detail rendered through `GET /intakes/:id` with 200 responses for Intake, client, collaborator, catalog, consultation, Formalization completion and Case summary; no console errors were observed. Both seeded Formalization objects returned 200 from `hms-bucket` with the expected byte sizes, and no stored-file reference was missing. | passed |
| `EV-LIVE-FORMALIZATION-FIXTURE` | runtime | Auth, REST, UI, Database | Authenticated Playwright flow created `INT-0023`, finalized its consultation form, advanced the Intake to `in_formalization`, started Formalization `b2a571d9-9692-412f-a7dc-ebaa50937d49`, saved the contract-form draft, corrected its responsible collaborator through the authenticated Intake update, and reloaded `/intakes/32e85063-e2d8-48f0-b471-2018da518f98` | Real REST/UI state is `IntakeStatus.in_formalization`; Formalization is `in_progress`, `contractFormState=open`, revision `0`, and has 8 saved answers with all 6 required fields complete. Formalization document selection/list remains blocked by the open form, so no Formalization document or signature package was created. Fresh Intake snapshot shows `Em Formalização`, `Abrir formalização`, and `Administrador de desenvolvimento` as responsible; the final browser reload has 0 console errors and 0 warnings. The consultation package used one approved consultation document solely to satisfy the existing completion guard; it is outside the Formalization fixture surface. The delayed `ConsultationCompletedEvent` from that synthetic setup was rejected as a non-retriable invalid transition after the Intake had already advanced; it did not change the final state. | passed_with_environment_note |
| `EV-WEB` | automated | UI, REST | `pnpm --filter web generate-routes`; `pnpm --filter web check:code`; `pnpm --filter web check:architecture`; `pnpm --filter web check:types`; `pnpm --filter web test`; escaped Intake focused route command; focused Formalization/Intake Playwright command; contracted-outcome focused correction suite | Route generation/code/architecture/types passed. The final full Web suite passed 166 files/547 tests after the send-action correction. The focused browser command passed 7/7 on a fresh server; the escaped Intake route passed with a 390×844 screenshot. The card correction suite passed 2 files/4 tests after the final success-surface correction; the preceding integrated card suite passed 6 files/10 tests. Route generation retains the existing non-route test-file warning. Fresh clone captures cover both contracted Intake projections, tracking at desktop/narrow, resend/cancellation dialogs, the controlled 99% state and main-page 100% readiness/confirmed states; the ready-to-send route capture is recorded in `EV-WEB-SEND-ACTION`. | passed_with_environment_note |
| `EV-WEB-SEND-ACTION` | automated/runtime | UI | `pnpm --filter web exec vitest run src/ui/formalization/widgets/pages/formalization-sending-configuration/tests/formalization-sending-configuration.test.tsx`; `CI=1 HMS_WEB_APP_PORT=5001 PLAYWRIGHT_WEB_APP_URL=http://127.0.0.1:5001 pnpm --filter web exec playwright test tests/routes/formalization/formalization-sending-configuration.test.tsx --project=chromium` | The panel test passes 7/7 and verifies the confirmation dialog calls `confirmSending` with the current expected version and UUID key. The clean Playwright route passes 2/2 and captures `/home/petros/projects/hms/apps/web/test-results/routes-formalization-forma-b9a02-trols-in-the-configured-tab-chromium/signatories-tab-redesign.png`, showing the ready state and `Enviar assinaturas` action. | passed_with_environment_note |
| `EV-WEB-SIGNATORY-AVATAR` | automated | UI | `pnpm --filter web exec vitest run` for the affected tracking-panel/widget files | The tracking-panel correction passes 29 files and 105 tests; the isolated panel test passes 3/3. Each signatory row renders the shared `CollaboratorAvatar` with stable recipient-based initials/color, the region name is accessible, and refresh-summary typing/announcement remains valid. Web Biome, type-check and architecture checks pass. | passed |
| `EV-WEB-PROGRESS` | automated | UI | `pnpm --filter web exec vitest run src/ui/formalization/widgets/pages/formalization-sending-configuration/signature-tracking-panel/signature-progress-summary/tests/signature-progress-summary.test.tsx src/ui/formalization/widgets/pages/formalization-sending-configuration/signature-tracking-panel/signature-progress-summary/tests/use-signature-progress-summary.test.ts src/ui/formalization/widgets/pages/formalization-sending-configuration/signature-tracking-panel/tests/signature-tracking-panel.test.tsx` | The summary and tracking-panel display tests pass 3/3 files and 6/6 tests; the UI renders the server-provided completed-document count and percentage without applying a second, conflicting completion rule. | passed |
| `EV-WEB-CONFIRMED-STATUS` | automated/runtime | UI | Focused summary Vitest tests; authenticated Playwright navigation to `/formalizacoes/00000000-0000-4000-8000-000000000701` | The main-page summary now maps the confirmed signature request to `Confirmado`; focused Web tests pass 2/2 files and 7/7 tests, and the live page receives 200 responses for formalization/review/status with no console errors or warnings. | passed_with_environment_note |
| `EV-LIVE-CLONE` | runtime | REST, UI, Database | Disposable PostgreSQL 17 clone on `localhost:55432`; persistent Server/Web sessions; Playwright CLI login and route navigation | The clone accepted the guarded 0043 migration and idempotent rerun. Seeded administrator login reached `/home`. Real server-backed routes rendered contracted no-Case Intake `1426b63f-e42c-4f26-ab29-022284aecbf3`, contracted Case Intake `c4f1199a-ce27-48d5-b0e3-8bedf195ce6c`, confirmed tracking and the provider-backed Formalization `00000000-0000-4000-8000-000000000701`. The final terminal Intake navigations showed zero console errors and 200 feature REST responses; the narrow capture had `scrollWidth=clientWidth=390` and keyboard focus advanced to Notifications. Role snapshots and provider captures are recorded in `EV-LIVE-ROLES` and `EV-LIVE-PROVIDER`; the disposable container is removed after this validation turn. | passed_with_environment_note |
| `EV-LIVE-PROGRESS` | runtime/visual | Auth, REST, UI | `playwright-cli -s=hms-progress` login, navigation to `/formalizacoes/00000000-0000-4000-8000-000000000701/configuracao-envio`, resize to `1200×900`, screenshot and console/network inspection | Fresh authenticated route returned 200 for formalization, review and status endpoints; both documents and all signatories rendered `Assinado`, the summary rendered `2 de 2 documentos concluídos`, the progressbar rendered `100%`, and the page showed `Todas confirmadas`. Console had 0 errors/warnings; capture: `/tmp/hms-formalization-progress-fixed-1200x900.png`. | passed_with_environment_note |
| `EV-LIVE-ROLES` | runtime | Auth, REST, UI | Fresh Playwright contexts for seeded administrator, assigned lawyer, eligible linked signatory, unlinked eligible collaborators, ineligible collaborator and client account | Admin and the assigned lawyer reached `/home` and the Formalization route with feature requests at 200. The standard assigned-lawyer fixture was then isolated in the clone by assigning the Formalization to the additional seeded lawyer while retaining `lawyer@hmsadvogados.com.br` as a current request recipient: the fresh `lawyer@hmsadvogados.com.br` context returned status `200`, `viewerMode=tracking_only`, no operational controls and only tracking data. `attendant`, `paralegal`, `lawyer.contracts` and `paralegal.documents` each rendered “Não foi possível carregar a formalização” with the three feature requests at expected 403; no sensitive projection was returned. `client@hms.br` received Auth success but `POST /auth/complete-sign-in` returned the expected 404 and remained on `/login`. Role snapshots: `/tmp/hms-role-lawyer-linked.yml`, `/tmp/hms-role-attendant-final2.yml`, `/tmp/hms-role-paralegal-final3.yml`, `/tmp/hms-role-contracts-lawyer-final3.yml` and `/tmp/hms-role-documents-paralegal-route-snapshot.yml`; expected authorization 403/404 console entries are classified as non-feature errors. | passed_with_environment_note |
| `EV-LIVE-PROVIDER` | runtime | REST, Database, Inngest, Provider, UI | Real Documenso v2 envelope and canonical HMS confirm/reconcile/cancel path | Admin `POST /formalizations/00000000-0000-4000-8000-000000000701/signature-sending/confirm` returned 200 and created request `45a27836-71d4-46ff-852f-c7cc5e09caf3` in `provisioning`; Inngest run `01M23BR3M9ETXT3N52H3XZAMA6` completed successfully. The real provider envelope `envelope_suoyzlrdbxisoiym` was `PENDING` with `NOT_SIGNED/NOT_SIGNED`, HMS persisted `sending`, and the 1200×900 route rendered “Enviando”. One actual Documenso signer completed through the signing UI (`POST /api/trpc/envelope.field.sign` 200 twice), producing provider `SIGNED/NOT_SIGNED`; scheduled reconciliation changed HMS to `partially_submitted`, and the 1200×900 HMS capture rendered “Parcialmente assinado”, 0/2 documents, one sent recipient and one eligible resend. The admin cancellation dialog required a reason; cancel returned 200, Documenso became `CANCELLED`, HMS became `cancelled` with `cancellation_requested_at` and `terminal_at`, and the final route rendered the cancelled projection with zero HMS console errors. Captures: `/tmp/hms-provider-mixed-snapshot.yml`, `/home/petros/projects/hms/.playwright-cli/page-2026-09-09T15-15-36-063Z.png`, `/tmp/hms-provider-cancelled2.yml`. | passed_with_environment_note |
| `EV-LIVE-RESEND` | runtime | REST, Database, Inngest, Provider, UI | Real admin resend against the disposable clone and Mailpit, after the Core envelope correction | Fresh authenticated operator confirmed the real resend POST with `201`; before the subsequent cancellation, the successful run persisted invitation generation 4 as active/delivered, revoked generations 1–3, recorded a delivery attempt and Mailpit message `5ojXvB7LfmMDR2PH6YrEJw`, and kept the Documenso request/resource unchanged. The historical generation-2/3 failures remain recorded under `FND-022` as the reproduced defect that drove the correction. The final browser context had zero console errors and feature requests at 200. Captures: `/tmp/hms-formalization-resend-dialog-final-1200x900.png`, `/tmp/hms-formalization-resend-success-refreshed-1200x900.png`. | passed_with_environment_note |
| `EV-COVERAGE` | automated | Cross-layer | `pnpm test:coverage`; direct `pnpm --filter server test:coverage` | The earlier 30m52s Server coverage attempt exited non-green, and the first post-correction aggregate invocation ended before Vitest’s final summary. The final uncached direct Server run after the resend correction completed green: 114/114 test files and 247/247 tests passed, exit 0, duration 1738.17s; V8 emitted the coverage report. The intentional Nest error logs are from negative-path tests and did not affect the exit. | passed |
| `EV-BUILD` | automated | Cross-layer | `pnpm build` | Turborepo build passed: Server Nest build and Web Vite/Nitro production build succeeded. The existing non-route test-file warning was emitted by route generation. | passed |
| `EV-REVIEWER` | review | Cross-layer | Single `implementation-reviewer-agent` review and same-reviewer recheck of the integrated revision-6 candidate | Initial review found seven actionable defects; owning Builders corrected them. The same reviewer rechecked the corrected Inngest fixture and found no implementation regression, and identified the incomplete coverage output as the only remaining evidence issue. The complete direct coverage run is now green. Final read-only recheck of the Formalization/Case cards and fresh terminal Intake captures found no actionable visual regressions; the success-tinted panel, terminal hierarchy, Case states and disabled manifest-authorized action are clean. | passed |

## Structural path gate

| ID | Command | Base ref | Base SHA | Counts | Result | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `PATH-01` | `pnpm check:spec-implementation -- documentation/features/formalization/formalization-completion/spec.md --base HEAD --json` | `HEAD` | `a786b47ff92c3b962a598d0b518010d17673d090` | `declaredPathsCount=239; passedPathsCount=239; failedChecksCount=0; changedFilesCount=436; unrelatedChangedFilesCount=197` | Passing integrated candidate gate after reconciling four Web `Create` classifications against the selected `HEAD` baseline; the existing panel entry remains `Modify`. | passed |
| `PATH-02` | `pnpm check:spec-implementation -- documentation/features/formalization/formalization-completion/spec.md --base HEAD` | `HEAD` | `a786b47ff92c3b962a598d0b518010d17673d090` | `declaredPathsCount=239; passedPathsCount=187; failedChecksCount=94; changedFilesCount=1415; unrelatedChangedFilesCount=1178` | Historical failed run retained to show the pre-reconciliation dirty-worktree result; it is superseded by `PATH-01` and is not current candidate evidence. | failed |

## Manual evidence

| ID | Scenario | Criteria | Expected | Observed | Status |
| --- | --- | --- | --- | --- | --- |
| `MV-01` | Operator tracking, resend, cancellation and contracting on real local Auth/REST/DB/Inngest/provider-stub services | `AC-01`, `AC-02`, `AC-05`–`AC-09`, `AC-12`, `AC-13` | Tracking, recoverable resend, reasoned cancellation, main-page confirmation and immutable PDF reuse complete with persistence, no duplicate provider resource, no Case effect and clean browser/network evidence. | Provider-backed confirmation, provisioning, sending, one real provider signature, reconciliation to `partially_submitted`, responsive mixed-state rendering, successful generation-4 resend, reasoned cancellation and contracting completion were executed on the disposable clone. The resend used real Auth/REST/Inngest/Mailpit boundaries and the contracting flow persisted Formalization `completed` plus Intake `contracted`; no Case side effect occurred. Supplemental captures are closed, and the recreated `postgres` application schema now passes the current authenticated Intake runtime check with `hms-bucket` as the canonical storage bucket. | passed_with_environment_note |
| `MV-02` | Contracted Intake with no Case and with related Case | `AC-08`, `AC-10`–`AC-12` | Both terminal Intake projections load through the existing authorized boundary; Formalization link works; Case absence/presence is optional and actions remain disabled with explanation. | The disposable clone supplied both terminal fixtures: no-Case `1426b63f-e42c-4f26-ab29-022284aecbf3` and with-Case `c4f1199a-ce27-48d5-b0e3-8bedf195ce6c`. Both loaded through the real authenticated REST path at `1200×2300`, showed the completed Formalization link, rendered the expected Case absence/presence data, kept `Abrir caso` disabled with explanation, and produced zero console errors with 200 feature responses. The visual comparison records compact runtime card differences and the manifest-authorized disabled Case action. | passed_with_environment_note |
| `MV-03` | Operator, linked recipient and forbidden collaborator projection | `AC-03`, `AC-04`, `AC-12` | Operator/admin can operate, eligible linked recipient sees only tracking, and unlinked/inactive/ineligible users receive 403 without disclosure. | Fresh contexts covered admin and assigned-lawyer operator access (200), the seeded `lawyer@hmsadvogados.com.br` request recipient as `viewerMode=tracking_only` after assigning the clone Formalization to the additional seeded lawyer, and four forbidden collaborator contexts (`attendant`, `paralegal`, `lawyer.contracts`, `paralegal.documents`) with 403 on Formalization/review/status and only the generic error surface. `client@hms.br` was rejected at collaborator completion with the expected 404 and remained on `/login`. No sensitive commercial/document projection was observed for the linked or forbidden paths; the expected 403/404 browser console entries are authorization diagnostics, not application defects. | passed_with_environment_note |
| `MV-04` | Intake fixture with an open Formalization contract form | `AC-06`, `AC-12` | A second Intake is in Formalization with only the commercial form completed as a draft; Formalization documents and signature sending remain unavailable until the form is closed. | `INT-0023` renders `Em Formalização`; the Formalization page shows `6 de 6 obrigatórios`, `Aberta`, and saved values after reload. Formalization document endpoints return the expected conflict while the form remains open, and no Formalization document/signature data exists for the fixture. | passed_with_environment_note |

## Visual evidence

| ID | Type | Surface and state | Viewport | Reference | Implementation | Differences | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `EV-VIS-JOOMF` | visual | Completed Formalization card | `945 × 244` | `design/JOoMf.png` | `/tmp/hms-visual-formalization-card-final.png` captured from the real contracted Intake at runtime size `863 × 270`; `/tmp/hms-visual-intake-no-case-final-1200x2300.png` provides the full-page context. | Fresh comparison confirms the success header/badge, visibly green confirmation panel, “Contratação confirmada”, completion time, “Todas confirmadas”, helper copy and enabled canonical link. Runtime width is constrained by the app content column rather than the isolated Pencil canvas. | passed_with_environment_note |
| `EV-VIS-C4XC5D` | visual | Contracted Intake, Case not started | `945 × 178` | `design/C4XC5D.png` | `/tmp/hms-visual-case-empty-card-final.png` captured at runtime size `863 × 218`; full-page evidence is `/tmp/hms-visual-intake-no-case-final-1200x2300.png`. | Fresh comparison confirms the Case header/status, neutral empty panel, “Nenhum caso relacionado”, muted copy and disabled/explained action. The disabled action is the manifest-authorized deviation from the enabled-looking reference. | passed_with_environment_note |
| `EV-VIS-N2XVJ` | visual | Contracted Intake, related Case summary | `945 × 176` | `design/N2Xvj.png` | `/tmp/hms-visual-case-present-card-final.png` captured at runtime size `863 × 259`; full-page evidence is `/tmp/hms-visual-intake-with-case-final-1200x2300.png`. | Fresh comparison confirms public code, status, area, primary lawyer, opened date and disabled action inside the neutral Case panel. Runtime width follows the app content column; no navigation/mutation occurs as required. | passed_with_environment_note |
| `EV-VIS-W0QBVY` | visual | Full contracted Intake without Case | `1200 × 2300` | `design/w0QbVY.png` | `/tmp/hms-visual-intake-no-case-final-1200x2300.png` | Exact viewport comparison confirms the terminal `Contratado` pipeline, existing Intake regions, completed Formalization and empty Case/activity hierarchy. Fixture copy differs by design; obsolete close action is absent and Case action is disabled per manifest. Card-level compactness is tracked in `EV-VIS-JOOMF`/`EV-VIS-C4XC5D`. | passed_with_environment_note |
| `EV-VIS-OKATX` | visual | Full contracted Intake with Case | `1200 × 2300` | `design/okATx.png` | `/tmp/hms-visual-intake-with-case-final-1200x2300.png` | Exact viewport comparison confirms the same terminal hierarchy plus public Case code/status/area/lawyer/date. Fixture copy differs by design; disabled Case action and no Case navigation are manifest-authorized. | passed_with_environment_note |
| `EV-VIS-FBWZH` | visual | Signature tracking, mixed signed/pending recipients | `1200 × 1240` | `design/FbWzH.png` | `/tmp/hms-visual-tracking-1200x1240.png` remains the confirmed tracking comparison; `/home/petros/projects/hms/.playwright-cli/page-2026-09-09T15-15-36-063Z.png` is the fresh provider-backed mixed/partial route capture at `1200 × 900`. | Desktop confirmed hierarchy and the provider-backed mixed/partial hierarchy, grouping, channels, dates and resend/cancel controls are verified. The supplemental mixed capture uses the available `1200 × 900` manifest viewport rather than the supplied `1200 × 1240` canvas, so the status remains an environment-qualified pass. | passed_with_environment_note |
| `EV-VIS-PROGRESS` | visual | Signature tracking, all documents signed | `1200 × 900` | `design/FbWzH.png` | `/tmp/hms-formalization-progress-fixed-1200x900.png` captured through a fresh authenticated route after the progress correction. | Both document groups and all signatories show `Assinado`; the aggregate shows `2 de 2 documentos concluídos`, a full progress bar and `100%`. The available runtime viewport is shorter than the supplied `1200 × 1240` canvas, so the comparison remains environment-qualified. | passed_with_environment_note |
| `EV-VIS-CONFIRMED-STATUS` | visual | Main Formalization page, confirmed signature request | `1200 × 900` | `design/manifest.md` supplemental decision | `/tmp/hms-formalization-status-confirmed-card-1200x900.png` captured from the authenticated main page after scrolling to the configuration summary. | The configuration card visibly shows the green `Confirmado` badge while the request is confirmed; the runtime page also exposes the confirmed contracting action. | passed_with_environment_note |
| `EV-VIS-MC4E2` | visual | Signature sending/provisioning in progress | `1200 × 1374` | `design/MC4E2.png` | `/home/petros/projects/hms/.playwright-cli/page-2026-09-09T15-15-36-063Z.png` captured the real route at `1200 × 900` while Documenso was `PENDING` and HMS was `partially_submitted`; the earlier sending capture was observed before the first provider signature. | Nonterminal provider-backed rendering is now verified, including “Enviando”/“Parcialmente assinado”, 0/2 progress, recipient statuses, resend and cancellation controls. The runtime viewport is the manifest supplemental `1200 × 900` rather than the supplied `1200 × 1374` canvas. | passed_with_environment_note |
| `EV-VIS-NARROW` | visual | Tracking narrow layout | `390 × 844` | `design/manifest.md` supplemental decision | `/tmp/hms-visual-tracking-narrow-390x844.png`; Playwright CLI keyboard Tab path and DOM overflow inspection | Content stacks, `scrollWidth=clientWidth=390`, focus advances to the Notifications button, feature requests are 200 and console errors are 0. | passed_with_environment_note |
| `EV-VIS-CANCEL` | visual | Cancellation dialog | `1200 × 900` | `design/manifest.md` supplemental decision | `/tmp/hms-formalization-cancel-dialog-final-1200x900.png` captures the focused reason field and pending cancellation action; the subsequent real submission persisted the trimmed reason and rendered the cancelled projection. | Fresh dialog comparison confirms title, provider warning, reason label/helper, character counter, focus and action hierarchy. | passed_with_environment_note |
| `EV-VIS-RESEND` | visual | Resend confirmation and feedback | `1200 × 900` | `design/manifest.md` supplemental decision | `/tmp/hms-formalization-resend-dialog-final-1200x900.png` captures the confirmation dialog and `/tmp/hms-formalization-resend-success-refreshed-1200x900.png` captures the real post-201 sending projection; provider-backed delivery is recorded in `EV-LIVE-RESEND`. | Fresh comparison confirms confirmation copy, access-revocation warning, operator action and recoverable sending state. | passed_with_environment_note |
| `EV-VIS-CONFIRM` | visual | Contract confirmation at 99% and 100% readiness | `1200 × 900` | `design/manifest.md` supplemental decision | `/tmp/hms-formalization-99-percent-1200x900.png` captures the controlled 99% progress state; `/tmp/hms-visual-ready-main-1200x900.png` captures 100% readiness; `/tmp/hms-formalization-confirmed-1200x900.png` captures the provider-backed terminal read-only state. | The 99% fixture is controlled because the production aggregate derives whole-document completion and cannot naturally produce 99% with the two-document seed. The contracting action remains disabled until all authoritative prerequisites are confirmed; 100% confirmation/persistence is verified separately. | passed_with_environment_note |
| `EV-VIS-TRACKING-ONLY` | visual | Tracking-only eligible linked collaborator | `1200 × 900` | `design/manifest.md` supplemental decision | `/tmp/hms-role-lawyer-linked.yml` is the fresh linked-recipient route snapshot after the clone assignment isolation; status returned `200`, `viewerMode=tracking_only`, and the rendered route contained tracking data only with resend controls disabled. | Role-specific disclosure and forbidden 403 matrix are verified; no commercial conditions or document-content route projection was exposed. | passed_with_environment_note |

## Rule and documentation compliance

| Authority | Reference | Result | Notes |
| --- | --- | --- | --- |
| Rule Pack | `documentation/rules/code-conventions-rules.md` | passed | Applied and checked by the final Builder sensors and reviewer; `git diff --check` passed. |
| Rule Pack | `documentation/rules/core-package-rules.md` | passed | Core ownership, use-case boundary, exports and ports passed final review and Core sensors. |
| Rule Pack | `documentation/rules/use-case-testing-rules.md` | passed | Truth-table and infrastructure-free test coverage passed in Core. |
| Rule Pack | `documentation/rules/validation-package-rules.md` | passed | Strict schema ownership and test-free package constraints passed final Validation checks. |
| Rule Pack | `documentation/rules/rest-layer-rules.md` | passed | Thin controller, response and REST-client checks passed. |
| Rule Pack | `documentation/rules/controllers-testing-rules.md` | passed | Focused and full Server controller suites passed. |
| Rule Pack | `documentation/rules/database-layer-rules.md` | passed | Generated migration and disposable contract passed; the recreated `postgres` application schema applied the full journal and the canonical `hms-bucket` contains every seeded stored-file reference. |
| Rule Pack | `documentation/rules/provision-layer-rules.md` | passed | Provider boundaries, rollback and PDF cleanup passed reviewer checks. |
| Rule Pack | `documentation/rules/server-app-layer-rules.md` | passed | Nest composition and owner boundaries passed static/reviewer checks. |
| Rule Pack | `documentation/rules/messaging-layer-rules.md` | passed | Post-commit/recovery paths passed; the corrected full Inngest suite passed 10/10 files and 31/31 tests. |
| Rule Pack | `documentation/rules/jobs-testing-rules.md` | passed | Focused reconcile passed 4/4 and the corrected full Inngest suite passed 10/10 files and 31/31 tests. |
| Rule Pack | `documentation/rules/ui-layer-rules.md` | passed_with_environment_note | Web tests and reviewer passed; real authenticated/manual evidence covers terminal Intake, tracking, resend/cancellation and contracting states, with the documented 99% controlled fixture. |
| Rule Pack | `documentation/rules/web-app-routing-rules.md` | passed | Route generation, focused routes and structural path gate passed; existing non-route warning remains. |
| Rule Pack | `documentation/rules/widget-testing-rules.md` | passed_with_environment_note | Full Web unit and focused Playwright tests passed; exact desktop comparison exposed the compact card delta recorded above. |
| Architecture and Modules | `documentation/architecture.md`; `documentation/modules.md` | passed | Owner boundaries, no Case side effect and final import-graph sensors passed. |
| Design | `documentation/design.md`; `design/manifest.md` | passed_with_environment_note | Fresh comparisons now exist for both terminal Intake pages, all three card artifacts, confirmed and provider-backed mixed/sending tracking, narrow tracking, tracking-only role projection, cancellation/resend dialogs, the controlled 99% state and 100% readiness/confirmed states. The mixed runtime capture is `1200×900` against taller supplied canvases; the 99% capture is controlled and explicitly documented because the seeded two-document aggregate cannot produce a natural 99% value. |
| Tooling | `documentation/tooling.md` | passed_with_environment_note | Repository commands, structural gate, build, Inngest suite and direct Server coverage are recorded; the 99% state remains a documented controlled visual fixture. |

## Findings

| ID | Classification | Source | Affected evidence | Status | Resolution |
| --- | --- | --- | --- | --- | --- |
| `FND-001` | environment | Pre-implementation structural diagnostic | `PATH-01` | accepted_non_blocking | Current worktree contains unrelated changes; keep diagnostic historical and rerun the gate only after final integration. |
| `FND-002` | implementation | Core tracking truth table/test after F1 handoff | `EV-CORE-F2`; `AC-03`, `AC-04` | resolved | Resumed `builder_core`; unknown/unauthorized access now rejects without repository reads and the expanded revision-5 projection has current assertions. Core sensors reran successfully. |
| `FND-003` | Contract | Core Builder handoff | `EV-CORE-F1`; `AC-08`, `AC-10`, `AC-11` | resolved | Resumed `builder_core`; completed contracting, Intake contracting, completion, Case summary, freeze/preview contracts and truth tables. `documentSpecificationId` is required. Core sensors reran successfully. |
| `FND-004` | implementation/validation | Web Builder handoff | `EV-WEB`; `AC-01`–`AC-07`, `AC-10`–`AC-12`, `MV-01`–`MV-03` | resolved_with_environment_note | Static, unit and focused mocked Playwright exits pass. Authenticated clone navigation provides provider-backed sending/partial/cancelled/resend/contracting and complete role evidence; all required visual comparisons and supplemental dialog/99% captures are closed with the documented viewport notes. |
| `FND-005` | implementation | Server type-check on integrated candidate | `EV-SERVER`; `EV-CORE-F1`, `EV-CORE-F2`; `AC-05` | resolved | `builder_core` replaced the ambiguous `.some()` narrowing with explicit `[] | [channel]` narrowing; Core architecture, types and 640 tests passed again. Server is resumed for its downstream exits. |
| `FND-006` | implementation/validation | Server Builder handoff | `EV-SERVER`; `AC-01`–`AC-13`, `MV-01`–`MV-03` | resolved_with_environment_note | Final Server and Inngest suites are green. Real curl health/auth preflight, canonical provider provisioning/reconciliation/cancellation/resend and role authorization evidence are green; the local `postgres` schema and canonical `hms-bucket` were subsequently repaired and revalidated. |
| `FND-007` | implementation/composition | Web focused route preflight | `EV-WEB`; `AC-01`–`AC-12`, `MV-01`–`MV-03` | resolved | `builder_server` imported `DocumentProductionModule` into `FormalizationMessagingModule` and corrected owner composition. Real Nest bootstrap now succeeds; `/health` and `/api/inngest` are 200, protected endpoints are 401 without bearer. The corrected server supplied the final authenticated browser evidence recorded in `EV-LIVE-CLONE` and `MV-02`. |
| `FND-008` | validation/tooling | Intake focused route command | `EV-WEB`; `AC-10`–`AC-12` | resolved | The canonical file path is preserved and the Spec/Plan command now uses an escaped `$` regex path; the escaped focused route passes. |
| `FND-009` | environment/validation | Intake focused route diagnostics | `EV-WEB`; `AC-12`, `MV-02` | resolved | Web Builder added deterministic local font/request handling while retaining app console/request assertions; escaped Intake focused route passed with the 390×844 screenshot. |
| `FND-010` | migration/integration | Server full and Inngest suites | `EV-SERVER`; `AC-06`, `AC-08`, `AC-13` | resolved | The generated migration is integrated; final full Server (114/114 files, 246/246 tests) and corrected Inngest (10/10 files, 31/31 tests) suites pass after correcting shared fixture assumptions and the transient Testcontainers startup path. |
| `FND-011` | migration/contract | Generated migration inspection | `EV-MIGRATION`; `AC-06`, `AC-08`, `AC-13` | resolved | Generated 0043 was reconciled with the Spec revision-6 contracted guarded transitions, its migration contract suite passed 2 tests, and the recreated `postgres` application schema accepted the complete migration journal plus seed without invented historical timestamps. |
| `FND-012` | structural/conformance | Integrated structural path gate | `PATH-01`; all affected paths | resolved | The candidate gate against the current integrated HEAD passed 239/239 declared paths with 0 failed checks; the earlier origin/develop result was a branch-baseline mismatch, not current missing implementation. |
| `FND-013` | review/blocking | Implementation reviewer | `EV-REVIEWER`; `AC-08`, `AC-09`, `FR-07` | resolved | Server contracting now throws inside the ambient transaction on Formalization CAS conflict; rollback coverage verifies no partial Intake state. |
| `FND-014` | review/high | Implementation reviewer | `EV-REVIEWER`; `AC-04`, `FR-03`, `FR-12` | resolved | Core/Web allow eligible linked recipients to view tracking-only state without operator controls and retain forbidden-user denial. |
| `FND-015` | review/high | Implementation reviewer | `EV-REVIEWER`; `AC-05`, `FR-04` | resolved | Resend state set now contains the six specified pre-signing/reconciliation states and excludes signing. |
| `FND-016` | review/high | Implementation reviewer | `EV-REVIEWER`; `AC-04`, `FR-01` | resolved | Tracking authorization no longer depends on resendability and covers historical confirmed/submitted states. |
| `FND-017` | review/high | Implementation reviewer | `EV-REVIEWER`; `AC-01`, `AC-10`, `FR-12` | resolved | Server serializes nullable projections as JSON null; Web preserves null and rejects malformed empty objects. |
| `FND-018` | review/medium | Implementation reviewer | `EV-REVIEWER`; `AC-06`, `FR-05` | resolved | Persisted cancellationRequestedAt is included in the Core/Validation/Web contract, hydrated after reload, and disables cancellation while pending. |
| `FND-019` | review/medium | Implementation reviewer | `EV-REVIEWER`; `AC-09` | resolved | Duplicate contracting uses the public Intake contract port and returns its actual persisted version. |
| `FND-020` | review/medium | Implementation reviewer | `EV-REVIEWER`; `AC-13`, `FR-08` | resolved | Frozen PDF cleanup now handles repository exceptions as well as undefined persistence results. |
| `FND-021` | environment/validation | Final coverage and Inngest gates | `EV-COVERAGE`, `EV-SERVER` | resolved | The Inngest host-port timeout is resolved by the fixture retry/cleanup correction and the full suite is green. The final direct Server coverage run passed 114/114 files and 247/247 tests with exit 0 in 1738.17s. |
| `FND-022` | implementation/integration | Disposable provider-backed resend flow | `EV-LIVE-RESEND`; `MV-01`; `AC-05` | resolved | The reproduced delivery failure had two contract mismatches: the resend context was corrected to the new invitation identifier, and its encrypted plaintext was aligned with the existing provider envelope `JSON.stringify({ token })`. Core regression coverage now asserts both context and envelope; focused Core/Server tests and the real Mailpit-backed resend completed green. |
| `FND-023` | implementation/validation | Missing pre-send UI action discovered while validating the ready configuration screenshot | `EV-WEB-SEND-ACTION`; `AC-01`, `AC-12` | resolved_with_environment_note | Added the ready-state `Enviar assinaturas` action and confirmation dialog in the `Assinaturas` tab, wired to the existing idempotent sending mutation. Focused component coverage and the clean focused browser route are green; the initial auth-fixture timeout was caused by reusing the non-test Vite server and was avoided with an isolated test port. |
| `FND-024` | implementation/integration | Collaborator invitation resend rejected with a generic channel error | `EV-SERVER`; `AC-05` | resolved | Resend was incorrectly checking the client-only consent-channel reader for every recipient, so collaborator signatories always appeared to have no available channel. The use case now validates the selected channel through `findPerson`, preserving client consent filtering and supporting collaborator contact channels; Core and controller coverage pass. |
| `FND-025` | implementation/contract | Aggregate progress remained at 0% while every document card showed `Assinado` | `EV-CORE-PROGRESS`; `AC-01`, `AC-02`, `AC-12` | resolved | The aggregate counted only `confirmed` documents, while the document cards correctly treated fully submitted signatures as signed during asynchronous PDF preservation. Progress now counts both `submitted` and `confirmed`; contracting readiness still requires `confirmed` documents and signed artifacts. |
| `FND-026` | implementation/UI | Main-page configuration badge showed `Pronto para envio` after signature request confirmation | `EV-WEB-CONFIRMED-STATUS`; `EV-VIS-CONFIRMED-STATUS` | resolved | The summary now receives the current signature-request status and gives `confirmed` precedence over `ready_for_sending`, rendering the success badge `Confirmado`. |
| `FND-027` | structural/conformance/tooling | Final Spec-path reconciliation and staged test whitespace | `PATH-01`; `EV-WEB-SIGNATORY-AVATAR` | resolved | Four Web paths were correctly classified as `Create` because they are absent from the selected `HEAD` baseline; the existing panel entry remains `Modify`. The staged Formalization REST test was refreshed to the single-newline EOF, and both worktree and index diff checks pass. |

## Lessons learned

| Lesson | Source finding | Authority disposition |
| --- | --- | --- |
| Structural path evidence must be captured only on the integrated candidate when unrelated worktree changes are present. | `FND-001` | No authority change; already covered by `documentation/tooling.md` and `documentation/sdd.md`. |
| Existing behavior tests must be reconciled to an amended transport projection while preserving strict authorization failures. | `FND-002` | No authority change; the Spec and `documentation/rules/use-case-testing-rules.md` already require current contract assertions and authorization truth tables. |
| A temporary compatibility relaxation is not an acceptable substitute for an explicit Spec contract. | `FND-003` | No authority change; enforce the revision-6 contract in Core and record any required implementation correction. |
| Every ownership Builder handoff requires Orchestrator reruns of the declared sensors on the shared candidate. | `EV-CORE-F1`; `EV-CORE-F2` | No authority change; required by `documentation/sdd.md` and `implement-spec`. |
| Static Web checks do not close a UI task without current behavior and visual evidence. | `FND-004` | No authority change; already required by the UI, routing, widget-testing and SDD authorities. |
| Cross-package type errors must be corrected by the owning package Builder before downstream server validation can close. | `FND-005` | No authority change; already required by the ownership and integrated-sensor rules. |
| A Server Builder handoff is incomplete until its tests and real boundary scenarios run on the corrected candidate. | `FND-006` | No authority change; already required by `controllers-testing-rules.md`, `jobs-testing-rules.md` and `documentation/sdd.md`. |
| Every newly injected provider must be exported/imported through the owning Nest module before browser or REST evidence is attempted. | `FND-007` | No authority change; already covered by `server-app-layer-rules.md` and the authenticated-browser preflight. |
| Dynamic route test paths containing `$` must use an executable Playwright selector/escape while preserving the canonical file path. | `FND-008` | No authority change; the Spec revision-6 command uses the executable escaped path while preserving the canonical filename. |
| Browser visual diagnostics must not depend on DNS availability of third-party fonts. | `FND-009` | No authority change; use deterministic local/test-scoped font handling and retain console-error checks for application requests. |
| Generated migration output must preserve required guarded data-transition semantics and must not silently reuse an occupied migration identity. | `FND-011` | No authority change after the revision-6 migration reconciliation; local occupied-id state remains an environment note. |
| Structural path gates are implementation-completeness checks; a passing package sensor cannot close missing canonical paths. | `FND-012` | No authority change; required by `documentation/sdd.md` and `documentation/tooling.md`. |
| Test databases must receive the generated migration before model-backed Server/Inngest evidence is interpreted as a feature failure. | `FND-010` | No authority change; required by `database-layer-rules.md`, `documentation/tooling.md` and the Plan’s F5 gate. |
| Invitation delivery encryption and decryption must share both the invitation identifier context and the provider’s established `{ token }` envelope across the Core event producer and delivery provider. | `FND-022` | No authority change; the existing provider contract already defined both expectations. Core regression coverage was added at the event boundary; no global rule change was warranted. |
| A ready-to-send configuration must expose the existing confirmation mutation at the owning UI boundary; a backend action alone is not an operable browser flow. | `FND-023` | No authority change; the Spec already requires the pre-send `Assinaturas` tab to own review/start. |
| Aggregate signature progress must reflect completed signer submissions before asynchronous signed-PDF preservation confirms the document, while contracting readiness remains artifact-backed. | `FND-025` | No authority change; the corrected behavior is now explicit in the `completedDocuments` response contract and regression coverage. |
| Main-page signature status must come from the current request when available; configuration readiness alone must not mask a confirmed request. | `FND-026` | No authority change; the corrected precedence is now explicit in the Spec and summary-hook regression coverage. |
| Candidate path classifications must describe the selected baseline, and closure checks must cover both worktree and index whitespace. | `FND-027` | No authority change; the structural checker and `documentation/tooling.md` already define these closure requirements. |

## PR CI quality gate

<!-- Populate during conclude-spec. The head SHA identifies the PR revision checked by CI; it
is not SDD current-commit metadata. Retain failed and superseded-head runs as history. -->

| ID | Workflow | Head SHA | Result | Run |
| --- | --- | --- | --- | --- |
| `CI-01` | Applicable PR workflows | — | pending | — |

## History

| Date/Time | Event |
| --- | --- |
| `2026-09-08 18:25` | Evaluation created for Spec revision `5`; authority, Docker/Auth/Server preflight, seed resolution, baseline conformance and `builder_core` activation recorded before feature edits. |
| `2026-09-08 18:43` | Orchestrator verified the `builder_core` handoff and reran the Core test suite; FND-002 and FND-003 opened, affected evidence invalidated, and the same Builder is resumed. |
| `2026-09-08 19:00` | `builder_core` completed F1/F2-T1; Orchestrator reran architecture, types and 640 Core tests, resolved FND-002/FND-003, and activated `builder_validation` for F2-T2. |
| `2026-09-08 19:06` | `builder_validation` completed F2-T2; Orchestrator reran architecture, types and lint successfully, then prepared Wave 3 Server/Web activation. |
| `2026-09-08 19:10` | Activated `builder_server` Halley for F3-T1 and `builder_web` Wegener for F4-T1 in parallel after F2 Core/Validation passed; generated migration outputs remain Orchestrator-owned. |
| `2026-09-08 20:44` | Server full and Inngest suites completed red because generated schema changes were not yet applied; FND-010 opened and F5 migration generation is activated. |
| `2026-09-08 23:20` | Final integrated Server suite passed 113/113 files and 244/244 tests; final Inngest suite passed 10/10 files and 31/31 tests. The shared fixture corrections were verified. |
| `2026-09-08 23:31` | Final Web unit suite passed 167/167 files and 545/545 tests; the focused Formalization/Intake Playwright command passed 7/7. Static Web checks and production build passed; route generation retained the existing non-route test-file warning. |
| `2026-09-08 23:34` | Authenticated browser preflight reached `/home` with the seeded administrator. Feature navigation exposed shared-DB `/clients` 500s because migration 0043 is absent from public tables while its id is occupied; no destructive repair was attempted. |
| `2026-09-08 23:36` | Fresh structural gate passed 239/239 declared paths with 0 failed checks (`changedFilesCount=271`, `unrelatedChangedFilesCount=32`). The single implementation reviewer was activated read-only; full coverage remained in progress. |
| `2026-09-09 00:56` | Final reviewer recheck found no remaining actionable implementation defects after the Validation and Server ownership corrections. Structural counts were refreshed to 272 changed and 33 unrelated files. |
| `2026-09-09 01:07` | Focused Inngest reconcile job passed 4/4, but the full Inngest suite remained environment-red at 9/10 files, 27 passed and 4 skipped because Testcontainers timed out waiting for host ports. Post-correction build passed. |
| `2026-09-09 04:49` | Final post-correction full coverage exited 1 at `server#test:coverage` after 30m52s; Core/Web tasks completed, but the Server coverage gate did not complete green. Docker/Auth/Server health remained green, and no destructive database repair was attempted. |
| `2026-09-08 21:12` | F5 generated 0043 SQL/snapshot/journal; local apply exited 1 with an occupied migration id and absent columns, and inspection found missing guarded backfills, opening FND-011. Web Intake evidence was recaptured green with deterministic fonts. |
| `2026-09-08 21:34` | Integrated structural gate ran after generated artifacts: 144/239 paths passed and 182 checks failed; FND-012 opened for missing canonical Create paths and baseline classification mismatches. |
| `2026-09-08 19:18` | Web Builder handoff was paused before required unit/focused-route/Playwright exits; FND-004 opened, Web evidence remains pending, and the same Builder is resumed for validation. |
| `2026-09-08 19:24` | Server handoff exposed FND-005 integrated Core type failure and FND-006 incomplete Server evidence; Core is resumed for correction and Server remains in progress. |
| `2026-09-08 19:31` | `builder_core` resolved FND-005 and reran all Core sensors successfully; `builder_server` is resumed for downstream type/test/Inngest/curl exits. |
| `2026-09-08 20:02` | Web unit suite passed; focused routes were blocked by the Server Nest composition failure and an absent Intake test path, opening FND-007 and invalidating browser evidence. |
| `2026-09-08 20:14` | `builder_server` resolved FND-007; real Nest bootstrap and protected-route preflight passed. Server static/test/Inngest reruns and Web browser evidence remain pending. |
| `2026-09-08 20:27` | Web created the missing Intake route test; literal `$intakeId` command and external font diagnostics failed, opening FND-008/FND-009 while preserving the escaped run as incomplete evidence. |
| `2026-09-09 08:18` | The first post-correction aggregate coverage invocation ended with exit 0 before Vitest emitted its final summary and only six Server coverage fragments were present; the same-reviewer recheck correctly rejected it as incomplete evidence. |
| `2026-09-09 08:25` | `builder_server` corrected the Inngest fixture with transient host-port retries and partial-container cleanup; the full suite passed 10/10 files and 31/31 tests in 717.25s. |
| `2026-09-09 08:30` | Restored a disposable PostgreSQL 17 clone on port 55432, applied migration 0043 and verified an idempotent `drizzle-kit migrate`; authenticated Playwright navigation reached `/home`, Formalization tracking and the main confirmation page with no console or failed-request evidence. The shared DB was not modified. |
| `2026-09-09 08:34` | Current structural gate passed 239/239 declared paths with 0 failed checks (`changedFilesCount=273`, `unrelatedChangedFilesCount=34`). Direct uncached Server coverage was started with persistent output at `/tmp/hms-server-coverage.log`; final coverage remains open. |
| `2026-09-09 08:51` | Direct uncached Server coverage completed green: 114/114 files and 247/247 tests passed, exit 0, duration 1618.37s, with the V8 report generated. FND-021 is resolved; MV/manual and exact visual evidence remain open. |
| `2026-09-09 09:19` | Recreated the disposable PostgreSQL 17 clone, seeded contracted Intake fixtures with and without a Case, and captured fresh real-server Playwright evidence at `1200×2300`, `1200×1240`, `1200×900` and `390×844`. The captures were inspected against all terminal Intake references; the Web Builder corrected the Formalization/Case card visual deltas, and the current focused suite passed 6 files/10 tests plus Web code/architecture/type checks. Mixed/sending provider states, role-matrix evidence and action persistence remain open. |
| `2026-09-09 09:31` | After the visual correction, the full Web suite passed 167/167 files and 553/553 tests in 120.79s; the structural gate remained 239/239 declared paths with 0 failed checks. A fresh live `390×844` contracted Intake capture confirmed no horizontal overflow and clean feature requests/console. Same-reviewer read-only recheck of the corrected cards was requested; provider mixed/sending and role evidence remain open. |
| `2026-09-09 09:47` | Rechecked the final success-tinted Formalization panel and captured fresh exact card/full-page evidence for both terminal Intake states. Final focused card suite passed 2 files/4 tests; Web code/types/architecture and structural conformance passed (`239/239` paths, `0` failed checks). The same implementation Reviewer completed a clean read-only visual recheck. Provider mixed/sending and role evidence remain open. |
| `2026-09-09 12:04` | On the disposable clone, the real HMS confirmation path created request `45a27836-71d4-46ff-852f-c7cc5e09caf3`; Inngest run `01M23BR3M9ETXT3N52H3XZAMA6` completed, Documenso envelope `envelope_suoyzlrdbxisoiym` reached `PENDING`, and HMS persisted `sending`. |
| `2026-09-09 12:15` | A real Documenso signer completed both fields, provider state became `SIGNED/NOT_SIGNED`, reconciliation persisted `partially_submitted`, and the HMS `1200×900` capture rendered the mixed/partial tracking state with zero HMS console errors. |
| `2026-09-09 12:17` | The admin cancellation dialog was exercised with a reason; HMS cancel returned 200, provider state became `CANCELLED`, and the clone request persisted `cancelled` with both cancellation and terminal timestamps. Fresh role contexts verified admin/assigned-lawyer 200, linked `lawyer` tracking-only after fixture assignment isolation, forbidden collaborators 403 and client collaborator-completion 404. |
| `2026-09-09 13:00` | Reproduced and fixed the resend delivery defect: Core now encrypts the provider-compatible `{ token }` envelope under the new invitation id. Core passed 129 files/644 tests; focused Server provider/controller tests passed 2 files/7 tests; a fresh Auth/REST/Inngest/Mailpit rerun delivered generation 4, revoked generations 1–3, and captured resend/cancellation/99% evidence at `1200×900`. Evaluation and Plan are ready; Spec closure remains a publication-authority step. |
| `2026-09-09 13:36` | Final post-correction sensors are green: direct Server coverage passed 114/114 files and 247/247 tests in 1738.17s; Web passed 166/166 files and 544/544 tests; build passed; and the final structural gate passed 239/239 paths with 0 failed checks (`changedFilesCount=304`, `unrelatedChangedFilesCount=65`). |
| `2026-09-10` | Ready-to-send correction validated: Web passed 166/166 files and 547/547 tests, focused Playwright passed 2/2 on an isolated server, production build passed, and the refreshed structural gate passed 239/239 paths with 0 failed checks (`changedFilesCount=330`, `unrelatedChangedFilesCount=91`). |
| `2026-09-08 20:44` | Server full and Inngest suites completed red because generated schema changes were not yet applied; FND-010 opened and F5 migration generation is activated. |
| `2026-09-10 12:45` | Added the missing pre-send `Enviar assinaturas` action and confirmation dialog; focused Web component coverage passed 7/7. The first Playwright attempt timed out in the reused non-test auth fixture; a clean isolated-port rerun passed 2/2 and captured the ready-state action. |
| `2026-09-10 13:52` | Added the shared `CollaboratorAvatar` to every signature-tracking row, verified initials rendering, and repaired the existing refresh-summary prop contract; focused tracking-panel coverage passed 10/10, Web types and architecture passed, and the clean browser route remained green at 2/2. |
| `2026-09-10 14:02` | Fixed collaborator invitation resend: the use case now checks the recipient’s resolved available channel instead of the client-only consent lookup. Core resend coverage passed 4/4, the real Server controller suite passed 3/3, and Core/Server/Web type checks passed. |
| `2026-09-10 13:02` | Moved send-dialog state and idempotency handling into the owning widget hook; Web code/architecture/types passed, the full Web suite passed 166/166 files and 547/547 tests, the focused route passed 2/2, the structural gate passed 239/239 paths, and the fresh ready-state screenshot was inspected. |
| `2026-09-10 16:27` | Created authenticated fixture `INT-0023` (`32e85063-e2d8-48f0-b471-2018da518f98`) with Formalization `b2a571d9-9692-412f-a7dc-ebaa50937d49`: advanced the Intake to `in_formalization`, saved the commercial-form draft with all required fields, corrected the responsible collaborator, verified the open-form guard blocks Formalization documents, and captured the fresh zero-error Intake snapshot showing `Em Formalização`. |
| `2026-09-10 22:22` | Corrected aggregate signature progress to count both `submitted` and `confirmed` request documents. Focused Core type-check and regression tests passed; prior aggregate evidence was invalidated and FND-025 resolved. |
| `2026-09-11 01:49` | Corrected the main-page configuration badge to prioritize the current confirmed signature request over `ready_for_sending`. Focused Web coverage passed 2/2 files and 7/7 tests; authenticated Playwright validation rendered `Confirmado` at `1200×900` with 200 feature responses and no console errors or warnings. |
