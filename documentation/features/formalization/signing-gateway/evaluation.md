---
feature: "formalization/signing-gateway"
spec: ./spec.md
plan: ./plan.md
spec_revision: 13
status: ready
prd: https://plataformahms.atlassian.net/wiki/spaces/~712020e69febeaca304dffb2d8d156ea17d2c4/pages/24051713
jira_tickets:
  - SCRUM-144
  - SCRUM-140
  - SCRUM-128
updated_at: 2026-09-07
---

# Evaluation

Evaluation of Spec revision `13` against the current implementation.

Current result: `ready`; revision 13 aggregates every current repository change into this delivery ledger, and PATH-03 passes structurally. Revision 12's accepted-risk waiver remains active: waived items are non-blocking, not passed or technically resolved. EV-234 records the latest authenticated-admin/client Gateway isolation regression coverage. Publication, pull-request CI and final closure remain pending.

## Acceptance matrix

| Criterion | Evidence | Status |
| --- | --- | --- |
| `CA-01` | `EV-02`; `MV-01`; revision-12 waiver | `waived` |
| `CA-02` | `EV-02`; `MV-01`; revision-12 waiver | `waived` |
| `CA-03` | `EV-02`; `MV-01`; revision-12 waiver | `waived` |
| `CA-04` | `EV-02`; `MV-03`; revision-12 waiver | `waived` |
| `CA-05` | `EV-02`; `MV-02`; revision-12 waiver | `waived` |
| `CA-06` | `EV-02`; `MV-01`; revision-12 waiver | `waived` |
| `CA-07` | `EV-02`; `MV-05`; revision-12 waiver | `waived` |
| `CA-08` | `EV-02`; `MV-07`; revision-12 waiver | `waived` |
| `CA-09` | `EV-02`; `MV-02`; revision-12 waiver | `waived` |
| `CA-10` | `EV-02`; `MV-01`; revision-12 waiver | `waived` |
| `CA-11` | `EV-02`; `MV-04`; revision-12 waiver | `waived` |
| `CA-12` | `EV-02`; `MV-04`; revision-12 waiver | `waived` |
| `CA-13` | `EV-02`; `MV-04`; revision-12 waiver | `waived` |
| `CA-14` | `EV-02`; `MV-05`; `MV-08`; revision-12 waiver | `waived` |
| `CA-15` | `EV-02`; `MV-06`; revision-12 waiver | `waived` |
| `CA-16` | `EV-02`; `MV-08`; revision-12 waiver | `waived` |
| `CA-17` | `EV-02`; `MV-02`; `MV-06`; revision-12 waiver | `waived` |
| `CA-18` | `EV-02`; `MV-01`; revision-12 waiver | `waived` |
| `CA-19` | `EV-02`; `MV-02`; `MV-04`; revision-12 waiver | `waived` |
| `CA-20` | `EV-02`; `MV-02`; `MV-07`; revision-12 waiver | `waived` |
| `CA-21` | `EV-02`; `MV-04`; `MV-06`; revision-12 waiver | `waived` |
| `CA-22` | `EV-02`; `MV-02`; `MV-06`; revision-12 waiver | `waived` |

## Automated and runtime evidence

| ID | Layer | Command or scenario | Result | Status |
| --- | --- | --- | --- | --- |
| `EV-01` | Cross-layer | `docker compose ps -a`; `curl -fsS http://localhost:8000/auth/v1/health`; `curl -fsS http://localhost:5555/health` | Supabase DB/Auth healthy; Auth reports GoTrue v2.189.0; Nest reports `status: ok`, database and Supabase `UP`. Server session `6671` and Web session `17381` are running; Web is ready at `http://localhost:5000/`. | `passed` |
| `EV-02` | Cross-layer | Baseline comparison of revision-5 required tree, contracts, exclusions and current repository | Current code contains only pre-send Formalization configuration and no request/document/recipient/invitation/provider/Gateway implementation; required feature paths and behaviors remain pending. No source edits made before Builder activation. | `pending` |
| `EV-03` | Core | F1 focused Core tests, lint, types and architecture checks | Not run; awaiting `builder_core` implementation and Orchestrator verification. | `pending` |
| `EV-04` | Validation | F2 Validation lint, types, architecture and consumer compilation | Not run; dependency on F1. | `pending` |
| `EV-05` | Database | F3 repository, transaction, constraint and concurrency checks | Not run; dependency on F1. | `pending` |
| `EV-06` | Server | F5 Nest/REST/provider/job checks and real curl scenarios | Not run; dependency on F4. | `pending` |
| `EV-07` | Web | F6/F7 focused tests, Playwright CLI behavior, console and failed-request checks | Not run; dependency on F5. | `pending` |
| `EV-08` | Cross-layer | F8 integrated quality commands, coverage, build and generated-artifact review | Not run; implementation pending. | `pending` |
| `EV-09` | Core | `builder_core` activation record | Agent `01a05eec-6278-75a0-88d2-993d7d4375a3` (`builder_core`); Spec revision 5; allowed paths `packages/core/**`; assigned phase F1-T1; RF-01–RF-27/RF-29–RF-38; CA-01–CA-15/CA-17–CA-22; prohibited paths Validation, Server, Web, Spec, Plan and Evaluation; Rule Pack Core/code-conventions/use-case-testing; design reference `design/manifest.md`; exits are focused Core code/type/unit/architecture checks and contract/tree verification. | `passed` |
| `EV-10` | Core | `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter @hms/core test`; `pnpm --filter @hms/core lint` | Types and architecture passed; existing Core suite passed with 99 files/299 tests; lint exited 0 with warnings. The signing declarations compile, but F1 signing use cases and behavior tests are absent. | `failed` |
| `EV-11` | Core | `pnpm --filter @hms/core test`; `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter @hms/core lint`; Core boundary scan; Spec Core-path scan | Core tests passed: 122 files/322 tests; types and architecture passed; lint exited 0 with 71 warnings and 2 infos; no Validation imports; 180 Spec Core paths checked with 0 missing. Contract-depth review found substantial behavior/security/concurrency coverage gaps. | `failed` |
| `EV-12` | Core | `git diff --check -- packages/core` after resumed F1 correction | Passed whitespace check, but no new correction files or focused sensors were produced. Placeholder identifiers/bytes, plaintext identity fallbacks, missing secret-verifier port and smoke-level lifecycle tests remain. | `failed` |
| `EV-13` | Core | `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter @hms/core test`; `git diff --check -- packages/core` on the current candidate | Types failed in `verify-signature-otp-use-case.test.ts` because the verifier mock contract is invalid; architecture and whitespace checks passed; Core tests failed 3/326 in OTP verification, including missing verifier setup and missing guard update. Prior EV-11 is stale. | `failed` |
| `EV-14` | Core | `builder_fix_core` activation record | Agent `01a05f0b-c869-7d40-8370-e4825ed92d63` (`builder_fix_core`); Spec revision 5; allowed paths `packages/core/**`; assigned independent FND-005 correction; prohibited paths Validation, Server, Web, Spec, Plan and Evaluation; exits are current Core tests, type-check, architecture and lint. | `passed` |
| `EV-15` | Core | `pnpm --filter @hms/core test`; `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter @hms/core lint`; `git diff --check -- packages/core` after FND-005 correction | All Core tests passed: 122 files/326 tests; type-check and architecture passed; lint exited 0 with unrelated existing warnings; diff check passed. OTP guard-update and verifier behavior now execute. FND-003/FND-004 remain open. | `passed` |
| `EV-16` | Core | Current-candidate contract inspection with `rg` over `packages/core/src/formalization` | Found raw-value hash fallbacks, empty/placeholder provider credentials and proxy paths, and insufficient lifecycle/security behavior evidence in the current Core slice. F1 remains non-conformant despite passing basic sensors. | `failed` |
| `EV-17` | Core | `builder_fix_core` activation record for FND-006 | Agent `01a05f10-2946-79d0-81bd-92a701f82c05` (`builder_fix_core`); Spec revision 5; allowed paths `packages/core/**`; independent security/contract correction; prohibited paths Validation, Server, Web, Spec, Plan and Evaluation; exits are focused behavior tests, Core type-check, architecture, lint and contract scan. | `passed` |
| `EV-18` | Core | Resumed `builder_fix_core` correction and current worktree inspection | No files changed and no new sensors completed; raw hash fallbacks, empty provider/proxy values and missing explicit secret-verifier boundary remain. Correction Builder was stopped after no progress. `EV-16` remains current for these findings. | `failed` |
| `EV-19` | Core | Fresh `builder_fix_core` activation record for resumed F1 correction | Agent `01a05f1c-f098-7ff3-a57e-dfd3a5cc48c7` (`builder_fix_core`); Spec revision 5; allowed paths `packages/core/**`; assigned FND-003/FND-004/FND-006 correction; prohibited paths Validation, Server, Web, Spec, Plan and Evaluation; exits are security-contract behavior tests, Core test/type/architecture/lint sensors and placeholder scan. | `passed` |
| `EV-20` | Core | Interrupted correction handoff inspection | Partial hasher/verifier/MAC/document-metadata interfaces and six use-case edits were left without current sensors; raw fallbacks and empty values remain, and the candidate may have type/test failures. | `failed` |
| `EV-21` | Core | Fresh `builder_fix_core` activation record for FND-007 | Agent `01a05f24-1337-7ae2-b484-21e26cfeb07a` (`builder_fix_core`); Spec revision 5; allowed paths `packages/core/**`; assigned completion of partial FND-006 wiring and restoration of Core sensors; prohibited paths Validation, Server, Web, Spec, Plan and Evaluation; exits are current Core tests, type-check, architecture, lint and placeholder scan. | `passed` |
| `EV-22` | Core | `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core test`; `git diff --check -- packages/core` after interrupted correction | Core tests passed 122/122 files and 326/326 tests; diff check passed; type-check failed with two remaining partial-edit errors: missing `FormalizationSignatureNotReadyError` and invalid `provider_recipient_credential` artifact kind. | `failed` |
| `EV-23` | Core | Narrow `builder_fix_core` activation record for FND-007 | Agent `01a05f2e-01de-7a71-afc6-19bcbbe62e7c` (`builder_fix_core`); Spec revision 5; allowed paths `packages/core/**`; assigned only the two current type errors; prohibited paths Validation, Server, Web, Spec, Plan and Evaluation; exits are current Core type/test/architecture/lint and diff checks. | `passed` |
| `EV-24` | Core | `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core test`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter @hms/core lint`; `git diff --check -- packages/core` after FND-007 correction | Type-check, architecture, tests (122 files/326 tests), lint and diff checks passed; FND-007 is resolved. FND-003/FND-004/FND-006 remain active. | `passed` |
| `EV-25` | Core | Current-candidate `rg` scans for raw hash fallbacks and empty provider/proxy/result values; `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core test` | No prohibited fallback/placeholder patterns found; type-check passed; tests passed 122 files/326 tests. Security placeholder finding is resolved, while lifecycle coverage remains to be strengthened. | `passed` |
| `EV-26` | Core | `builder_fix_core` activation record for lifecycle evidence correction | Agent `01a05f34-67e4-7ed2-8145-8a439c465c2d` (`builder_fix_core`); Spec revision 5; allowed paths `packages/core/**`; assigned FND-003/FND-004 behavior-focused test strengthening; prohibited paths Validation, Server, Web, Spec, Plan and Evaluation; exits are fake-clock/race/idempotency tests plus current Core sensors. | `passed` |
| `EV-27` | Core | Current-candidate lifecycle test inspection and sensor run | OTP tests now cover cooldown, rolling limit, lock, expiry, rotation, race and session expiry; current send/provision/reconcile/cancel/submission tests remain smoke-level, with no broader lifecycle correction sensor evidence yet. Core tests/types/architecture/lint pass on the current candidate. | `failed` |
| `EV-28` | Core | `pnpm --filter @hms/core test`; `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter @hms/core lint`; `git diff --check -- packages/core`; Core path/export/boundary scan | Current integrated Core candidate passes: 122 files/363 tests; type-check, architecture (986 modules/3,131 dependencies), lint exit 0 with existing warnings, diff check, no Core→Validation imports, complete declared Core path/export inventory, and no raw hash/empty provider/proxy/result placeholders. F1 exit passes; artifact download/storage orchestration remains a later Server concern. | `passed` |
| `EV-29` | Validation | `builder_validation` activation record | Agent `01a05f45-8a3c-7d60-b5b3-63af766fb2ee` (`builder_validation`); Spec revision 5; allowed paths `packages/validation/**`; assigned F2-T1; RF/CA mapping from Plan F2; prohibited paths Server, Web, Core edits, Spec, Plan and Evaluation; design reference `design/manifest.md`; exits are strict schema/export/type/architecture checks with no local Validation test suite. | `passed` |
| `EV-30` | Database | `builder_server` activation record | Agent `01a05f45-80c0-7220-9f7d-0f0b8e8529e6` (`builder_server`); Spec revision 5; allowed paths `apps/server/**`; assigned F3-T1; RF-09–RF-25/RF-31–RF-38 and CA-09–CA-14/CA-18–CA-21; prohibited paths migrations, package/lockfiles, generated artifacts, Spec, Plan and Evaluation; exits are focused real-PostgreSQL persistence/transaction checks. | `passed` |
| `EV-31` | Validation | `pnpm --filter @hms/validation lint`; `pnpm --filter @hms/validation check-types`; `pnpm --filter @hms/validation check:architecture`; schema/export inspection | Validation package sensors pass; strict Zod REST/event schemas, canonical Core values, six-digit ASCII OTP, email-only channel, unknown-key rejection and no provider secret/link/contact fields are present. Consumer checks are deferred until concurrent Server/Core changes stabilize. | `passed` |
| `EV-32` | Server/Database | `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; `pnpm --filter server test` | Correct Server code and architecture checks pass. Type-check fails because `FormalizationSignatureSourceReader` and its fixture mock do not yet implement the Core authentication-source methods. The broad Server suite was interrupted after the Formalization controller regression file reported 10 failed tests; repeated PostgreSQL NOTICE output made this unsuitable as focused F3 evidence. | `failed` |
| `EV-33` | Server | `builder_fix_server` activation record | Agent `01a05f65-c79c-7071-a9e2-297c93878840` (`builder_fix_server`); Spec revision 5; allowed paths `apps/server/**`; assigned F3 correction for the source-reader contract and focused persistence/transaction tests; prohibited paths migrations, package/lockfiles, generated artifacts, Spec, Plan and Evaluation; exits are current Server type/code/architecture checks plus focused real-PostgreSQL F3 tests. The Builder was stopped after two timed-out progress windows without a handoff or verifiable changes. | `failed` |
| `EV-34` | Server | `builder_fix_server` activation record | Agent `01a05f6b-edce-71c2-be67-6c650e8ed06a` (`builder_fix_server`); Spec revision 5; allowed paths `apps/server/**`; assigned F3 correction for the source-reader contract and focused persistence/transaction tests; prohibited paths migrations, package/lockfiles, generated artifacts, Spec, Plan and Evaluation; exits are current Server type/code/architecture checks plus focused real-PostgreSQL F3 tests. The Builder was stopped after two timed-out progress windows without a handoff or verifiable changes. | `failed` |
| `EV-35` | Server | `builder_fix_server` activation record | Agent `01a05f71-db00-7e42-bbb2-f775a45e9366` (`builder_fix_server`); Spec revision 5; allowed paths `apps/server/**`; assigned only the source-reader Core consumer correction and fixture update, with current Server type/code/architecture checks; prohibited paths migrations, package/lockfiles, generated artifacts, Spec, Plan and Evaluation. | `passed` |
| `EV-36` | Server | `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; `pnpm --filter server exec vitest run src/formalization/provision/tests/formalization-signature-source-reader.test.ts` | Current Server type-check passed; Biome checked 756 files with no fixes; architecture passed with 822 modules/3,915 dependencies; focused source-reader tests passed 5/5. FND-008 is resolved. F3 persistence/transaction evidence is still missing. | `passed` |
| `EV-37` | Server/Database | `builder_server` activation record for the remaining F3 exit | Agent `01a05f75-12e5-7af3-a039-81fade6e4d0b` (`builder_server`); Spec revision 5; allowed paths `apps/server/**`; assigned focused Drizzle repository/mapper/transaction tests for the implemented request graph, without migrations or generated artifacts; exits are exact focused tests plus current Server sensors. | `passed` |
| `EV-38` | Server/Database | Builder feasibility check for focused F3 PostgreSQL tests | No files changed. The repository `DatabaseFixture` applies only checked-in migrations, while the F3 tables have no migration yet; adding duplicate test-only DDL would violate the database rules and bypass the migration contract. F3 persistence evidence is therefore blocked until Orchestrator-owned F4 migration generation. | `failed` |
| `EV-39` | Database | `pnpm --filter server db:migration:generate`; `pnpm --filter server db:migration:apply` | Migration generation produced `0042_signing_gateway` after Orchestrator naming normalization. Forward apply failed with PostgreSQL identifier-truncation notices for generated foreign-key/unique names; the migration is not accepted until source constraint names are made collision-safe and the migration is regenerated/applied cleanly. | `failed` |
| `EV-40` | Server/Database | `builder_fix_server` activation record for PostgreSQL-safe model names | Agent `01a05f7a-215b-75d3-8b13-28c7e0f6ad84` (`builder_fix_server`); Spec revision 5; allowed paths `apps/server/src/formalization/database/**` plus Server tests; assigned explicit <=63-byte unique names for new foreign keys and column-level unique constraints; prohibited paths migrations/generated artifacts, package/lockfiles, Core, Validation, Web, Spec, Plan and Evaluation; exits are current Server type/code/architecture checks. The Builder stopped without changes or checks. | `failed` |
| `EV-41` | Server/Database | Replacement `builder_fix_server` activation record for PostgreSQL-safe model names | Agent `01a05f7d-5045-7471-97a8-911209d2a01f` (`builder_fix_server`); Spec revision 5; allowed paths `apps/server/src/formalization/database/**` plus Server tests; assigned explicit <=63-byte unique names for every new foreign key and column-level unique constraint; prohibited paths migrations/generated artifacts, package/lockfiles, Core, Validation, Web, Spec, Plan and Evaluation; the Builder left only a partial conversion and no handoff/check results. | `failed` |
| `EV-42` | Server/Database | Narrow replacement `builder_fix_server` activation for remaining unnamed relations | Agent `01a05f83-777a-7ae3-87b1-f48a070b31e7` (`builder_fix_server`); Spec revision 5; allowed paths `apps/server/src/formalization/database/drizzle/models/**`; assigned only the remaining inline foreign-key and column-unique declarations listed by the Orchestrator scan; prohibited paths migrations/generated artifacts, package/lockfiles, Core, Validation, Web, Spec, Plan and Evaluation; all remaining declarations were converted to explicit globally unique names <=63 bytes. | `passed` |
| `EV-43` | Database | `pnpm --filter server db:migration:generate`; `pnpm --filter server db:migration:apply`; generated SQL identifier scan | Regenerated `0042_signing_gateway` from the corrected models; the SQL has no long `CREATE` constraint/index identifiers or duplicate truncated names. Forward migration applied successfully to the local PostgreSQL database; PostgreSQL emitted only notices for legacy dropped identifiers and successful truncation. FND-011 is resolved. | `passed` |
| `EV-44` | Server/Database | `builder_server` activation record after canonical migration | Agent `01a05f88-8eef-73a2-a856-a77ecfe11574` (`builder_server`); Spec revision 5; allowed paths `apps/server/**`; assigned focused real-PostgreSQL F3 repository/transaction tests now that migration `0042_signing_gateway` is applied; prohibited paths migrations/generated artifacts, package/lockfiles, Core, Validation, Web, Spec, Plan and Evaluation; exits are focused tests plus current Server sensors. | `passed` |
| `EV-45` | Server/Database | Builder review of F3 testability after migration | No files changed. Direct repository/mapper/model/database-adapter tests are prohibited by `documentation/rules/database-layer-rules.md`; current F3 has no new REST/application action through which the canonical schema can be exercised. F3 persistence behavior must be covered indirectly by F5 controller/flow integration tests. | `failed` |
| `EV-46` | Server/Database | `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; `pnpm --filter server db:migration:generate` after model correction | Current Server types pass; Biome checks 757 files with no fixes; architecture passes with 822 modules/3,915 dependencies; migration generation reports no schema changes after `0042_signing_gateway` was applied. The formatted canonical snapshot is now clean. F3 remains held for indirect integration evidence per FND-012. | `passed` |
| `EV-47` | Server | `builder_server` activation record for F5 internal sending REST slice | Agent `01a05f91-9e1c-7752-a2b6-bc6e01512159`; Spec revision 5; allowed paths `apps/server/src/formalization/**` and approved Communication paths; assigned thin review/confirm/status/cancel controllers with real Core use-case composition and integration tests, preserving Gateway/proxy routes as dark until the later public slice; prohibited paths migrations/generated artifacts, package/lockfiles, Core, Validation, Web, Spec, Plan and Evaluation. Activation completed, but the handoff was blocked by upstream contract gaps recorded in `EV-48`. | `passed` |
| `EV-48` | Server/Core | `builder_server` F5 contract preflight | No files changed. F5 cannot add the internal REST slice yet: Core has no status use case, no Server binding exists for `FormalizationSignatureDocumentMetadataReader`, and the cancel use case contract requires `requestId` while the REST command is keyed by `formalizationId`; thin-controller lookup would violate the boundary. | `failed` |
| `EV-49` | Core | `builder_fix_core` activation for FND-013 | Agent `01a05f97-c406-7c62-99bb-d3befff1a762`; allowed paths `packages/core/**` only; add status use case/tests and resolve the formalization-scoped cancellation boundary without editing Server, Validation, Web, or shared SDD artifacts. The Builder was stopped after no handoff or changes. | `failed` |
| `EV-50` | Core | Narrow replacement `builder_fix_core` activation for FND-013 | Agent `01a05f9c-117a-7162-81aa-c574d14473d5`; allowed paths `packages/core/src/formalization/use-cases/**`, focused use-case tests, and required Core barrels only; add the missing status use case and tests, preserving the existing Spec-exact cancel requestId contract. The Builder was stopped after no handoff or changes. | `failed` |
| `EV-51` | Core | Minimal replacement `builder_fix_core` activation for FND-013 | Agent `01a05f9f-e85e-7072-95eb-ad3b0224b52f`; allowed paths `packages/core/src/formalization/use-cases/get-formalization-signature-sending-status-use-case.ts`, its focused test, and the use-case barrel only; implement status lookup with existing repositories and domain errors. The Builder was stopped after no handoff or new use-case file. | `failed` |
| `EV-52` | Core | Final minimal `builder_fix_core` activation for FND-013 | Agent activation did not produce a handoff; the Orchestrator applied the one-file fallback after three Builder activations failed to progress. | `failed` |
| `EV-53` | Core | `pnpm --filter @hms/core exec vitest run src/formalization/use-cases/tests/get-formalization-signature-sending-status-use-case.test.ts`; `pnpm --filter @hms/core check-types`; Biome format/check | Added and validated `GetFormalizationSignatureSendingStatusUseCase` with authorization, progress counts, retry/cancel flags and focused tests: 1 file, 2 tests passed; Core type-check and formatting checks passed. | `passed` |
| `EV-54` | Server | `builder_server` reactivation for F5 internal sending composition | Agent `01a05fa5-1ef7-7630-bc10-6f0bd35c57f2`; allowed Server formalization/provision/rest/module/fixture paths and approved tests; implement metadata-reader binding, application composition and thin review/confirm/status/cancel controllers with real Nest integration coverage. The Builder was stopped after no handoff or feature changes. | `failed` |
| `EV-55` | Server | `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; focused status-controller test | Added and wired the private document metadata reader, Core sending-status composition, and the status route with UUID boundary coverage. Server types, Biome (759 files), architecture (824 modules/3,934 dependencies), and the focused Nest/Supertest test (1 test) passed. Review/confirm/cancel REST actions remain pending. | `passed` |
| `EV-56` | Server | `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; focused review/status/configuration controller tests | Added the formalization-scoped review route, confirm/cancel application composition, signed-secret HMAC adapter and confirm/cancel controllers. Current Server types, Biome (765 files), architecture (830 modules/3,978 dependencies), and three focused Nest/Supertest tests passed. End-to-end send persistence and provider/delivery work remain pending. | `passed` |
| `EV-57` | Integrated Core/Server | Final formatting, `pnpm --filter server check:types`, `pnpm --filter server check:code`, `pnpm --filter server check:architecture`, `pnpm --filter @hms/core check-types` | Final current-candidate sensors passed after the internal sending composition changes. No Web, provider, delivery, Gateway/proxy, webhook, migration-race, or authenticated-browser evidence was run in this turn. | `passed` |
| `EV-58` | Server | `builder_server` activation for F5 provider/delivery/Gateway backend | Agent `01a061dd-f4ee-7a93-afdc-c86f675654bf`; allowed `apps/server/src/formalization/provision/**`, `apps/server/src/formalization/messaging/**`, `apps/server/src/formalization/rest/**`, feature module/fixture/barrel paths and approved Communication paths; no handoff or changes were produced, so provider/delivery/Gateway remains pending. | `failed` |
| `EV-59` | Web | `builder_web` activation for F6 internal sending surface | Agent `01a061de-8abb-76a1-8d0a-e5ae28bff032`; allowed `apps/web/src/rest/services/formalization-service.ts`, `apps/web/src/ui/formalization/**` and colocated tests; no handoff or changes were produced because the Core service contract was missing at activation. | `failed` |
| `EV-60` | Server/Web | F5/F6 Builder handoff review | `builder_server` `01a061dd-f4ee-7a93-afdc-c86f675654bf` and `builder_web` `01a061de-8abb-76a1-8d0a-e5ae28bff032` completed without changes. Web is blocked by the missing Core REST service contract; provider/delivery/Gateway backend remains unimplemented. | `failed` |
| `EV-61` | Core | `builder_fix_core` activation for missing Formalization REST service contract | Agent `01a061e1-9e8b-74b1-b95f-9436eb02e58a`; allowed `packages/core/src/formalization/interfaces/formalization-service.ts` and required Core contract structures/barrels/tests only; no handoff or changes were produced. | `failed` |
| `EV-62` | Core | Core contract correction fallback | The Core Builder activation produced no handoff. The Orchestrator added the four existing Spec-defined FormalizationService methods using existing Core command/response structures; Core check-types passed and the Web consumer was then compiled. | `passed` |
| `EV-63` | Web | `builder_web` reactivation for F6 typed service and internal sending surface | Agent `01a061e4-2a9f-7c33-a066-a385896d7329`; the Builder produced no handoff or changes after the corrected Core contract was available, so the Orchestrator applied a bounded Web fallback and closed the activation. | `failed` |
| `EV-64` | Web/Core | `pnpm --filter web check:types`; `pnpm --filter web check:code`; `pnpm --filter web check:architecture`; focused sending-configuration test | Added the four typed Formalization REST adapter methods, the sending review/status action hook, and internal review/confirm/progress/cancel states. Removed unused imports from the new Core repository interfaces exposed by the Web compiler. Web type/code/architecture checks passed; focused UI test passed with 12 tests. | `passed` |
| `EV-65` | Web | `pnpm --filter web test` | Full Web suite passed: 110 test files and 403 tests. The expected jsdom `requestSubmit()` notice was non-failing; no test failures remained. | `passed` |
| `EV-66` | Server | `builder_server` bounded provider-adapter activation | Agent `01a061ee-dde2-74c1-9f22-bbcc2e6bc1bd`; allowed Server provision/messaging/REST/module/fixture/barrel paths and approved Communication paths; after a narrowed provider-only instruction, the Builder still produced no handoff or changes and was closed. | `failed` |
| `EV-67` | Web/Core | `pnpm --filter @hms/core check-types`; `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; `pnpm --filter web check:types`; `pnpm --filter web check:code`; `pnpm --filter web check:architecture`; focused Gateway/client tests | Added the isolated public Gateway client contract with in-memory CSRF storage, cookie-including request options, Validation response parsing, binary PDF mapping and no global sign-out callback. Current Core, Server and Web sensors passed; Gateway client tests passed (3 tests). | `passed` |
| `EV-68` | Web | `pnpm --filter web test` | Full Web suite after Gateway client/context integration passed: 111 test files and 407 tests. The existing jsdom `requestSubmit()` notice was non-failing. | `passed` |
| `EV-69` | Validation/Server | `git diff --check`; `pnpm --filter @hms/validation check-types`; `pnpm --filter @hms/validation check:architecture`; focused Server provision/controller tests | Validation type-check and architecture checks passed; focused Server source-reader and signature-sending controller coverage passed (3 files, 7 tests); no whitespace errors were reported. Validation has no `check:code` script, so no nonexistent command was treated as evidence. | `passed` |
| `EV-70` | Validation/Web | `pnpm --filter @hms/validation check-types`; `pnpm --filter @hms/validation check:architecture`; `pnpm --filter web check:types`; focused Gateway client tests | Promoted the public channel response to the named Validation schema `signatureGatewayChannelsSchema`; Validation and Web type/architecture checks passed and Gateway client tests remained green (3 tests). | `passed` |
| `EV-71` | Server | `builder_server` activation for server composition/provider/jobs/Gateway | Agent `01a06205-558d-7841-9735-ee8feddca263`; Spec revision 5; allowed Server formalization/provision/messaging/rest, required shared provider/job registration and approved REST examples; prohibited Core, Validation, Web, migrations/generated artifacts, package/lockfiles and SDD files. The Builder delivered the dedicated internal sending composition only; provider, jobs, public Gateway and proxy were not handed off. | `passed` |
| `EV-72` | Web | `builder_web` activation for public Signing Gateway experience | Agent `01a06205-6c74-7d42-9e40-4b1a8cd863fc`; Spec revision 5; allowed Web routes, `ui/signing-gateway/**`, Gateway service/tests and Axios adapter only; prohibited Server, Core, Validation, migrations/generated route metadata and SDD files. The Builder reported no files because the public route depends on the missing Server endpoints. | `failed` |
| `EV-73` | Server | `pnpm --filter server check:code`; `pnpm --filter server check:types`; focused sending-controller tests | Integrated the delivered `FormalizationSignatureSendingService` into the review, status, confirm and cancel controllers and `FormalizationModule`, keeping the existing application service out of this composition. Replaced generic cancellation errors with the stable domain conflict error. Biome checked 766 files, Server types passed, and 2 focused controller tests passed. Provider/delivery jobs, public Gateway/proxy and real persistence-flow evidence remain pending. | `passed` |
| `EV-74` | Runtime | `docker compose ps -a`; `curl -fsS http://localhost:8000/auth/v1/health`; `curl -fsS http://localhost:5555/health` | Compose database/Auth and supporting services were healthy and Auth returned `v2.189.0 GoTrue`; the Nest health endpoint was unavailable because the Server was not running. No authenticated browser session was started, per the required preflight. | `failed` |
| `EV-75` | Server | `builder_server` activation for the next F5 provider/jobs/public-Gateway slice | Agent `01a0620d-6d08-73c0-a3e7-ea061815d671`; Spec revision 5; allowed Server provision/messaging/rest/module/barrel/fixture paths and approved Communication integration; prohibited Core, Validation, Web, migrations/generated artifacts, package/lockfiles and SDD files. The Builder owns Documenso adapter, durable provisioning/delivery/cancellation/reconciliation jobs, public Gateway controllers/proxy/webhook and focused Server evidence. | `in_progress` |
| `EV-76` | Server | `builder_server` handoff checkpoint and shutdown | Agent `01a0620d-6d08-73c0-a3e7-ea061815d671` produced no files or handoff after three bounded progress windows and was shut down. The Orchestrator is applying the documented same-boundary fallback for the provider/jobs/Gateway slice; no implementation is accepted without current Server sensors. | `failed` |
| `EV-77` | Server | `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; focused provider/controller tests; Nest bootstrap and `curl http://localhost:5555/health` | Same-boundary fallback integrated the typed Documenso provider, real provisioning/cancellation Inngest registrations, public Gateway controllers, webhook receipt boundary and private proxy. Server types passed, Biome checked 776 files, architecture passed with 841 modules/4,063 dependencies, focused provider/controller tests passed (4 tests), and Nest booted with database/Supabase `UP`. | `passed` |
| `EV-78` | Web | `builder_web` activation for the now-unblocked public Gateway route/UI | Agent `01a06222-6370-70e3-8fca-a5e67abe71f5`; Spec revision 5; allowed Web public route/UI/service/test paths and safe login integration; prohibited Server/Core/Validation, migrations, generated route metadata and SDD files. The Builder produced no handoff and was shut down; same-boundary Web fallback is now permitted. | `failed` |
| `EV-79` | Web/Browser | Web fallback route/UI integration, `pnpm --filter web check:types`, `check:code`, `check:architecture`, focused REST tests and Playwright Gateway test | Added `/assinaturas/acesso`, typed public Gateway UI, fragment clearing, one-input OTP surface and generated route metadata. Web types/code/architecture passed; focused REST tests passed (8 tests); Playwright passed the mocked public exchange at 390×844 with keyboard focus evidence. The broad integration run stopped in pre-existing client fixture scenarios before reaching Gateway, so it is not full-stack Gateway evidence. | `passed` |
| `EV-80` | Cross-layer | `pnpm --filter server check:code`; `pnpm --filter @hms/validation check-types`; `pnpm --filter web check:code`; `git diff --check` | Current boundary sensors passed: Server Biome 776 files, Validation types, Web Biome 578 files and whitespace checks. No release or authenticated end-to-end claim is made from these package-level results. | `passed` |
| `EV-81` | Web | `builder_web` activation for Spec-conformant external widget tree | Agent `01a06257-a458-7611-aff7-0cabd10413c8`; Spec revision 5; allowed `apps/web/**` widget, hook, route and test paths; prohibited Server, Core, Validation, migrations, generated route metadata and SDD files. Assignment: replace the compact fallback with the literal page/feature-hook/step-widget/document-viewer/result/unavailable tree required by Spec §2364; exits are Web types/code/architecture, focused widget tests and Playwright narrow/keyboard evidence. | `in_progress` |
| `EV-82` | Web | `builder_web` handoff checkpoint and shutdown | Agent `01a06257-a458-7611-aff7-0cabd10413c8` produced no files or handoff after three bounded progress windows and was shut down. Same-boundary local fallback is permitted for the Spec-conformant widget-tree correction; no Web implementation claim is accepted without current sensors. | `failed` |
| `EV-83` | Web/Browser | `pnpm --filter web check:types`; `pnpm --filter web check:code`; `pnpm --filter web check:architecture`; focused widget/service Vitest; `docker compose ps -a`; Auth and Nest health; focused Playwright route | Applied the permitted same-boundary fallback for the exact Spec tree: feature hooks, page state machine, independent step widgets/hooks/tests, OTP input, private document viewer, result/unavailable states, Gateway-only Axios client and route composition. Web types, Biome, architecture, 27 focused test files/29 tests and the mocked public route at 390×844 with keyboard focus passed. Nest booted with database/Supabase `UP`; broad integration remains non-gating because existing Client fixture scenarios fail before Gateway. | `passed` |
| `EV-84` | Web/UI | `pnpm --filter web check:types`; `pnpm --filter web check:code`; `pnpm --filter web check:architecture`; `pnpm --filter web exec vitest run src/ui/formalization/widgets/pages/signing-gateway-page`; `pnpm --filter web test`; `git diff --check` | Replaced placeholder hook export checks with real `renderHook` behavior coverage across every Gateway widget hook: state, callbacks, guards, retries, OTP sanitization, private object URL creation/revocation and page transitions. The tests exposed and the implementation corrected channel continuation losing its channel ID and OTP verification retaining a stale page state closure. Focused coverage passed with 26 files/41 tests; full Web Vitest, types, Biome, architecture (766 modules/2,921 dependencies) and whitespace checks passed. | `passed` |
| `EV-85` | Web/Browser | `docker compose ps -a`; `curl http://localhost:8000/auth/v1/health`; `curl http://localhost:5555/health`; `pnpm --filter web exec playwright test tests/routes/formalization/signing-gateway.test.tsx` | Recaptured the public Gateway route after the page-hook correction. Docker database/Auth were healthy, Nest reported database/Supabase `UP`, and the focused Chromium flow passed at 390×844, including fragment removal and keyboard focus on the primary action. | `passed` |
| `EV-86` | Core/Server | `pnpm --filter @hms/core test -- --run src/formalization/use-cases/tests/confirm-formalization-signature-sending-use-case.test.ts`; `pnpm --filter @hms/core check-types`; `pnpm --filter server check:types`; `pnpm --filter server check:architecture`; focused Biome checks | Versioned signing events now inject the protocol `version: 1` at construction, matching every strict Validation event schema. Core focused tests passed (6 tests), Core/Server types passed and Server architecture passed. | `passed` |
| `EV-87` | Server | `pnpm --filter server test -- --run src/formalization/provision/tests/formalization-signature-document-metadata-reader.test.ts` | Metadata reader tests passed (2 tests), proving confirmation/provisioning reads the ready PDF preview file rather than the source DOCX. | `passed` |
| `EV-88` | Runtime/Inngest | Local Inngest event submission through the registered `formalization/provision-signature-request-document` function with `version: 1`; `docker logs hms-inngest`; database/provider inspection | A corrected event was accepted and completed through the live Inngest trigger. Replaying the user’s pending attempts then reached Documenso, which rejected the old request snapshot because it contained DOCX bytes; no provider envelopes were created. The old request remains pending and requires cancel/re-send after the source fix. | `failed` |
| `EV-89` | Database/Server | `pnpm --filter server db:seed` (three consecutive executions); `pnpm --filter @hms/core check-types`; `pnpm --filter server check:types`; `pnpm --filter server check:architecture`; focused Biome and `git diff --check`; PostgreSQL row-count verification | Seed reset and repopulation passed with exit code 0 on all three executions. The Signing Gateway records are cleared before Formalization deletion, and the final database contains one Formalization and zero stale signature requests, snapshots, request documents, provisioning attempts or previews. | `passed` |
| `EV-90` | Structural gate | `pnpm check:spec-implementation -- documentation/features/formalization/signing-gateway/spec.md --base origin/develop --json`; base `origin/develop` resolved to `7aa8684f02593f17c9201f89b16c666a8efa19a8` | Gate failed before path comparison because the Spec affected-path ledger header is `Operation | Exact paths and responsibility`, while the checker requires a table beginning exactly with `Path | Change`; it reported `declaredPathsCount=0`, `passedPathsCount=0`, `failedChecksCount=1`, `changedFilesCount=1310`, `unrelatedChangedFilesCount=1310`. | `failed` |
| `EV-91` | Communication/Core | `pnpm --filter server test -- --run src/communication/provision/tests/resend-email-provider.test.ts`; `pnpm --filter @hms/core test -- --run src/formalization/use-cases/tests/mark-signature-otp-delivery-use-case.test.ts src/formalization/use-cases/tests/provision-formalization-signature-request-document-use-case.test.ts`; current type/code/architecture checks | Resend/Mailpit adapter tests passed 3/3; focused Core tests passed 6/6; Core and Server type-checks, Biome and architecture passed. Communication now revalidates identity/consent, decrypts payloads, sends HMS-only fragment links and publishes redacted delivery outcomes; Formalization records invitation and OTP outcomes. | `passed` |
| `EV-92` | Runtime/Inngest/Mailpit | `curl http://127.0.0.1:5555/health`; registered Inngest function inspection; live scheduled delivery run and outcome runs; Mailpit API/message inspection; PostgreSQL delivery-ledger inspection | Nest health returned database/Supabase `UP`. Delivery and outcome functions were registered and completed. Mailpit received HMS invitation messages whose links contain `/assinaturas/acesso#`, with no Documenso URL; invitation ledgers converged from pending to delivered/failed according to current identity consent. | `passed` |
| `EV-91` | Core/Server | `pnpm --filter server check:code`; `pnpm --filter @hms/core test -- --run src/formalization/use-cases/tests/confirm-formalization-signature-sending-use-case.test.ts` | Server Biome passed across 775 files; the focused Core confirmation test passed with 1 file and 6 tests. | `passed` |
| `EV-93` | Server/Communication | `builder_server` activation for HTML transactional templates, `EmailProvider` HTML contract, and `FormalizationSignatureEmailDeliveryProvider` naming correction; Spec revision 5; allowed `packages/core/src/communication/interfaces/send-email-message-params.ts`, `apps/server/src/communication/{constants,messaging,provision}/**` and `apps/server/nest-cli.json` | Agent `01a06419-32a7-7230-adf1-51da5e71f447` was activated with the bounded scope but produced no verifiable handoff after repeated progress windows and was shut down. Same-boundary fallback is permitted; no completion evidence is inferred from the partial worktree. | `failed` |
| `EV-94` | Server/Code | `pnpm --filter server check:code` after the first template placement | Initial placement under `apps/server/src/communication/provision/templates` caused Biome HTML parser and `noImportantStyles` failures for email CSS/placeholders; this evidence is stale after moving the assets to the requested `volumes/communication/templates` boundary. | `failed` |
| `EV-95` | Server/Communication | `pnpm --filter server exec vitest run src/communication/provision/tests/resend-email-provider.test.ts src/communication/provision/tests/formalization-signature-email-delivery-provider.test.ts` | Passed 2 files and 5 tests: invitation/OTP HTML rendering, plaintext fallback, HTML escaping, Mailpit `HTML` payload and Resend `html` payload/idempotency. | `passed` |
| `EV-96` | Server/Build | `pnpm --filter server build`; `find apps/server/dist -type f`; Dockerfile asset-copy inspection | Server webpack build passed; the runtime image now copies `volumes/communication/templates` to `/app/templates/communication`, and the provider resolves both local repository and packaged runtime paths. | `passed` |
| `EV-97` | Core/Server/Architecture | `pnpm --filter @hms/core check-types`; `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; `git diff --check` | Core and Server type-checks, Biome (787 files), dependency architecture (853 modules/4,144 dependencies) and whitespace checks passed. | `passed` |
| `EV-98` | Core/Web/Browser | `pnpm --filter @hms/core test -- --run src/formalization/use-cases/tests/get-signature-gateway-context-use-case.test.ts src/formalization/use-cases/tests/establish-collaborator-signing-session-use-case.test.ts`; `pnpm --filter web test -- --run src/ui/formalization/widgets/pages/signing-gateway-page src/rest/services/tests/signing-gateway-service.test.ts src/ui/identity/widgets/pages/sign-in-page/tests`; `pnpm --filter web check:types`; `pnpm --filter web check:code`; `pnpm --filter web exec playwright test tests/routes/formalization/signing-gateway.test.tsx` | Collaborator recipients now resolve to `collaborator_login`, the safe HMS return target is preserved, collaborator session establishment is bound to the exact invited person, client channel selection remains email-only, focused Core coverage passed 12/12 tests, Web coverage passed 53/53 tests, Web types/Biome passed, and the focused 390×844 Chromium route passed through the collaborator-login state. | `passed` |
| `EV-99` | Runtime/Database/Mailpit | `docker compose ps -a`; Auth/API health curls; Mailpit API inspection; read-only PostgreSQL recipient/invitation inspection | The delivered Mailpit message targets `lawyer@hmsadvogados.com.br`; the matching database recipient is `actor_kind=collaborator` and the tested invitation is already `consumed`, explaining why replaying that exact link is rejected. A new invitation is required for a fresh browser-backed run. | `passed` |
| `EV-100` | Core/Web/REST | Read-only cancellation trace from `FormalizationSendingConfigurationPanel` through `FormalizationSignatureSendingService`, `CancelFormalizationSignatureSendingUseCase` and `requestCancellation` | The UI sends the current Formalization version, but the use case compared it with the request version and then passed the request version as the expected Formalization version. After confirmation those versions are intentionally different, so cancellation returned a request/projection conflict. The Server cancellation boundary also omitted the administrator exception already honored by review/status, so an administrative actor was rejected even with the correct version. | `failed` |
| `EV-103` | Server/REST | `builder_server` activation for cancellation authorization/version integration; Spec revision 5; allowed `apps/server/src/formalization/formalization-signature-sending.service.ts`, `apps/server/src/formalization/rest/controllers/cancel-formalization-signature-sending.controller.ts` and matching controller tests; RF-36–RF-38, CA-13, CA-17, CA-18; Server/REST/Controller Testing Rule Pack; Nest/Supertest and type/code/architecture exits | The scoped Server correction owns the administrator authorization exception and the REST-to-service actor-profile mapping. It must preserve assigned-lawyer access, reject non-authorized profiles and pass the command version unchanged to the corrected Core use case. | `failed` |
| `EV-104` | Server | `builder_server` activation `01a06442-7d48-7de0-93b9-2511e098af1e`; Spec revision 5; allowed `apps/server/src/formalization/formalization-signature-sending.service.ts`, `apps/server/src/formalization/rest/controllers/cancel-formalization-signature-sending.controller.ts` and matching controller tests; RF-36–RF-38, CA-13, CA-17, CA-18; Server/REST/Controller Testing Rule Pack | The scoped Builder produced no verifiable handoff after three bounded progress windows and was shut down. Same-boundary local fallback is permitted for this correction; no completion evidence is inferred from the activation. | `failed` |
| `EV-101` | Core | `builder_core` activation `01a06439-2adb-7e90-8747-55209c255aa5`; Spec revision 5; allowed `packages/core/src/formalization/use-cases/cancel-formalization-signature-sending-use-case.ts` and its colocated test; RF-36–RF-38, CA-13, CA-17, CA-18; Core/use-case-testing Rule Pack; focused Core tests, types and diff checks | The scoped Builder produced no verifiable handoff after three bounded progress windows and was shut down. Same-boundary local fallback is permitted for this correction; no completion evidence is inferred from the activation. | `failed` |
| `EV-105` | Core/Server | `pnpm --filter @hms/core exec vitest run src/formalization/use-cases/tests/process-formalization-signature-cancellation-use-case.test.ts src/formalization/use-cases/tests/cancel-formalization-signature-sending-use-case.test.ts`; `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter server exec vitest run src/formalization/rest/controllers/tests/cancel-formalization-signature-sending.controller.test.ts src/formalization/provision/tests/documenso-signature-provider.test.ts`; `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture` | Core cancellation/version and processing tests passed 2 files/11 tests; Server cancellation/provider tests passed 2 files/6 tests; Core and Server type-checks, Biome and architecture checks passed. Coverage includes independent Formalization/request versions, administrator REST mapping, attempt-ID lookup, missing-resource handling and Documenso `DRAFT`/pending cancellation behavior. | `passed` |
| `EV-106` | Runtime/REST/Inngest/Database | Authenticated local administrator POST to `/formalizations/00000000-0000-4000-8000-000000000701/signature-sending/cancel` with `expectedVersion: 9`; follow-up status GET; direct PostgreSQL projection/attempt inspection; replay of the two pending cancellation events through local Inngest; Documenso envelope inspection | REST returned `200 OK` with `outcome: scheduled` and 2 pending documents. PostgreSQL converged to Formalization version 10/signature status `cancelled`, request version 3/status `cancelled`, both cancellation attempts `cancelled` with one processing attempt each, and the status endpoint returned `canCancel: false`. Inngest completed both cancellation jobs without errors: the `DRAFT` Documenso envelope was treated as already terminal and the document without a provider resource was closed locally. | `passed` |
| `EV-108` | Web/UI | `pnpm --filter web check:code`; `pnpm --filter web exec vitest run src/ui/formalization/widgets/pages/formalization-page/formalization-sending-configuration/tests/formalization-sending-configuration.test.tsx` | Web Biome passed across 640 files and the internal widget regression suite passed 1 file/23 tests, including sending the current Formalization version to cancel, preserving the active request on a cancellation error and rendering a cancelled request as terminal with no cancel action. | `passed` |
| `EV-109` | Web/Browser | `pnpm --filter web exec playwright test tests/routes/formalization/formalization-sending-configuration.test.tsx --project=chromium` | The existing route fixture failed during its automatic mock-auth setup while waiting for `/home`; both tests timed out before reaching the sending-configuration assertions. This is not evidence against the real REST cancellation flow in `EV-106`. | `failed` |
| `EV-110` | Web/Types | `pnpm --filter web check:types` | The current worktree still has two unrelated generated-login-route typing errors in `src/middlewares/require-auth-middleware.ts` and `src/ui/shared/widgets/pages/landing-page/index.tsx`, where `to={{}}` no longer satisfies the generated `returnTo` search contract. No cancellation widget file is involved. | `failed` |
| `EV-111` | Server/Database | `builder_server` activation `01a0650f-3d89-7ea2-aa1e-be582cfba6bf`; Spec revision 5; Plan F5 correction; allowed `apps/server/**`, focused on configuration persistence/models/migrations and tests; RF-36–RF-38, CA-21; Server/Database Rule Pack; exits are a cancellation-terminal reset against real PostgreSQL plus current Server type/code/architecture checks. Prohibited paths are Core, Validation, Web, SDD artifacts and unrelated changes. | The Builder produced no handoff after repeated bounded progress windows and was shut down. Same-boundary fallback was used; no completion is inferred from this activation. | `failed` |
| `EV-112` | Web/UI | `builder_web` activation `01a0650f-6761-7620-9c3e-427a01729821`; Spec revision 5; Plan F6 correction; allowed `apps/web/src/ui/formalization/widgets/pages/formalization-page/formalization-sending-configuration/**`, including tests; RF-36–RF-38, CA-21–CA-22; UI/Widget Testing Rule Pack; exits are cancelled-state reset interaction coverage plus current focused Web checks. Prohibited paths are Core, Server, SDD artifacts, generated route metadata and unrelated changes. | The Builder completed the cancelled-state regression; the Orchestrator verified its focused test and code results in `EV-114`. | `passed` |
| `EV-113` | Server/Database/REST | Authenticated local administrator `POST /formalizations/00000000-0000-4000-8000-000000000701/signature-configuration/reset` with `expectedVersion: 10`; PostgreSQL projection/history inspection; `curl -fsS http://localhost:5555/health`; `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`; focused reset controller test | Before the fix, reset attempted to delete signatories still referenced by historical recipients under `fs_recipient_signatory_fk` (`ON DELETE RESTRICT`). The corrected transaction retains referenced signatory IDs, removes only unreferenced current rows, and updates/reuses the reset defaults. The real endpoint returned `200`; Formalization version advanced to 11, configuration became `configuring` with 2 signatories, 2 documents and 0 assignments, while the cancelled request and its 4 historical recipients remained intact. API health and all listed Server sensors passed. | `passed` |
| `EV-114` | Web/UI | `pnpm --filter web exec vitest run src/ui/formalization/widgets/pages/formalization-page/formalization-sending-configuration/tests/formalization-sending-configuration.test.tsx`; `pnpm --filter web check:code`; `git diff --check` | Focused widget coverage passed with 1 file/24 tests, including the cancelled-send regression: the reset action remains enabled, opens confirmation, delegates the current Formalization version and returns to the signatories tab. Web Biome and whitespace checks passed. | `passed` |
| `EV-115` | Web/Browser | `pnpm --filter web exec playwright test tests/routes/formalization/formalization-sending-configuration.test.tsx --reporter=line` | Both existing route tests timed out in the automatic mock-auth fixture while waiting for `/home`, before reaching the sending-configuration route. This reproduces the pre-existing fixture limitation recorded in `EV-109`; it is not evidence against the authenticated real-REST result in `EV-113`. | `failed` |
| `EV-116` | Cross-layer/Quality | `pnpm test:coverage` | Core and Web coverage tasks completed, but the Server coverage task failed after 75 files with 138 passed and 21 skipped tests; six unrelated Server suites failed during existing setup/teardown, including an `afterAll` `fixture.close` TypeError. No reset-specific failure was reported. Coverage/release evidence remains open. | `failed` |
| `EV-117` | Server/Database | `builder_server` activation `01a066b3-99bc-7d60-8c41-fbfb377e5395`; Spec revision 5; allowed `apps/server/src/formalization/database/drizzle/repositories/drizzle-formalization-signature-requests-repository.ts` and focused Formalization integration tests; RF-36–RF-38, CA-21; Database/REST/Controller Testing Rule Packs; exits are terminal-request exclusion from current-request lookup, active cancel/status preservation, focused tests, Server types/code and diff check. Prohibited paths are Core, Web, migrations, unrelated modules and SDD artifacts. | Builder produced an isolated diff, but it was not materialized in the shared workspace and its final type/integration checks were not verifiable here; same-boundary fallback was required. | `failed` |
| `EV-118` | Web/UI | `builder_web` activation `01a066b3-9176-7842-aed3-5d72ea2b518e`; Spec revision 5; allowed `apps/web/src/ui/formalization/hooks/use-formalization-signature-configuration-action.ts` and the existing sending-configuration widget test tree; RF-36–RF-38, CA-21–CA-22; UI/Widget Testing Rule Packs; exits are cache refresh after configuration mutations, cancelled-history regression coverage, focused tests, Biome and diff check. Prohibited paths are Core, Server, migrations, generated routes and SDD artifacts. | The Web correction invalidates and immediately refetches sending review/status after every successful configuration mutation, and adds hook plus cancelled-request refresh coverage. Focused widget tests passed 3 files/32 tests; Biome passed across 641 files; Web type-check has only the two unrelated generated-login-route errors from `EV-110`; diff check passed. | `passed` |
| `EV-119` | Server/Database | Orchestrator same-boundary fallback after `EV-117`; allowed only `apps/server/src/formalization/database/drizzle/repositories/drizzle-formalization-signature-requests-repository.ts` and its focused status/review integration coverage; RF-36–RF-38, CA-21; Database/REST/Controller Testing Rule Packs; exits are terminal-request exclusion, active-request preservation, focused tests, Server types/code and diff check. | `findCurrentByFormalizationId` now excludes `confirmed`, `rejected`, `cancelled`, `expired` and `failed`. Real HTTP/PostgreSQL coverage passed 1 file/7 tests, including all terminal states plus an active `sent` request; Server type-check, Biome and architecture also passed. | `passed` |
| `EV-120` | Web/Signing Gateway | `builder_web` activation `01a066cd-3d3a-7b50-b3e8-18e8db48ed43` (`Newton`); Plan F7 correction; Spec revision 5; allowed `apps/web/src/routes/login/**`, `apps/web/src/routes/assinaturas/acesso/**`, `apps/web/src/middlewares/**` only when required for safe return-target/session continuity, `apps/web/src/ui/formalization/widgets/pages/signing-gateway-page/**` and relevant Web REST/service tests; RF-01–RF-08, RF-26–RF-27, CA-01–CA-08 and CA-14–CA-16; UI, routing, REST and widget-testing Rule Packs; exits are collaborator login/session establishment, invitation return continuity, fail-closed access and focused Web tests/Biome. Prohibited paths are Server, Core, Validation, generated route metadata, unrelated routes and SDD artifacts. | Builder produced no verifiable handoff after bounded progress windows and was shut down; same-boundary Orchestrator fallback is recorded for the correction. | `failed` |
| `EV-121` | Web/Signing Gateway | Same-boundary Orchestrator fallback after `EV-120`; changed `apps/web/src/middlewares/redirect-authenticated-middleware.ts`, `apps/web/src/routes/login/index.tsx`, `apps/web/src/middlewares/require-auth-middleware.ts`, `apps/web/src/ui/shared/widgets/pages/landing-page/index.tsx` and their focused tests; `pnpm --filter web exec vitest run src/middlewares/tests/redirect-authenticated-middleware.test.ts src/ui/formalization/widgets/pages/signing-gateway-page src/ui/identity/widgets/pages/sign-in-page`; isolated Playwright CLI run with `HMS_WEB_APP_PORT=5010 PLAYWRIGHT_WEB_APP_URL=http://127.0.0.1:5010 pnpm --filter web exec playwright test tests/routes/identity/login.index.test.tsx --project=chromium`; RF-01–RF-08, RF-26–RF-27, CA-01–CA-08 and CA-14–CA-16. | Authenticated users returning to the Gateway are no longer redirected away from `/login?returnTo=/assinaturas/acesso`; login preserves the Gateway return target, while ordinary authenticated login still redirects home. Focused Web tests passed 29 files/52 tests, isolated browser tests passed 6/6, Web Biome passed 642 files, and Web types now pass. | `passed` |
| `EV-122` | Cross-layer/Signing Gateway | Fresh user runtime reports for both a natural-person client and collaborator, correlated with local PostgreSQL after invitation exchange; Docker/Auth/Server preflight: `docker compose ps -a`, `curl http://localhost:8000/auth/v1/health`, `curl http://localhost:5555/health`; local origins `http://localhost:5000` and `http://localhost:5555`. | Compose dependencies are healthy and Auth/Server return `200`. Invitation exchange consumes the invitation and persists an active `flow` Gateway session, but the next browser `GET /formalizations/signing-gateway/context` reaches the generic unavailable state with `Signing Gateway session is required.` for both actor kinds. This invalidates any inference that the collaborator login route correction established end-to-end session continuity. | `failed` |
| `EV-123` | Server/Signing Gateway | `builder_server` activation `01a06707-5d22-7a80-8d08-093b6e48892a` (`Hegel`); Spec revision 5; F5/F7 correction; RF-01–RF-08 and RF-26–RF-27; CA-01–CA-08 and CA-14–CA-16; allowed `apps/server/**` for Gateway cookie/CORS/controller tests; prohibited Web, packages, SDD artifacts and shared/generated root paths; Rule Pack code conventions, REST and controller testing; exits are exact `Set-Cookie`/credentialed request proof, focused Nest/Supertest/curl and no token exposure. | Builder completed the scoped correction: exact credentialed CORS now allows and exposes `X-HMS-Signing-CSRF`; existing cookie names/path/set/clear semantics are preserved; missing CSRF and missing session transport errors are distinguishable. | `passed` |
| `EV-124` | Web/Signing Gateway | `builder_web` activation `01a06707-671a-74b0-bfbc-3ccf2b32d0de` (`Archimedes`); Spec revision 5; F7 correction; RF-01–RF-08 and RF-26–RF-27; CA-01–CA-08 and CA-14–CA-16; allowed `apps/web/**` except generated route metadata for cookie-bound REST, CSRF lifetime, page/login state and focused tests; prohibited Server, packages and SDD artifacts; Rule Pack UI, routing, REST and widget testing; exits are focused service/widget tests plus real Playwright cookie/network/console evidence at desktop and narrow viewport with no token exposure. | Builder completed the scoped correction: Gateway REST requests default to credential inclusion, exchange CSRF is retained for both next actor mutations, and collaborator login uses SPA navigation so the RF-25 in-memory token survives. | `passed` |
| `EV-125` | Server/REST | `pnpm --filter server exec vitest run src/formalization/rest/controllers/tests/signing-gateway.controller.test.ts` after the first `FND-025` patch. | Failed 3/3 before assertions because the isolated Nest test module did not satisfy the controller's `AuthGuard` dependencies; production Server CORS runtime separately returned `204` with exact allow-origin, credentials, allowed `X-HMS-Signing-CSRF` and exposed `X-HMS-Signing-CSRF` headers. The same `builder_server` was resumed to correct test composition. | `failed` |
| `EV-126` | Web/Signing Gateway | `pnpm --filter web exec vitest run src/rest/services/tests/signing-gateway-service.test.ts src/ui/formalization/widgets/pages/signing-gateway-page/collaborator-login-step/tests`; `pnpm --filter web check:code` after first patch. | Focused behavior passed 3 files/8 tests, proving the response CSRF token is retained and sent on client OTP/collaborator session calls and the login link is canonical; Biome failed one formatting-only diff in `signing-gateway-service.test.ts`, so Web evidence remains failed until the resumed Builder reruns it. | `failed` |
| `EV-127` | Server/REST | `pnpm --filter server exec vitest run src/formalization/rest/controllers/tests/signing-gateway.controller.test.ts`; `pnpm --filter server check:types`; `pnpm --filter server check:code`; `pnpm --filter server check:architecture`. | Current correction passed 1 file/3 controller tests, TypeScript, Biome with no warnings and dependency-cruiser with 858 modules/4172 dependencies. It proves cookie resend, exact credentialed CORS exposure/preflight and distinct missing-CSRF handling without exposing invitation/cookie values. | `passed` |
| `EV-128` | Web/Signing Gateway | `pnpm --filter web exec vitest run src/rest/services/tests/signing-gateway-service.test.ts src/ui/formalization/widgets/pages/signing-gateway-page`; `pnpm --filter web check:types`; `pnpm --filter web check:code`. | Current correction passed 27 files/49 tests, TypeScript and Biome across 642 files. Client OTP and collaborator-session mutations carry the CSRF token captured from exchange; unsafe login targets fail closed. | `passed` |
| `EV-129` | Browser/runtime | Actual local Chromium credentialed exchange against `http://localhost:5555` plus `curl` preflight; focused Playwright: `HMS_WEB_APP_PORT=5010 ... playwright test tests/routes/formalization/signing-gateway.test.tsx --project=chromium` and desktop equivalent on port `5011`; inspected transient captures `/tmp/hms-signing-gateway-csrf-narrow.png` and `/tmp/hms-signing-gateway-csrf-desktop.png`. | Before correction, Chromium proved the exchange stored `hms_signing_flow` and `hms_signing_device` and reached the invitation screen with no console/request failures, isolating the later mutation. After correction, the real Server OPTIONS returns `204` with exact origin/credentials/allow/expose headers; focused browser flows pass 1/1 at 390×844 and 1/1 desktop, clean the fragment, reach collaborator login, and preserve `performance.timeOrigin` through SPA navigation. Both captures were inspected with no layout regression or token in the URL. | `passed` |
| `EV-130` | Web/Architecture | `pnpm --filter web check:architecture` on the integrated candidate. | Failed on two existing violations in `formalization-sending-configuration/tests/use-formalization-signature-configuration-action.test.ts`: the widget test imports `use-rest-context` and React Query directly. This is outside the FND-025 runtime paths but inside the active Spec candidate; scoped `builder_fix_web` correction `EV-131` was activated. | `failed` |
| `EV-131` | Web/Architecture | `builder_fix_web` activation `01a0671f-0330-78a3-b6bb-9ff02420c65e` (`Fermat`); Spec revision 5; independent current-candidate architecture correction; allowed to move `apps/web/src/ui/formalization/widgets/pages/formalization-page/formalization-sending-configuration/tests/use-formalization-signature-configuration-action.test.ts` to `apps/web/src/ui/formalization/hooks/tests/use-formalization-signature-configuration-action.test.ts`, plus an indispensable nearest test helper; prohibited production, Server/Core/Validation/generated/SDD paths; exits are focused test plus Web architecture/code/types. | Activated and then scope-refined before edits so the action-hook test resides at its owning feature-hook boundary; implementation handoff pending. | `pending` |
| `EV-132` | Browser | Combined seven-test isolated Playwright run for Gateway plus the full login route file on port `5010`. | Gateway and collaborator-return scenarios passed, but two unrelated login fixture scenarios failed (`redirects an authenticated user from login to home`; `renders the login form for an unauthenticated user`). The correction was rerun with the two affected scenarios only and passed 2/2; the broader fixture failures remain historical environment/test-isolation evidence rather than FND-025 failures. | `failed` |
| `EV-133` | Web/Architecture | `builder_fix_web` handoff plus Orchestrator rerun: `pnpm --filter web exec vitest run src/ui/formalization/hooks/tests/use-formalization-signature-configuration-action.test.ts`; `pnpm --filter web check:architecture`; `pnpm --filter web check:code`; `pnpm --filter web check:types`; `git diff --check`. | Moved the action-hook test from the widget tree to its owning feature-hook test boundary without production changes. Focused test passed 1/1; dependency-cruiser passed with 768 modules/2941 dependencies; Biome passed 642 files; TypeScript and whitespace checks passed. | `passed` |
| `EV-134` | Core/REST/Runtime | Real local client replay against the latest provisioned recipient: targeted development invitation reactivation, `POST /formalizations/signing-gateway/invitations/exchange`, `GET /formalizations/signing-gateway/context`, then `POST /formalizations/signing-gateway/otp` with the returned cookies and `X-HMS-Signing-CSRF`. | Exchange returned `200`, both Gateway cookies and browser-visible CSRF; context returned `200 choose_channel` with the one consented masked e-mail; OTP returned `500 Assinatura indisponível`. Persistence inspection proves the recipient person and active e-mail consent are valid. Source inspection identified that `RequestSignatureOtpUseCase` passes `session.recipientId` to the Identity authentication-channel reader, which requires `recipient.personId`. | `failed` |
| `EV-135` | Core | `builder_fix_core` activation `01a06732-766a-7e60-bf9d-954a426ca795` (`Curie`); Spec revision 5; FND-027 correction; RF-03–RF-06 and RF-25; CA-02–CA-04; allowed `packages/core/src/formalization/interfaces/formalization-signature-invitations-repository.ts`, OTP request/verify use cases and their existing colocated tests; prohibited app, Validation, generated and SDD paths; Core and Use-case Testing Rule Packs; exits are consumed-invitation resolution, recipient-to-person channel lookup, invitation-bound issue/verify coverage, focused Core tests, Core types/code and diff check. | Activation was expanded before implementation after source inspection proved both OTP issue and verification incorrectly treated `session.recipientId` as the invitation ID. Implementation handoff pending. | `pending` |
| `EV-136` | Server | `builder_fix_server` activation `01a06736-3f09-7051-a5aa-4ef761ca0c38` (`Nash`); Spec revision 5; FND-027 integration correction; RF-03–RF-06 and RF-25; CA-02–CA-04; allowed `apps/server/src/formalization/database/drizzle/repositories/drizzle-formalization-signature-invitations-repository.ts`, `apps/server/src/formalization/database/formalization-signature-gateway-transaction.ts`, `apps/server/src/formalization/formalization-signature-sending.service.ts` and indispensable nearest controller integration coverage; prohibited packages, schema/migration, generated, unrelated app and SDD paths; Database/Server App/REST/Controller Testing Rule Packs; exits are latest consumed-invitation lookup, first-use OTP guard initialization under the existing locked transaction, Core dependency composition, focused real REST/PostgreSQL OTP issue, Server types/code/architecture and diff check. | Initial handoff added deterministic invitation lookup and dependency wiring with Server checks passing. The first current-code real REST replay still returned `500`; database inspection showed no OTP guard row, while `issueOtp` rejects every missing guard despite the Core use case explicitly supporting an absent first-use guard. Same Builder resumed for the transaction correction. | `pending` |
| `EV-137` | Server | Replacement `builder_fix_server` activation `01a06745-3750-7681-aaf8-32cb2a4d982d` (`Plato`); Spec revision 5; narrow continuation of EV-136/FND-027; allowed only `apps/server/src/formalization/database/formalization-signature-gateway-transaction.ts` and an indispensable nearest test; prohibited packages, SDD, schema/migration, generated and unrelated paths; Database Rule Pack; exits are type-safe first-use guard initialization, focused real REST/PostgreSQL OTP issue, Server types/code/architecture and diff check. | Activated after three bounded waits on the resumed original Builder. Its partial insertion passed Biome but failed Server typecheck because optional guard-change fields were spread into required insert columns; replacement must complete that existing patch before validation. | `pending` |
| `EV-138` | Server | Second replacement `builder_fix_server` activation `01a06748-e835-7ab2-b3d2-648902eb6b27` (`Planck`); Spec revision 5; narrow continuation of EV-136/EV-137/FND-027; allowed only `apps/server/src/formalization/database/formalization-signature-gateway-transaction.ts`; prohibited every other path; Database Rule Pack; exit is a type-valid first-use guard insertion followed by Orchestrator Server/runtime sensors. | Activated after EV-137 produced no diff through two bounded post-PROCEED waits; implementation handoff pending. | `pending` |
| `EV-139` | Messaging/Database/Runtime | Real local OTP issue and Mailpit delivery after the FND-027 candidate, using the consumed invitation, active flow cookies and CSRF against the freshly booted Server on port `5556`; Inngest function discovery plus resulting PostgreSQL and Mailpit inspection. | OTP issue returned `200`, created an invitation-bound challenge, first-use guard and send attempt; Inngest delivered the e-mail to `client@hms.br` and marked the attempt `delivered`. The challenge incorrectly remained `pending_delivery`: `MarkSignatureOtpDeliveryUseCase` calls `findCurrentByInvitationId(attempt.challengeId)`, conflating challenge and invitation identifiers, so verification can never accept the delivered OTP. | `failed` |
| `EV-140` | Core | Resumed `builder_fix_core` `01a06732-766a-7e60-bf9d-954a426ca795` (`Curie`); Spec revision 5; FND-028; RF-04–RF-05; CA-02–CA-03; allowed only the OTP challenge repository interface, `mark-signature-otp-delivery-use-case.ts` and its existing test; prohibited apps, SDD, generated and unrelated paths; Core/Use-case Testing Rule Packs; exits are exact challenge-ID delivery transition, focused tests, Core types/code/architecture. | Exact challenge-ID lookup is now part of the repository contract and delivery outcome uses it. Focused test passed 1/1 and Core type-check passed; integrated runtime proof is EV-144. | `passed` |
| `EV-141` | Server | Resumed `builder_fix_server` `01a06748-e835-7ab2-b3d2-648902eb6b27` (`Planck`); Spec revision 5; FND-028; RF-04–RF-05; CA-02–CA-03; allowed only `apps/server/src/formalization/database/drizzle/repositories/drizzle-formalization-signature-otp-challenges-repository.ts`; prohibited every other path; Database Rule Pack; exits are challenge-ID repository lookup, Server types/code/architecture and real OTP delivery/verification replay. | Drizzle exact challenge-ID lookup is implemented; Server type-check passed and integrated runtime proof is EV-144. | `passed` |
| `EV-142` | Server | `builder_fix_server` activation `01a0675b-6b72-7a00-92bb-c91cd7f25d90` (`Gibbs`) for FND-029; Spec revision 5; RF-04–RF-05; CA-02–CA-03; allowed only `apps/server/src/formalization/database/drizzle/repositories/drizzle-formalization-signature-otp-rate-reservations-repository.ts` and an indispensable nearest test; prohibited packages, schema/migration, generated, SDD and unrelated paths; Database Rule Pack; exits are inclusive rolling-window reservation counting, focused regression, Server types/code/architecture and real OTP issue/verification replay. | Both invitation and source-IP counts now include the rolling-window boundary. Focused Gateway controller passed 3/3; Server types/code/architecture passed (792 files; 858 modules/4172 dependencies); integrated runtime proof is EV-144. | `passed` |
| `EV-143` | Core | Resumed `builder_fix_core` `01a06732-766a-7e60-bf9d-954a426ca795` (`Curie`) for FND-030; Spec revision 5; RF-05; CA-02–CA-03; allowed only `packages/core/src/formalization/use-cases/verify-signature-otp-use-case.ts` and its existing test; prohibited apps, interfaces outside the use-case need, generated, SDD and unrelated paths; Core/Use-case Testing Rule Packs; exits are current guard-version CAS on valid OTP, regression with a non-initial guard version, focused tests and Core types/code/architecture. | Valid and invalid verification now require the current guard and pass its actual version. Focused verification passed 11/11; integrated three-file OTP suite passed 20/20; Core type and architecture passed, with lint completing under the existing warning baseline. | `passed` |
| `EV-144` | Core/Server/Messaging/Database/Runtime | Real local natural-client replay through current Server, PostgreSQL, Inngest and Mailpit: OTP resend, delivered-outcome event `01M1KPAH9WETCBCG6FZZWMHRKX`, run `01M1KPAHFN9ZF9WNCHG4BAF3T1`, OTP verification and authenticated context. | OTP issue returned `200`; delivery outcome completed and moved the exact challenge to `active`; the Mailpit code verified with `200 step=reading`; the follow-up context remained `200 step=reading`. PostgreSQL ended with challenge `consumed`, guard version `4`, two sends, revoked flow session and one active authenticated session. The prior event replay without required schema version failed validation and made no application-state claim. | `passed` |
| `EV-145` | Core/Server/Web/Browser | Current focused gates after EV-144: Core OTP request/verify/delivery 3 files/20 tests plus types/lint/architecture; Server Gateway controller 3/3 plus types/code/architecture; Web Gateway service/widget 27 files/49 tests plus types/code/architecture; isolated Chromium Gateway route on port 5012. | All commands passed. Core lint retained only the existing warning baseline; Browser route passed 1/1 with the pre-existing route-file and `NO_COLOR` warnings classified as non-feature output. Browser coverage is isolated/mocked; EV-144 is the real service-backed proof. | `passed` |
| `EV-146` | Web/Browser | Resumed `builder_web` `01a06707-671a-74b0-bfbc-3ccf2b32d0de` (`Archimedes`); Spec revision 5; FND-031 correction; RF-05–RF-10, RF-25–RF-27; CA-03–CA-05, CA-15; allowed only `apps/web/src/ui/formalization/widgets/pages/signing-gateway-page/use-signing-gateway-page.ts`, its existing hook test, and the existing Signing Gateway route integration test when indispensable; prohibited Server/Core/Validation/generated/SDD/unrelated paths; UI, Widget Testing, Routing and REST Rule Packs. | Bootstrap context is now applied once independently of HMS auth-token changes, while collaborator establishment is a separate guarded transition. Focused hook coverage passed 10/10, including client OTP and collaborator reading persistence across blur, visibility change, token refresh and rerender; the complete page-widget tree passed 26 files/47 tests. Orchestrator reruns passed Web TypeScript, Biome (642 files), architecture (768 modules/2,942 dependencies), and the isolated mocked Chromium route at 390×844 and desktop, 1/1 each. | `passed` |
| `EV-147` | Core/Server/Web/Browser | Real local collaborator replay with targeted development invitation reactivation against healthy Auth/Server/Web/PostgreSQL. A fresh anonymous Chromium context exchanged the invitation, stored both HttpOnly Gateway cookies and received `200 collaborator_login`; a second fresh context authenticated as the exact seeded lawyer, exchanged the same reactivated invitation, received `200` from `POST /collaborator/session` and entered `reading`. Both `GET /document` and `GET /document/content` then returned `500`. | The correct collaborator identity/session path is working. The private-document use case calls `sourceReader.findDocumentVersion(session.requestId, sourceDocumentVersionId)`, but that reader's first argument is the Formalization ID. The persisted request-document is provisioned and has private file metadata, while the request ID cannot resolve the owning Formalization document version. | `failed` |
| `EV-148` | Core | `builder_fix_core` activation for FND-032; Spec revision 5; RF-06–RF-08 and RF-25; CA-04, CA-06–CA-07, CA-15; allowed only `packages/core/src/formalization/use-cases/get-signature-document-use-case.ts`, `get-signature-gateway-context-use-case.ts`, their existing colocated tests and indispensable Core imports; prohibited apps, Validation, generated, SDD and unrelated paths; Core/Use-case Testing Rule Packs; exits are request-to-Formalization resolution, immutable source title/version mapping, private-document metadata coverage, focused tests, Core types/code/architecture and diff check. | Builder implemented request-to-Formalization resolution, immutable title and actual request version, plus fail-closed request/source coverage. Focused Core tests passed 11/11; types, focused lint, architecture and diff check passed. | `passed` |
| `EV-149` | Auth/Server/Web/Browser | Real local replay of the same collaborator invitation while the browser is authenticated as the seeded administrator instead of the assigned lawyer. | Exchange and context return `200`, then `POST /collaborator/session` fails closed with the generic unavailable response because the authenticated collaborator ID does not equal the invited recipient's person ID. This reproduces the user's exact invitation → generic-unavailable screenshots and is the required exact-identity behavior, not the private-document defect seen with the correct lawyer. | `passed` |
| `EV-150` | Server | `builder_fix_server` activation for FND-032 composition/runtime coverage; Spec revision 5; RF-06–RF-08 and RF-25; CA-04, CA-06–CA-07, CA-15; allowed only `apps/server/src/formalization/formalization-signature-sending.service.ts` and indispensable existing Signing Gateway controller/fixture tests; prohibited packages, Core, Validation, migrations/generated, SDD and unrelated paths; Server App/REST/Controller Testing Rule Packs; exits are request-repository composition for private-document retrieval, focused controller coverage, Server types/code/architecture and real exact-lawyer document metadata/content replay. | Builder wired the request repository into private-document composition; focused controller passed 3/3 and Server types/code/architecture passed. Real exact-lawyer replay returned the correct title and `200` for both metadata and the 42,056-byte PDF stream. | `passed` |
| `EV-151` | Validation/Web/Browser | Real exact-lawyer replay after the FND-032 Core/Server candidate. `GET /document` returned `200` with the Spec-defined private metadata and `GET /document/content` returned a 42,056-byte `application/pdf` with no console/request failure, but the reader still rendered its recoverable private-document error. | `SigningGatewayService.getDocument()` parses the metadata response with `signatureGatewayDocumentSchema`, whose strict shape is only `{ id, title, position, pageCount? }`; the actual use-case response is `{ privateFileId, title, mediaType, byteCount, sha256 }`. The strict parser converts the successful metadata response into a client-side 422 and the combined query marks the PDF unavailable. | `failed` |
| `EV-152` | Validation | `builder_fix_validation` activation for FND-033; Spec revision 5; RF-06–RF-07 and CA-04, CA-06–CA-07, CA-15; allowed only the Signing Gateway validation schema/barrel paths needed for the exact authenticated private-document metadata response; prohibited apps, Core, package/lockfiles, generated, SDD and unrelated paths; Validation/Core boundary Rule Packs; exits are strict exact metadata schema, Validation lint/types/architecture and consumer type compatibility. | Added the strict UUID/title/PDF/positive-byte/lowercase-SHA-256 metadata schema and export. Validation lint/types/architecture and Web consumer type-check passed. | `passed` |
| `EV-153` | Web | `builder_fix_web` activation for FND-033 consumer correction; Spec revision 5; RF-06–RF-07 and CA-04, CA-06–CA-07, CA-15; allowed only `apps/web/src/rest/services/signing-gateway-service.ts`, its existing test, `apps/web/src/ui/formalization/hooks/use-signature-document-query.ts` and indispensable existing hook tests; prohibited Server/Core/Validation, generated, SDD and unrelated paths; REST/UI/Widget Testing Rule Packs; exits are exact metadata parsing, valid/unknown-field coverage, no false reader error, focused tests and Web types/code/architecture. | Web now parses the authenticated metadata schema directly without coercion and covers valid metadata plus unknown-field rejection; focused tests passed 6/6 and Web types/architecture passed. Its code check exposed only the concurrently active FND-034 page-hook formatting/lint findings, assigned to EV-156. | `passed` |
| `EV-154` | Web | Read-only next-transition inspection after isolating the reader error. | `DocumentReadingStep` delegates “Li o documento” to its supplied `onContinue`, but `contextToState(reading)` supplies a permanent no-op and the page hook never replaces it. A successfully rendered private PDF therefore cannot transition to `SignatureAcknowledgementStep`. | `failed` |
| `EV-155` | Core | `builder_fix_core` activation for the FND-033 shared service contract; Spec revision 5; RF-06–RF-07 and CA-04, CA-06–CA-07, CA-15; allowed only the Formalization Gateway metadata response structure/barrel and `SigningGatewayService` interface; prohibited apps, Validation, use cases, generated, SDD and unrelated paths; Core Rule Pack; exits are an explicit metadata return type aligned with the use case/schema, Core types/lint/architecture and Web consumer type compatibility without `unknown` coercion. | Added the distinct metadata response type and corrected `SigningGatewayService.getDocument()` while preserving the context summary. Core types/lint/architecture and Web consumer type-check passed. | `passed` |
| `EV-156` | Web | `builder_fix_web` activation for FND-034; Spec revision 5; RF-07–RF-08 and CA-06–CA-08, CA-15; allowed only the Signing Gateway page hook and its existing hook test; prohibited service/query/widget component, Server/Core/Validation, generated, SDD and unrelated paths; UI/Widget Testing Rule Packs; exits are reading→acknowledgement transition with immutable document/current request version, focused regression and Web types/code/architecture. | Reading now transitions to acknowledgement with the exact document and authenticated request version. Focused hook tests passed 11/11 and Web types/code/architecture passed. | `passed` |
| `EV-157` | Web/Config | Read-only provider-entry preflight against Spec G-05 and current `apps/web/vite.config.ts`. | The Server proxy controller exists at `/assinaturas/provedor/:alias`, but local Web has no Vite longest-prefix proxy. The provider action uses the required same-origin relative path, so localhost:5000 would fall through to TanStack instead of reaching Nest. The Spec explicitly assigns local parity to `apps/web/vite.config.ts`. | `failed` |
| `EV-158` | Core/Provider/Browser | Real exact-lawyer replay at 1200×900 and 390×844 after the private-document and page-state corrections, followed by the same-origin provider transition through the local Nitro prefix proxy. Provider API state was inspected without exposing recipient tokens. | The lawyer now reads the 42,056-byte PDF, acknowledges it and reaches the provider step without console or HMS endpoint failures. The prefix proxy reaches Nest, but Documenso returns its own 404 page because both provisioned envelopes remain `DRAFT` and every technical recipient remains `NOT_SENT`. A targeted `distributionMethod: NONE` call then returned 400 because provisioning also discarded all four configured signature fields and sent `fields: []`; no provider e-mail was emitted. | `failed` |
| `EV-159` | Core | `builder_fix_core` activation `01a067aa-b3ca-7f20-a83c-056f6de1f61b` (`Sartre`) for FND-036; Spec revision 5; RF-32; CA-07–CA-08, CA-19; allowed only the provisioning use case and its existing focused test; prohibited apps, interfaces, Validation, generated and SDD paths; Core/Use-case Testing Rule Packs. | The field/distribution behavior passed its original focused sensors, but the configuration-version equality later proved incompatible with the post-confirmation aggregate transition and multi-document processing; superseded by FND-038 and EV-172/EV-173. | `stale` |
| `EV-160` | Server | `builder_fix_server` activation `01a067aa-bdfe-7803-876f-357b38711740` (`Gibbs`) for FND-036; Spec revision 5; RF-14, RF-32; CA-07–CA-08, CA-19; allowed only the Documenso adapter/test and provisioning-job composition; prohibited Core, other Server paths, generated and SDD paths; Server App/Provision/Testing Rule Packs. | Creation and distribution now both carry `distributionMethod: NONE`; distribution is DRAFT-only/idempotent and the job composes the configuration repository. Focused adapter tests passed 9/9; Server types, code and architecture passed (792 files; 858 modules/4,172 dependencies); diff check passed. Targeted local distribution returned `200` with no Mailpit message. | `passed` |
| `EV-161` | Server/Provider/Browser | Targeted local repair added the two configured signature fields to the current DRAFT test envelope and distributed it with `distributionMethod: NONE`; Documenso returned `200`, moved the recipient UI to an actionable signing document and emitted zero provider e-mails. The exact-lawyer 390×844 replay then entered the same-origin proxy. | The proxied HTML returned `200` and contained the correct Documenso signing state, but every root `/assets/*` request fell through to Web as `404`; the proxy's replacement CSP also discarded Documenso's per-response script/style nonce and blocked the hydration bootstrap. The server-rendered shell is therefore visible but signing actions are non-functional. | `failed` |
| `EV-162` | Server | Resumed `builder_fix_server` `01a067aa-bdfe-7803-876f-357b38711740` (`Gibbs`) for FND-037; Spec revision 5; RF-13; CA-07, CA-09, CA-14–CA-15; allowed only the proxy controller and nearest controller contract test; prohibited prior provider files, Core/Web/Validation, configs, generated and SDD paths; Server REST/Controller Testing Rule Packs. | Added root asset/API rewriting, nonce-CSP preservation, restrictive frame/form policy and unsafe-path rejection. Focused tests passed 4/4 and Server types/code/architecture/diff checks passed, but EV-163 found the test did not reproduce the real Express/Nitro wildcard path shape. | `failed` |
| `EV-163` | Server/Provider/Browser | Real exact-lawyer replay at 390×844 after EV-162, through invitation exchange, authenticated private PDF reading, acknowledgement and same-origin provider entry. | Provider SSR returned the actionable signer state, but all 182 rewritten asset requests were forwarded beneath `/sign/{token}/assets/*` and returned `404`; the page retained browser-default typography and no hydrated interaction. The controller derived its upstream path only from wildcard params and discarded the original query string, while the focused Nest test supplied a simplified param shape that did not reproduce the real route. | `failed` |
| `EV-164` | Core/Server/Web/Provider/Browser | Corrective exact-lawyer replay at 390×844 and 1200×900 using the real Auth, REST, PostgreSQL, private PDF, HMS same-origin proxy and pinned Documenso container. | The opaque public path now provides an alias-scoped virtual React Router base/sign route; Nitro handles extensionless proxy paths while Vite's earlier prefix proxy handles fingerprinted assets. The proxy reconstructs the real incoming path/query, maps provider-root assets/API/manifest/font/PDF requests, rewrites the Vite preload base and translates alias-bearing PDF/API paths server-side. Both viewports rendered the assigned PDF with zero HTTP errors and zero provider-origin DOM leakage; the narrow flow opened the real signature dialog and the desktop HMS transition was keyboard-operated. Two Konva no-parent warnings appeared only after opening the narrow signature dialog and are classified as a non-blocking pinned-provider warning. Focused Core tests passed 2/2, Server proxy tests 5/5, combined provider/proxy tests 14/14 and Web Gateway tests 18/18; Core/Server/Web type, code and architecture gates passed. | `passed` |
| `EV-165` | Server/Inngest/Provider | Canonical provisioning event replay for pending request document `e5f5dff6-cfe4-4e31-bca8-41b5e0d7c2dc` after the FND-036 code and provider tests passed. The Inngest dev container was restarted without deleting its volume, rediscovered all 24 functions and received run `01M1KY9DX1P7W1785M48EM6PH6`. | The local dev executor again left the run `Running` with no step trace and never acquired the PostgreSQL provisioning lease; Documenso received no request. Direct SDK discovery at `/api/inngest` returns `200`, scheduled functions continue to finish, and this is isolated to local event-run dispatch rather than the provisioning use case/provider candidate. Real current-code async provisioning remains an open integration gate. | `failed` |
| `EV-166` | Core/Database | User-provided Inngest provisioning stack mapped to the current compiled line and live PostgreSQL request/configuration state. Activated `builder_core` `01a0680f-a13e-7a71-8824-908b601ab315` (`Aristotle`) for Spec revision 5, RF-32/RF-36–RF-37 and CA-19/CA-21, limited to the provisioning use case and its test. | The newest open request captured signature configuration version `53`; the confirmation transaction advanced the Formalization-backed configuration projection to version `54`; the job then failed deterministically on strict equality before contacting Documenso. Historical cancelled requests are also not rejected before provisioning work. FND-038 opened before Core source changes. | `failed` |
| `EV-167` | Server/Provider | User-provided cancellation stack correlated with the pinned Documenso API logs and both PostgreSQL databases. Activated `builder_server` `01a0680f-b042-7f01-a3c0-d2b58eb931cc` (`Hume`) for Spec revision 5, RF-36–RF-37 and CA-21, limited to the Documenso provider and its test. | HMS retains provider resource `envelope_afdomoocccemzmzn`, while the current Documenso database has no envelopes and API V2 returns `NOT_FOUND`; the adapter collapses that absence into a generic outage, so cancellation retries six times instead of converging as absent provider access. FND-039 opened before Server source changes. | `failed` |
| `EV-168` | Server/Messaging | Source inspection of the repeated provisioning stack against the Messaging Rule Pack; resumed `builder_server` `01a0680f-b042-7f01-a3c0-d2b58eb931cc` (`Hume`) for the provisioning job and its existing real-Inngest test. | The job lets deterministic `AppError` failures enter the five-retry policy instead of translating them to `NonRetriableError`, which explains the repeated identical configuration failure. FND-040 opened before job source changes. | `failed` |
| `EV-169` | Server/Messaging | Two bounded waits plus an explicit progress interrupt for resumed `builder_server` `01a0680f-b042-7f01-a3c0-d2b58eb931cc` (`Hume`). | The Builder remained running without changing either allowed job path and was closed. Its completed FND-039 provider candidate is preserved; FND-040 requires a narrow replacement Builder. | `failed` |
| `EV-170` | Server/Messaging | Activated replacement `builder_fix_server` `01a0681d-958f-7030-873b-af4dafca9864` (`Locke`) for Spec revision 5, RF-32/CA-19, restricted to the provisioning job and its existing real-Inngest test. | The job now maps only deterministic `FormalizationSignatureProvisioningFailedError` to `NonRetriableError`; unexpected provider/infrastructure errors remain retriable. The real Dockerized Inngest test passed 3/3 without mocked repositories. | `passed` |
| `EV-171` | Core/Server | Orchestrator reran the current Core provisioning test/type/architecture sequence and Server provider test/type sequence after the Builder handoffs. | Core behavior tests passed 12/12 and provider tests passed 11/11, but both Core and Server type checks failed because the canonical terminal-status array inferred a narrow union that rejected the complete `FormalizationSignatureRequestStatus` input. FND-038 remains active and the same Core Builder is resumed for correction. | `failed` |
| `EV-172` | Core/Server/Messaging | Current-candidate focused and static gates after EV-171 correction: Core provisioning Vitest, Core types/architecture; Server Documenso adapter Vitest, Server types/code/architecture; real Dockerized provisioning-job Vitest; `git diff --check`. | Core passed 12/12, types and architecture (990 modules/3,164 dependencies); Documenso adapter passed 11/11; Server types/code and architecture passed (793 files; 859 modules/4,185 dependencies); real Inngest job passed 3/3; whitespace check passed. | `passed` |
| `EV-173` | Core/Server/Inngest/Provider/Communication/Database | Replayed both documents of current request `3174a106-f02c-4bf3-b671-de2a2f6416e4` sequentially through canonical Inngest events after the first provisioning transaction advanced the Formalization aggregate. Runs `01M1M1ZT2KZ3KD6T6W803HECKT` and `01M1M20D6RRVF2T7WB9TRSX1DE` were inspected through the local Inngest MCP. | Both runs completed. HMS converged to `sending` with 2 provider resources, 4 provider-recipient resources, 4 invitations and 4 delivered invitation attempts. Documenso contains 2 distributed envelopes and 4/4 technical `@signing.invalid` aliases with zero real recipient addresses. No manual provider/database repair was used. | `passed` |
| `EV-174` | Server/Inngest/Provider/Database | Replayed stale-envelope cancellation attempt `e169680e-e485-4996-a20b-bd13bf44e2a7` through the canonical event after Documenso API V2 had authoritatively returned `404 NOT_FOUND`. Run `01M1M21QZED2TCHW5J0TVM0JXM` was inspected through the local Inngest MCP. | The run completed once; the HMS attempt converged to `cancelled` with `attempts=1`. The adapter did not read or expose the 404 body, while its focused test preserves generic retriable failure for non-404 responses. | `passed` |
| `EV-175` | Cross-layer/Preflight | User browser screenshot; source/Spec comparison; `docker compose ps -a`; `curl http://localhost:8000/auth/v1/health`; `curl http://localhost:5555/health`; current worktree inspection. | Browser displayed `Signing Gateway CSRF token is required.` after refresh. Auth and Server returned `200`; required Compose dependencies, including Documenso and databases, are healthy. The active Spec is revision 5 and explicitly requires `GET /context` to issue a fresh session-bound CSRF value. Current controller returns only the context body and Web stores CSRF only in memory, proving FND-041 without an environment failure. Existing unrelated dirty-worktree changes are preserved. | `failed` |
| `EV-176` | Core | Activated `builder_core` `01a06848-e280-7ad1-bd7e-58739c858942` (`Banach`) for FND-041, Spec revision 5, RF-25 and CA-01/CA-04/CA-14. Allowed only `packages/core/src/formalization/use-cases/get-signature-gateway-context-use-case.ts` and its existing test; prohibited Server, Web, generated and SDD paths. Rule Pack: code conventions, Core and use-case testing. | Implemented validated session/device bootstrap, fresh synchronizer-token generation and optimistic CSRF-hash rotation. Focused tests passed 10/10 and Core types/architecture passed. | `passed` |
| `EV-177` | Server/REST | Activated `builder_server` `01a06848-f562-7db1-be65-f17be3f12f35` (`Godel`) for FND-041, Spec revision 5, RF-25 and CA-01/CA-04/CA-14. Allowed only `apps/server/src/formalization/formalization-signature-sending.service.ts`, `apps/server/src/formalization/rest/controllers/signing-gateway.controller.ts` and its existing test; prohibited Core, Web, generated and SDD paths. Rule Pack: code conventions, REST and controller testing. | Integrated the Core result into GET, OTP-verification and collaborator-session responses: only the strict context is returned and the latest rotated token is set in the CORS-exposed header. Focused tests passed 5/5 and Server types/code/architecture passed. | `passed` |
| `EV-178` | Web/REST | Activated `builder_web` `01a06848-eefe-7d51-a7e4-7cdec9ae0a1a` (`Hypatia`) for FND-041, Spec revision 5, RF-25 and CA-01/CA-04/CA-14. Allowed only `apps/web/src/rest/services/tests/signing-gateway-service.test.ts` unless a production adapter defect is independently proven; prohibited Core, Server, generated and SDD paths. Rule Pack: code conventions, UI and REST. | Proved case-insensitive context-header ingestion replaces a stale in-memory token and the next mutation sends the refreshed value. Focused tests passed 6/6 and Web types/code/architecture passed. | `passed` |
| `EV-179` | Core/Server/Web | Orchestrator official correction sensors: focused context use-case, Gateway controller and Web service Vitest files; Core/Server/Web type checks; Core/Server/Web architecture checks; Server/Web Biome checks. | Focused tests passed 10/10 Core, 3/3 Server and 6/6 Web. All type, architecture and code checks passed (Core 990 modules/3,164 dependencies; Server 793 files and 859 modules/4,185 dependencies; Web 642 files and 768 modules/2,942 dependencies). | `passed` |
| `EV-180` | Browser/REST/Auth | Real local collaborator invitation exchange, page reload, HMS collaborator sign-in and next Gateway mutation through Chromium against Web `:5000`, Nest `:5555`, Supabase Auth `:8000` and PostgreSQL. | Reloaded `GET /context` returned 200 with a fresh exposed CSRF header and `collaborator_login`; the following `POST /collaborator/session` sent CSRF and returned 200. Web then failed strict parsing because that endpoint returned the internal `{ context, csrfToken }` envelope instead of the public context body. FND-041's refresh mechanism is proven; FND-042 opened before Server edits. | `failed` |
| `EV-181` | Browser/REST/Auth/Document | Reused the resulting real authenticated collaborator session after the FND-042 Server rebuild; reloaded the public route, inspected context/document requests, acknowledged reading and inspected Chromium console plus failed HTTP responses. | `GET /context` returned 200 with a fresh CSRF header, strict public `reading` body and no internal envelope; metadata/content requests returned 200. `POST /acknowledgements` sent the refreshed CSRF header, returned 200 and produced a same-origin provider path. All post-correction feature requests were 200 and Chromium had 0 console errors. No provider signature was submitted. | `passed` |
| `EV-182` | Server/REST | Orchestrator reran the Gateway controller test and Server type/code/architecture gates after the FND-042 response projection. | Controller tests passed 5/5, including exact OTP-verification and collaborator-session body/header assertions. Server types passed; Biome checked 793 files; architecture passed with 859 modules/4,186 dependencies. | `passed` |
| `EV-183` | Server/Provider proxy | Activated `builder_server` `01a06875-760c-7bc2-b870-f257ab22f23c` (`Goodall`) for FND-043, Spec revision 5, RF-13 and CA-07/CA-14. Allowed only `apps/server/src/formalization/rest/controllers/signing-gateway-proxy.controller.ts` and its existing test; prohibited Core, Validation, Web, root/configuration, generated and SDD paths. Rule Pack: code conventions, REST, controller testing and provision boundaries. | The proxy now synthesizes only Documenso's serialized `lang=pt-BR` cookie after stripping all incoming cookie and authorization headers. Focused coverage proves HMS flow/device cookies and bearer credentials never reach the provider while existing proxy behavior remains green. | `passed` |
| `EV-184` | Server/Provider proxy | Orchestrator reran the focused proxy test and Server type/code/architecture gates on the integrated correction. | Proxy tests passed 6/6; TypeScript passed; Biome checked 793 files; architecture passed with 859 modules and 4,186 dependencies. | `passed` |
| `EV-185` | Provider configuration/browser | Documenso v2.17.0 completion-route/auth source inspection; `docker compose --profile signing-gateway up -d --force-recreate --no-deps documenso`; container environment inspection; `docker compose ps documenso`; `curl http://127.0.0.1:3004/api/health`; real Chromium replay of the latest signed recipient completion page at 1440×900. | The recreated container has `NEXT_PUBLIC_DISABLE_EMAIL_PASSWORD_SIGNUP=true`, is healthy, and reports database/certificate health `ok`. The real signed page rendered its completion confirmation with no claim prompt, zero password fields, zero claim-account buttons, zero console errors and no failed HTTP response. Existing-account sign-in remains enabled because no sign-in switch was changed. | `passed` |
| `EV-186` | Server/Provider proxy | Activated `builder_server` `01a06887-9d50-7800-b7e0-b6abe3e19d86` (`Galileo`) for FND-045, Spec revision 5, RF-13 and CA-07/CA-14. Allowed only `apps/server/src/formalization/rest/controllers/signing-gateway-proxy.controller.ts` and its existing test; prohibited Core, Validation, Web, root/configuration, generated and SDD paths. Rule Pack: code conventions, REST, controller testing and provision boundaries. | The Builder remained running through two bounded waits and an explicit progress checkpoint without changing either allowed path, then was closed. No implementation evidence is accepted from this attempt. | `failed` |
| `EV-187` | Server/Provider proxy | Activated replacement `builder_fix_server` `01a0688d-a281-76d2-abb0-5e879e296623` (`Pasteur`) for the same FND-045 two-file boundary, Spec revision 5, RF-13 and CA-07/CA-14. | Injects a completion-route-only style using the provider's validated `style-src` nonce and targets only the signing-card button identified by its `lucide-sparkles` icon. The first handoff incorrectly used `/completed`; Orchestrator review rejected it and the resumed Builder corrected it to the real `/complete` route. Focused coverage includes positive suppression, unaffected non-completion pages, signing/download preservation and fail-safe absence for missing/unsafe CSP or HTML head data. | `passed` |
| `EV-188` | Server/Provider proxy | Orchestrator reran focused proxy Vitest, Server TypeScript, Biome, architecture and whitespace checks after the FND-045 route/format correction. | Proxy tests passed 12/12; TypeScript passed; Biome checked 793 files; architecture passed with 859 modules and 4,186 dependencies; diff whitespace passed. A live proxied completion capture remains pending because no raw active alias is recoverable from its stored hash. | `passed` |
| `EV-189` | Spec/Contract | Revision-6 material amendment and mandatory independent Spec Reviewer audit, followed by focused rechecks of all findings. | The direct 2026-09-03 decision now defines one multi-document Documenso envelope per HMS request, one package invitation/session/binding per recipient, per-document acknowledgements/artifacts, authoritative envelope/item reconciliation and all-required-item submission. The same Spec Reviewer passed the final revision with no blocking/high finding; affected-path parsing reported a canonical deduplicated ledger. | `passed` |
| `EV-190` | Cross-layer baseline | Read-only revision-6 conformance scan of `packages/core`, `packages/validation`, `apps/server` and `apps/web` using `rg` over provisioning, recipient, provider-resource, session, binding, protocol, acknowledgement and Gateway document paths. | The current integrated source still contains `ProvisionFormalizationSignatureRequestDocumentUseCase`, its document provisioning event/job, `requestDocumentId` ownership on recipients/sessions/bindings/protocols/provider resources, one-document retrieval/context and `AcknowledgeAndStartSigningUseCase`. It cannot produce one request envelope or package-level all-read flow. Prior revision-5 behavior evidence is historical and stale for changed cardinality. | `failed` |
| `EV-191` | Plan/Evaluation | Reconciled `plan.md` and this Evaluation from Spec revision 5 to revision 6 before feature source edits. | Historical evidence and failed attempts were preserved. F10–F13 now assign non-overlapping Core, Validation, Server and Web recovery boundaries; F8 owns generated/shared integration and the structural path gate; F9 retains one integrated Reviewer; G-06 is an explicit conclusion gate. | `passed` |
| `EV-192` | Core assignment | Activated `builder_core` agent `01a068d8-cddd-7530-95e2-c806a27fd737` (`Volta`) for F10-T1 after a read-only hold. Spec revision 6; RF-06–RF-25/RF-31–RF-38; CA-04–CA-14/CA-18–CA-22; allowed `packages/core/src/formalization/**` and exact Core communication event/barrel paths; prohibited Validation/apps/generated/root/SDD/unrelated paths; Core, code-conventions and use-case-testing Rule Packs; required exact revision-6 Core Create/Modify/Remove ledger; exits are focused and full Core test/lint/type/architecture plus declaration/removal/boundary and whitespace scans. | Assignment, source authorities, superseding direct decision, required package tree, observable result, non-overlapping ownership and validation exits were recorded before GO or any revision-6 feature-source edit. | `passed` |
| `EV-193` | Core | Orchestrator independently ran `pnpm --filter @hms/core test`, `check-types`, `lint` and `check:architecture` after the first F10 handoff. | Tests passed 124 files/372 tests; TypeScript passed; architecture passed with 1,005 modules/3,191 dependencies; lint exited 0 with 26 warnings and two infos. This evidence predates the revision-7 atomic provider-observation amendment and cannot validate the current Core contract. | `stale` |
| `EV-194` | Core contract review | Read-only comparison of the F10 candidate against the exact revision-6 signatures, document-reading/provider-entry algorithm and contracted tests. | Sensors pass but F10 is not conformant: Gateway context exposes `sourceDocumentId` instead of request-document ID; start-signing checks only assigned documents rather than every request document, omits required actor/eligibility revalidation and returns `/assinaturas/proxy/` instead of `/assinaturas/provedor/`; acknowledgement makes required audit fingerprints optional and omits actor input; reconciliation returns an outcome without applying transaction projections, artifact preservation or package protocols; provisioning accepts duplicate provider mappings; its required focused test contains only the stale-token case. | `failed` |
| `EV-195` | Spec/Core contract | Orchestrator compared the revision-6 singular `recordProviderObservationAndDerive` signature with one-envelope/multi-recipient reconciliation, paused F10, amended the Spec to revision 7 and reran heading, exact-signature and whitespace integrity checks. | A per-recipient call necessarily reuses stale request/Formalization versions or advances part of the envelope independently. Revision 7 replaces it with one complete `recipientObservations` batch plus shared request-document/request/Formalization mutations; singular compatibility fields and per-recipient CAS loops are explicitly prohibited. The draft retains five canonical top-level sections and passes `git diff --check`. | `failed` |
| `EV-196` | Spec review | Same Spec Reviewer `01a068ba-7cb4-72d2-9c69-c9b918f26ace` audited revision-7 draft hash `9d042aec36defe2c62a15d6dcd6dea3cdb5f95f9cb1eb5c94847ea14f0cc21fd` against sources, Rules, exact declarations and affected-path integrity. | Review completed with R7-F01 blocking and R7-F02/R7-F03 high: receipt IDs were incorrectly one-per-recipient, independently timed recipient reads did not produce a coherent envelope status, and Core mock call-count coverage did not prove adapter rollback. Orchestrator verified and accepted all three findings. | `failed` |
| `EV-197` | Spec recheck | Same Reviewer rechecked the corrected revision-7 draft hash `b347359927d3ce484811142cfecf0f3467a6f819cc72170454c2fbd4e963c58c`. | R7-F01 and R7-F02 resolved. R7-F03 remained high only because receipt rollback/replay was assigned to scheduled reconciliation even though that path correctly supplies `receiptUpdates: []`. Orchestrator accepted the narrowed finding and moved receipt-bearing database assertions to the existing webhook job test while retaining recipient/document rollback in the reconciliation job test. | `failed` |
| `EV-198` | Spec final recheck | Same Reviewer rechecked revision-7 hash `59760b33a1eabd1929a8d8a0de9f7caac6e4aab016152398df18b040f40f28d8` after the validation-ownership split. | R7-F03 resolved with no dependency regression. RF/CA traceability, exact paths, cross-layer receipt semantics, Core/Server test ownership and validation executability pass; five canonical headings and whitespace integrity pass. Revision 7 is open-ready and the Orchestrator changed its status to `open`. | `passed` |
| `EV-199` | Plan/Evaluation | Reconciled `plan.md` and this Evaluation to Spec revision 7 after EV-198, preserving historical evidence and Builder identity. | F10 now requires one coherent envelope read, independent receipt updates, exactly one batch transaction and no singular compatibility fields; F12 owns the split real-PostgreSQL webhook/reconciliation rollback evidence. Spec and Plan returned to `in_progress` before Builder resumption. | `passed` |
| `EV-200` | Core reassignment | Resume existing `builder_core` agent `01a068d8-cddd-7530-95e2-c806a27fd737` (`Volta`) for F10-T1 under Spec revision 7. Allowed paths remain `packages/core/src/formalization/**` and exact Core communication event/barrel paths; prohibited Validation, apps, generated/root/SDD/unrelated paths. | Assignment requires the exact revision-7 provider observation/provider method/transaction signatures, removal of singular compatibility fields and global cross-recipient item rejection, full FND-047 behavior tests, named runtime `AppError`s, required dependencies, actual SHA-256 and focused/full Core test/type/lint/architecture/whitespace exits. Server real-PostgreSQL rollback remains F12 ownership. | `passed` |
| `EV-201` | Core | Orchestrator independently ran the six changed focused suites, full Core test, type, architecture and lint gates, then inspected the exact provider/receipt/reconciliation/provisioning contracts. | Focused tests passed 6 files/51 tests; full Core passed 124 files/405 tests; types passed; architecture passed with 1,005 modules/3,197 dependencies; lint exited 0 with 30 warnings and two infos. Contract review failed: processing receives a receipt primary key but calls `findByDedupeKey`, never claims/checks `leaseOwner`, and can publish a receipt ID as request ID after decryption failure. Reconciliation also confirms with versions read before its observation transaction, trusts provider recipient status before all required items, accepts incomplete per-recipient item snapshots, omits request-level evidence artifacts and reintroduces mutable configuration-version equality in provisioning. | `failed` |
| `EV-202` | Spec review | Same Spec Reviewer `01a068ba-7cb4-72d2-9c69-c9b918f26ace` audited the first revision-8 draft against repository database/messaging/job rules and the receipt concurrency failure in EV-201. | R8-F01/R8-F02/R8-F03 were high: state-changing `claimById` did not belong on the low-level repository, a reusable worker owner could not distinguish stale same-worker claims, and `retry_required` had no executable Inngest mapping or terminal recovery. R8-F04 required exact attempts/due/reclaim database assertions; R8-F05 required rechecking the current draft hash. All findings were accepted. The correction moves compare-and-swap claim/fail operations to `FormalizationSignatureGatewayTransaction`, uses a fresh per-attempt `IdProvider` claim token, declares five retriable job retries and preserves exhausted failures for independent request-level reconciliation. | `failed` |
| `EV-203` | Spec final recheck | Same Spec Reviewer rechecked complete revision-8 SHA-256 `42ef58082dbba132106b09c8e163a9652b3285033c44585917699d8eecf8301b` after every R8 correction. | R8-F01 through R8-F05 are resolved with no blocking, high, medium or low regression. Repository/transaction ownership, fresh claim-token generation, process input, five-retry job mapping, real-PostgreSQL attempt accounting and recovery are executable. Hash matched; five canonical headings, one 577-row unique affected-path ledger and clean whitespace passed. Revision 8 is open-ready and the Orchestrator changed its status to `open`. | `passed` |
| `EV-204` | Plan/Evaluation | Reconciled `plan.md` and this Evaluation to open Spec revision 8 after EV-203, preserving historical evidence and the existing Builder identity. | F10 now requires primary-key claim/fail transaction operations, fresh per-execution claim tokens, complete package truth, fresh confirmation versions, all artifact kinds and deep lifecycle tests. F12 owns the exact PostgreSQL attempt accounting and five-retry Inngest behavior. Spec and Plan returned to `in_progress` only after this reconciliation. | `passed` |
| `EV-205` | Core reassignment | Resume existing `builder_core` agent `01a068d8-cddd-7530-95e2-c806a27fd737` (`Volta`) for F10-T1 under Spec revision 8. Allowed paths remain `packages/core/src/formalization/**` and exact Core communication event/barrel paths; prohibited Validation, apps, generated/root/SDD/unrelated paths. | Assignment requires FND-047/FND-049/FND-050 behavior-first corrections: exact claim-token transaction contract, no wrong-key/fabricated reconciliation behavior, complete item-derived state, fresh optimistic confirmation versions, signed PDFs plus request evidence/certificate, immutable snapshot provisioning, safe acknowledgement/context/start behavior and the full focused/full Core exits. Server PostgreSQL claim accounting and job retry wiring remain F12 ownership. | `passed` |
| `EV-206` | Core | Orchestrator reran the six focused revision-8 suites and inspected the exact webhook, reconciliation, acknowledgement, context and provider-entry branches after the Builder checkpoint. | The six files still pass 61 tests, but the candidate is not accepted. Webhook processing never classifies `receipt.eventType`, makes `reconciliation_requested` unreachable and lets an unknown valid event apply speculative graph observations, contrary to RF-17 and the explicit event map. Its parser accepts arbitrary status strings. Acknowledgement ignores the runtime `acknowledged === true` invariant and acknowledgement/start do not reject terminal or otherwise non-signable request/recipient state. Reconciliation reloads versions but can still confirm a concurrently terminal/non-submitted graph without verifying the fresh package state. The acknowledgement/start suites cover only 3/2 scenarios and omit the contracted state, identity, version, snapshot, assignment, audit and conflict matrix. | `failed` |
| `EV-207` | Spec/Core contract | Orchestrator compared RF-17's unknown-event policy with the revision-8 transaction and process response contracts after EV-206. | Revision 8 can owner-check observation finalization or fail a claim, but cannot mark a valid reconciliation-only receipt processed without fabricating graph observations, bypassing claim ownership or leaving the lease to expire. Revision 9 adds exact-token `completeWebhookReceiptClaim` and a strict provider-neutral encrypted `observation`/`reconciliation_only` hint produced by the Server Documenso adapter. F10 remains paused pending mandatory same-reviewer audit and Plan reconciliation. | `failed` |
| `EV-208` | Spec review | Same Spec Reviewer audited revision-9 draft SHA-256 `8074f8f36a0949a262025acf13cfeb65bd13fd4399c43c47d9450d7b2432e39c` against Core/database/Server/provision/messaging/job Rules and the current webhook controller. | R9-F01–R9-F03 were high and accepted. The draft added receipt-only completion but left raw `eventType`/provider ID/encrypted-payload fields in Core/persistence, named no exact Server normalization declaration/path, and omitted complete Server mapping plus PostgreSQL reconciliation-only test ownership. The correction replaces the receipt boundary with neutral `hintKind`/`encryptedHint`, declares `DocumensoWebhookNormalizer` and its module/controller composition, adds its exact path/test, and expands receiver/controller/job evidence. | `failed` |
| `EV-209` | Spec final recheck | Same Spec Reviewer rechecked complete revision-9 SHA-256 `1574b726b3631097f1ae19e0faf62612c59926a26dde3917ad65279ff99b6ca3` after R9-F01–R9-F03 correction. | All three findings are resolved with no blocking, high, medium or low regression. Neutral Core/persistence receipts, exact Server normalizer ownership/composition, complete cross-layer test ownership and receipt-only transaction semantics pass. Hash matched; five canonical headings, one 580-row valid unique ledger and clean whitespace passed. Revision 9 is open-ready and the Orchestrator changed its status to `open`. | `passed` |
| `EV-210` | Plan/Evaluation | Reconciled `plan.md` and this Evaluation to open Spec revision 9 after EV-209, preserving historical evidence and the existing Builder identity. | F10 now owns neutral receipt fields, exact-token receipt-only completion, strict closed hint parsing, trusted reconciliation-only publication, fresh submitted-state confirmation guards and deep access tests. F12 owns the named Documenso normalizer, neutral persistence/migration and complete real-PostgreSQL job evidence. Spec and Plan returned to `in_progress` only after reconciliation. | `passed` |
| `EV-211` | Core reassignment | Resume existing `builder_core` agent `01a068d8-cddd-7530-95e2-c806a27fd737` (`Volta`) for F10-T1 under Spec revision 9. Allowed paths remain `packages/core/src/formalization/**` and exact Core communication event/barrel paths; prohibited Validation, apps, generated/root/SDD/unrelated paths. | Assignment requires neutral receipt entity/receiver contracts, `completeWebhookReceiptClaim`, strict discriminated processing, reconciliation-only no-graph behavior, literal acknowledgement, signable request/recipient guards, fresh submitted-state confirmation checks and the complete focused/full Core exits. Server normalizer, persistence migration and job wiring remain F12 ownership. | `passed` |
| `EV-212` | Core checkpoint | Existing `builder_core` agent `01a068d8-cddd-7530-95e2-c806a27fd737` implemented the revision-9 neutral receipt model, receipt-only completion, strict hint parsing, signable acknowledgement/start guards, fresh reconciliation checks and expanded focused tests. The Orchestrator independently reran the six focused suites and all Core sensors. | Focused tests passed 6 files/144 tests; full Core passed 124 files/505 tests; types passed; architecture passed with 1,005 modules/3,197 dependencies; lint exited 0 with 36 warnings and two infos; whitespace and provider-neutral receipt scans passed. Sensor success remained provisional pending source/contract review. | `passed` |
| `EV-213` | Core implementation review | Independent reviewer `01a0699a-b041-7ee2-bbc8-0f2864e40a79` (`Avicenna`) audited the integrated F10 checkpoint against Spec revision 9, the detailed test ledger and current Core source after EV-212. | Rejected the checkpoint despite green sensors. Blocking: result sessions and `GetSignatureResultUseCase` hard-code `submitted`, omit request/recipient/protocol authority and expiry. High: authenticated document/context reads omit expiry/live collaborator assignment checks; collaborator session establishment lacks request/snapshot/terminal binding; webhook receiver dedupe is check-then-insert; one terminal item fans out terminal state to unrelated/completed documents; signed PDFs accept empty/non-PDF bytes; provider binding creation/persistence is not retry-safe and existing active bindings are rejected. Medium: duplicate hint item IDs and terminal observation handling are unproven; PostgreSQL atomicity remains F12 evidence. | `failed` |
| `EV-214` | Core correction assignment | Activated `builder_fix_core` agent `01a069a6-ece6-7e22-a4f6-74bcbe710777` (`Lovelace`) for EV-213/FND-052 under Spec revision 9. Allowed paths are `packages/core/src/formalization/**` and indispensable existing Core communication event/barrels; apps, Validation, generated/root/SDD and unrelated paths are prohibited. | The broad owner remained in analysis without a patch or checkpoint through bounded progress windows and was shut down. No completion is inferred; the correction is split into four disjoint Core write sets in EV-215–EV-218. | `failed` |
| `EV-215` | Core access/result reassignment | Activated `builder_fix_core` agent `01a069b0-bcc0-7783-a0c6-2fbf3d1139f3` (`Hilbert`) for only the context, result, document-read and collaborator-session use cases plus their colocated tests. | Requires authoritative result-state/protocol narrowing, active/unexpired device-bound sessions, request/recipient/document/snapshot membership and live collaborator identity/profile/assignment revalidation. No other Core, app, Validation or SDD paths. | `passed` |
| `EV-216` | Core webhook reassignment | Activated `builder_fix_core` agent `01a069b0-d6fd-7401-93e2-a9eaa2957780` (`Carver`) for only receive/process webhook use cases and their colocated tests. | Requires conflict-safe neutral receipt dedupe, duplicate item-ID rejection and monotonic terminal-hint handling while preserving revision-9 exact-token semantics. No other paths. | `passed` |
| `EV-217` | Core reconciliation reassignment | Activated `builder_fix_core` agent `01a069b0-c8f6-7af0-9080-112f438aaae2` (`Kuhn`) for only reconciliation use case/tests. | Requires per-item terminal derivation, completed/unrelated artifact preservation and nonempty valid PDF checks before storage/confirmation. No other paths; PostgreSQL atomicity remains F12. | `passed` |
| `EV-218` | Core provider-entry reassignment | Activated `builder_fix_core` agent `01a069b0-b33f-7192-89f5-e0d3c3ee1668` (`Laplace`) for only start-signing use case/tests. | Requires expiry and retry-safe package binding behavior consistent with the provider adapter's credential-read semantics, one active binding and zero credential exposure. No other paths. | `passed` |
| `EV-219` | Core corrected checkpoint | Integrated EV-215–EV-218 and independently ran ten affected use-case suites plus all Core sensors. | Focused tests passed 10 files/199 tests; full Core passed 124 files/541 tests; types passed; architecture passed with 1,005 modules/3,206 dependencies; lint exited 0 with 25 warnings and two infos; whitespace passed. Result/session authority, conflict-safe neutral dedupe, per-item terminal derivation, PDF integrity and provider-entry retry tests are present. Existing active bindings remain fail-closed because only the alias hash is persisted; no plaintext recovery was introduced. Final acceptance awaits the same independent reviewer's EV-213 recheck. | `passed` |
| `EV-220` | Core implementation recheck | Same independent reviewer `01a0699a-b041-7ee2-bbc8-0f2864e40a79` (`Avicenna`) rechecked every EV-213 finding against the EV-219 source and tests. | Result-state authority, context/document membership, collaborator-session binding, conflict-safe receipt insertion, per-item reconciliation and PDF validation are materially corrected. F10 still fails on two blocking and two high defects: `RecordProviderSubmissionUseCase` stores raw result/CSRF secrets in hash columns, omits invitation/binding revocation, fabricates Formalization identity/version, ignores `providerObservationId`, omits session expiry and returns secrets after transaction conflict; provider entry rejects the only recoverable active-binding state and does not atomically rotate an alias or advance signing state; acknowledgement omits session expiry and exact permitted collaborator-source checks; webhook processing can apply terminal item/envelope observations while forcing request/Formalization `in_progress`. These expose Contract gaps, so revision 10 is required before correction. | `failed` |
| `EV-221` | Spec revision 10 first audit | Same Spec Reviewer `01a068ba-7cb4-72d2-9c69-c9b918f26ace` inspected the evolving revision-10 draft after the assigned `717e2b38…` hash had changed. | Audit failed first on immutable-hash mismatch. It also accepted six substantive gaps: context GET's CSRF recovery was inconsistent with the non-mutating wording and response types; newly minted result secrets could themselves be lost after submission commit; Validation still admitted a forbidden provider context; confirmation/terminal transactions retained caller aggregate changes; exact PostgreSQL controller ownership was missing; and binding change/repository contracts could not execute alias-hash CAS rotation. | `failed` |
| `EV-222` | Spec revision 10 first correction recheck | Same Spec Reviewer rechecked frozen SHA-256 `f053e8d20e048dd41aab26a08c1bcd49d6a2f2df1daf6c0bc133e896a900eee2` after the R10-F02–R10-F07 correction. | R10-F01, F02 and F04–F07 resolved. R10-F03 remained blocking because the recipient table omitted durable `submission_observation_id`, so duplicate predecessor proof could not survive a transaction/process boundary. A high dependency regression also retained language for an independent result bearer/cookie after the in-place session-kind decision. | `failed` |
| `EV-223` | Spec revision 10 final recheck | Same Spec Reviewer rechecked frozen SHA-256 `d94fba83e7190a2e69a99f1fc73ffd8ab3fd15d9bf90272ec14663371554e291` after the final persistence/security correction. | Hash matched; R10-F03 and the high dependency regression are resolved; full source/RF/CA traceability, paths/declarations, cross-layer contracts, Rule/test ownership, generation/migration commands, design handoff and Validation executability pass with no regression or ambiguity. Five canonical headings, one 580-row valid unique ledger and whitespace pass. Revision 10 is open-ready and the Orchestrator changed its status to `open`. | `passed` |
| `EV-224` | Plan/Evaluation and Core reassignment | Orchestrator reconciled Plan/Evaluation to reviewed Spec revision 10 and resumed existing `builder_core` agent `01a068d8-cddd-7530-95e2-c806a27fd737` (`Volta`). | Assignment is bounded to `packages/core/src/formalization/**` and exact indispensable Core barrels/events. It owns `startProviderEntry`, alias CAS changes, in-place result conversion/deterministic observation proof, acknowledgement authority, context CSRF/recovery, transaction-owned observation/confirmation/terminal aggregates and behavior-first tests. Apps, Validation, root/generated and SDD files remain prohibited; real PostgreSQL implementation remains F12. | `passed` |
| `EV-225` | Core revision-10 checkpoint and implementation recheck | Existing `builder_core` completed the EV-220 correction; the Orchestrator independently ran ten focused suites, full Core tests, types, architecture and lint, then the same implementation reviewer `01a0699a-b041-7ee2-bbc8-0f2864e40a79` rechecked the candidate. | Focused tests passed 10 files/224 tests; full Core passed 124 files/592 tests; types passed; architecture passed with 1,005 modules/3,209 dependencies; lint exited 0 with 25 warnings and two infos. Provider-entry recovery, in-place result conversion, acknowledgement authority, context CSRF/recovery and observation-scope ownership are corrected. F10 remains rejected because the submitted event uses every request document instead of the recipient's immutable assignment set, and a commit followed by failed event publication returns as a silent duplicate on retry instead of restoring the submitted signal or requesting `ambiguous_submission` reconciliation. Transaction-side atomic effects remain explicitly pending F12 PostgreSQL evidence. | `failed` |
| `EV-226` | Core final correction and same-reviewer acceptance | Existing `builder_core` corrected the EV-225 recipient-assignment event scope and post-commit publication recovery; the Orchestrator reran the focused suite and type check, and the same reviewer `01a0699a-b041-7ee2-bbc8-0f2864e40a79` performed a narrow read-only recheck. | The focused submission suite passed 1 file/25 tests; full Core passed 124 files/600 tests in the Builder exit; types and architecture passed; lint exited 0 with 25 warnings and two infos; whitespace passed. Submission now rejects malformed assignment graphs, emits only the recipient's assigned document IDs in package order, and converts durable duplicate replay into an `ambiguous_submission` reconciliation signal. The reviewer reported no remaining finding and no EV-220 regression. F10 is accepted; real PostgreSQL atomicity remains F12 evidence. | `passed` |
| `EV-227` | Validation revision-10 implementation and F12 activation | Existing `builder_validation` aligned strict package acknowledgement/documents/context/result/webhook/event schemas and barrels with accepted Core; the Orchestrator independently reran Validation types, architecture and lint, then activated `builder_server` `01a06a25-d56e-7322-b367-da858f166472` (`Descartes`) for F12. | Validation types passed; architecture passed with 102 modules/242 dependencies; lint checked 94 files with no diagnostics. Provider-only context/proxy/credential scans are clean; unknown keys remain strict and no Validation test suite was added. F11 is accepted. F12 owns only listed Server paths and must provide real PostgreSQL/provider/job/controller evidence; generated migration metadata remains Orchestrator-owned. | `passed` |
| `EV-228` | Direct F12 request-level implementation | By explicit user direction, the Orchestrator completed Server integration without further reviewer agents: request-level Drizzle graph/transaction, recipient-document and provider-item mappings, one multi-PDF Documenso envelope, request-scoped provisioning, Gateway document/acknowledgement/start routes, private PDF response headers and normalized webhook handling. | Server types, code and architecture pass; focused controller/provider/job suites pass; Documenso proves one create call with two PDF parts and both recipient assignments; the Docker-backed canonical provisioning job passes 3/3; authenticated-but-unmappable webhooks return 202 without persistence while infrastructure failures remain retryable. | `passed` |
| `EV-229` | Consolidated migration and F13 Web implementation | Replaced incompatible unreleased 0042/0043 intermediates with generated `0042_blushing_sinister_six`; applied all migrations to an isolated PostgreSQL database, verified 25 signature tables and three critical uniqueness indexes, then removed the temporary database. Web now renders ordered document tabs, fetches one private PDF at a time, records independent acknowledgements and enters the shared provider once only after all assigned documents are read; the obsolete separate acknowledgement step was removed. | Disposable migration passed. Focused Web package/service/hook/widget tests pass 33/33 plus the sequential two-document page suite 10/10; full Web passes 138 files/483 tests; Web types, code and architecture pass. Real client/collaborator browser ceremony remains F13/F8 evidence. F9 reviewer work is waived by explicit user direction. | `passed` |
| `EV-230` | Documenso local origin/auth browser correction | Read-only browser network/console capture at `http://localhost:3004/signin`, Compose environment inspection, `docker compose up -d --force-recreate --no-deps documenso`, health verification and browser reload. | Before correction, the page requested `http://127.0.0.1:3004/api/auth/session-json` from the `localhost:3004` origin and Chromium reported a CORS failure, leaving sign-in in the generic “Unable to sign in” state. Compose now defaults `DOCUMENSO_PUBLIC_WEBAPP_URL`/`NEXT_PUBLIC_WEBAPP_URL` to `http://localhost:3004`; after recreation the same request returned `200` with zero console errors. Container health, database and certificate checks remain `ok`. | `passed` |
| `EV-231` | Web cancellation state correction | Direct Orchestrator implementation under Spec revision 10 and FND-054; focused action-hook/widget tests plus Web package type/code/architecture checks and whitespace validation. | The cancellation REST action is accepted before the asynchronous provider job reaches terminal state. The Web sending controller now refreshes related queries, exposes an immediate cancellation-pending state and polls the authoritative review/status endpoints until terminal. The internal configuration therefore updates without a reload while reset remains locked until provider/request reconciliation is complete. Core/Server/Web types, Web static gates, focused 31-test coverage and the full Web suite (139 files/494 tests) pass. | `passed` |
| `EV-232` | Web/Playwright integration preflight | `pnpm --filter web exec playwright test tests/routes/formalization/formalization-sending-configuration.test.tsx --project=chromium` with healthy Compose/Auth/Nest dependencies. | Both route scenarios timed out in the shared `auth` fixture while waiting for `/home` after login, before reaching the Formalization route. No cancellation assertions ran; this is fixture/auth integration evidence, not a failure of the focused cancellation correction. | `failed` |
| `EV-233` | Cross-signer reading correction and real client replay | Direct Orchestrator correction under the existing F13 scope, by the user's standing direction to continue implementation without reviewer agents. Allowed paths are the acknowledgement/start/OTP Core use cases and tests, the OTP repository contract/adapter, Gateway reading page hook/widget tests and this Plan/Evaluation. | Core focused tests pass: 4 files/96 tests. Web focused tests pass: 3 files/25 tests. Core, Server and Web type checks pass; all three architecture checks pass. Real PostgreSQL/Mailpit/browser replay on request `efd006a3-63cd-4b06-8495-8ce0da996219` passed twice: desktop confirmed the first document without unmounting its loaded PDF iframe, and 390×844 keyboard activation confirmed the second document without private-content errors; acknowledgement/content responses were `200` and no Gateway response was 4xx/5xx. Exact port-5555 replay also proves OTP generation continues after consumed challenges and rolls into a new 30-minute window. | `passed` |
| `EV-234` | Authenticated-admin/client Gateway isolation regression | Focused Core tests for context, private document retrieval, acknowledgement and signing start; full Core suite; Core/Server type and architecture checks; lint. | The client Gateway flow now ignores an unrelated HMS actor already authenticated in the same browser while retaining exact actor enforcement for collaborators. Four focused files passed 114 tests; full Core passed 58 files/386 tests; Core types/architecture, Server types and lint passed. No fresh real-browser ceremony is claimed for this correction. | `passed` |
| `EV-235` | Revision-12 accepted-risk decision | Explicit user selection of option 2 on 2026-09-07 after the conclusion-readiness audit. | MV-01–MV-08, remaining visual evidence, G-01/G-02/G-05/G-06, FND-047/FND-049/FND-050/FND-052 and reviewer evidence are waived for conclusion. They are not marked passed or technically resolved; public/staging/production activation is not authorized. | `passed` |
| `EV-236` | Revision-13 current-worktree aggregation | Regenerated the canonical affected-path ledger from the exact Git state used by `check:spec-implementation`, relative to resolved `origin/develop` SHA `529b874808c88f8ec098fface014e2cc30f8c031`, including untracked non-ignored files. | Every current change is included without changing implementation files. PATH-03 reports 1,279 declared/current paths, 1,279 passed paths, zero failed checks and zero unrelated changes. | `passed` |

## Structural path gate

| ID | Command | Base ref | Base SHA | Counts | Result | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `PATH-01` | `pnpm check:spec-implementation -- documentation/features/formalization/signing-gateway/spec.md --base origin/develop --json` | `origin/develop` | `7aa8684f02593f17c9201f89b16c666a8efa19a8` | `declaredPathsCount=0`; `passedPathsCount=0`; `failedChecksCount=1`; `changedFilesCount=1310`; `unrelatedChangedFilesCount=1310` | `MISSING_AFFECTED_PATH_TABLE`: the Spec has no table beginning exactly with `Path | Change`; path comparison did not run. | `failed` |
| `PATH-02` | `pnpm check:spec-implementation -- documentation/features/formalization/signing-gateway/spec.md --base origin/develop --json` | `origin/develop` | `529b874808c88f8ec098fface014e2cc30f8c031` | `declaredPathsCount=580`; `passedPathsCount=451`; `failedChecksCount=259`; `changedFilesCount=1279`; `unrelatedChangedFilesCount=788` | Current structural gate fails because the branch contains a broad mixed-feature diff and the ledger also has stale Create/Modify declarations. This repository-wide control is outside the revision-12 feature waiver. | `failed` |
| `PATH-03` | `pnpm check:spec-implementation -- documentation/features/formalization/signing-gateway/spec.md --base origin/develop --json` | `origin/develop` | `529b874808c88f8ec098fface014e2cc30f8c031` | `declaredPathsCount=1279`; `passedPathsCount=1279`; `failedChecksCount=0`; `changedFilesCount=1279`; `unrelatedChangedFilesCount=0` | Every current tracked or untracked non-ignored change is included in revision 13. | `passed` |

## Manual evidence

| ID | Scenario | Criteria | Expected | Observed | Status |
| --- | --- | --- | --- | --- | --- |
| `MV-01` | Local automated lifecycle and race suite | `CA-01`, `CA-02`, `CA-03`, `CA-06`, `CA-10`, `CA-18` | All exchange, channel, OTP, acknowledgement, send, webhook and transaction race cases pass with idempotent persistence and no secret leakage. | Not run; accepted risk under EV-235. | `waived` |
| `MV-02` | Local full-stack client signing flow | `CA-05`, `CA-08`, `CA-09`, `CA-12`, `CA-17`, `CA-18`, `CA-19`, `CA-20`, `CA-22` | A configuration with at least two PDFs creates one request envelope and one package invitation; the client authenticates, reads and acknowledges every PDF in HMS tabs, enters the real pinned provider once, completes the ceremony and reaches submitted then confirmed with one signed artifact per item. | Fresh revision-12 execution waived; accepted risk under EV-235. | `waived` |
| `MV-03` | Local real Auth collaborator flow | `CA-04`, `CA-06` | The exact seeded collaborator signs in through `/login`, passes safe return, and completes the assigned flow without OTP. | Fresh revision-12 execution waived; accepted risk under EV-235. | `waived` |
| `MV-04` | Controlled failure and recovery | `CA-11`, `CA-12`, `CA-13`, `CA-19`, `CA-21` | PDF/provider/webhook/artifact failures remain fail-closed and reconcile without duplicate effects; cancellation revokes access and preserves evidence. | Not run; accepted risk under EV-235. | `waived` |
| `MV-05` | Token-boundary release gate | `CA-07`, `CA-14` | Canary scan across browser and server surfaces finds zero raw secrets or provider URLs. | Not run; accepted risk under EV-235. | `waived` |
| `MV-06` | Accessibility and responsive flow | `CA-15`, `CA-17`, `CA-21`, `CA-22` | Keyboard, screen-reader, narrow viewport, zoom, themes and motion states remain operable. | Not run; accepted risk under EV-235. | `waived` |
| `MV-07` | Staging readiness | `CA-08`, `CA-10`, `CA-12`, `CA-19`, `CA-20` | Resend, provider privacy, webhook, X.509, private origin and pinned runtime evidence pass. | Not run; accepted risk under EV-235. | `waived` |
| `MV-08` | Security and operations review | `CA-14`, `CA-16` | Security, AGPL, scanning, alerting and redacted operations evidence is complete. | Not run; accepted risk under EV-235. | `waived` |

## Visual evidence

| ID | Surface and state | Viewport | Reference | Implementation | Differences | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `VIS-01` | Internal configuration confirmed/configuring | 1200×1830; 390×844 | `design/qOfh6.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-02` | Internal ready-for-sending action card | 1200 desktop; 390×844 | `design/YWfhi.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-03` | Internal add-signatory dialog | 1200 desktop; 390×844 | `design/sxENj.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-04` | Internal candidate search dialog | 620×680; 390×844 | `design/Vx43H.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-05` | Internal positioning fields | 1200×1100; 390×844 | `design/HcT8k.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-06` | Internal review and confirm dialog | 760-wide; 390×844 | `design/nI1B0.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-07` | Internal provisioning/delivery/partial failure/sent | 1200×1374; 390×844 | `design/MC4E2.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-08` | Internal cancel-all dialog | 480-wide; 390×844 | `design/NSYug.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-09` | External access invitation | 1200×900; 390×844 | `design/01-access-invitation.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-10` | External channel selection, revision-5 e-mail-only interpretation | 1200×900; 390×844 | `design/02-select-channel.png` | — | Historical WhatsApp/multi-channel visual must not be copied; comparison waived under `EV-235`. | `waived` |
| `VIS-11` | External OTP verification | 1200×900; 390×844 | `design/03-verify-otp.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-12` | External multi-document reading tabs | 1200×900; 390×844 | Revision-6 amendment of `design/04-read-document.png` | — | Must add a semantic tablist, one native PDF viewer for the active document, per-document read state and no custom zoom controller; comparison waived under `EV-235`. | `waived` |
| `VIS-13` | Per-document acknowledgement and package all-read gate | 1200×900; 390×844 | Revision-6 interaction amendment of `design/05-confirm-signature.png`; no separate acknowledgement screen | — | Must persist acknowledgement in the reading surface and enable one provider action only after every package document is acknowledged; comparison waived under `EV-235`. | `waived` |
| `VIS-14` | External submitted | 1200×900; 390×844 | `design/06-signature-submitted.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-15` | External confirmed | 1200×900; 390×844 | `design/07-signature-confirmed.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-16` | External collaborator package tabs | 1200×900; 390×844 | Revision-6 amendment of `design/09-collaborator-read.png` | — | Must expose the same ordered package/tab/read behavior after exact HMS collaborator authentication; comparison waived under `EV-235`. | `waived` |
| `VIS-17` | Collaborator per-document acknowledgement and all-read gate | 1200×900; 390×844 | Revision-6 interaction amendment of `design/10-collaborator-confirm.png`; no separate acknowledgement screen | — | Must share the package acknowledgement/provider-entry behavior without introducing OTP; comparison waived under `EV-235`. | `waived` |
| `VIS-18` | External access unavailable | 1200×900; 390×844 | `design/11-access-unavailable.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-19` | External no authorized channel | 1200×900; 390×844 | `design/12-no-authorized-channel.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-20` | External invalid/expired OTP | 1200×900; 390×844 | `design/13-otp-invalid-expired.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-21` | External OTP attempt limit | 1200×900; 390×844 | `design/14-otp-attempt-limit.png` | — | No current implementation capture; waived under `EV-235`. | `waived` |
| `VIS-22` | Derived PDF/provider/rejected/cancelled/expired/ineligible/reconciliation states | 1200×900; 390×844 | `design/11-access-unavailable.png` | — | Required variants are not implemented; visual comparison waived under `EV-235`. | `waived` |
| `VIS-23` | Collaborator login after CSRF-aware exchange | 1200×900; 390×844 | No supplied canonical login reference; behavior derives from RF-07/RF-25 and the HMS design system. | Inspected transient captures `/tmp/hms-signing-gateway-csrf-desktop.png` and `/tmp/hms-signing-gateway-csrf-narrow.png`. | Card, heading, copy and full-width `Entrar` action remain aligned and operable at both viewports; no visible regression, overflow, failed request, console error or invitation token in the URL. | `passed` |
| `VIS-24` | Client OTP and collaborator authenticated reading after tab blur/focus | 1200×900; 390×844 | Supplied user screenshots plus RF-05–RF-10/RF-26 state continuity; no new visual composition is requested. | User-supplied before-state captures; focused lifecycle regression and isolated route replay recorded in `EV-146`. | The correction changes lifecycle ownership rather than composition: OTP input/code and collaborator reading remain mounted across blur/focus and refreshed auth sessions. Isolated Chromium remains visually operable at narrow and desktop viewports; exact continuity is asserted at the hook boundary where the Supabase session change is controlled. | `passed` |
| `VIS-25` | Documenso signed-recipient completion without provider account promotion | 1440×900 | User screenshot with the undesired “Need to sign documents?” claim-account panel. | Inspected transient capture `apps/web/.playwright-cli/documenso-complete-without-claim.png`. | Completion confirmation remains visible; the entire account promotion, name/e-mail/password form and claim button are absent. No overflow or replacement blank column remains. | `passed` |

## Rule and documentation compliance

| Authority | Reference | Result | Notes |
| --- | --- | --- | --- |
| SDD | `documentation/sdd.md` | `passed` | Revision-6 Spec passed mandatory independent review; the stale Plan and Evaluation were reconciled before source assignments. |
| Rule Pack | `documentation/rules/rules.md` and selected Rule files | `passed` | Rule Pack was read and mapped in the Plan before activation. |
| Design | `documentation/design.md`, `design/manifest.md` | `passed` | Design manifest and references exist; PNGs are preserved as evidence inputs. |
| Architecture | `documentation/architecture.md` | `passed` | Signing Gateway HTML template placement and text fallback are documented alongside the approved email transports. |
| Modules | `documentation/modules.md` | `passed` | Final boundary review confirms Formalization owns request/signing lifecycle state; Communication owns delivery and shared module boundaries remain respected. |
| Infrastructure | `documentation/infrastructure.md` | `passed` | Static Signing Gateway HTML assets, local Mailpit transport, staging/production Resend transport and plain-text fallback are documented. |
| Tooling | `documentation/tooling.md` | `passed` | pnpm/Turbo/Biome/Vitest/Drizzle commands govern execution. |

## Findings

| ID | Classification | Source | Affected evidence | Status | Resolution |
| --- | --- | --- | --- | --- | --- |
| `FND-001` | environment | Required services were stopped at kickoff. | `EV-01` | `resolved` | Started existing Compose dependencies and persistent Server/Web sessions; health and Nest bootstrap now pass. |
| `FND-002` | implementation | Revision-5 request-side and Gateway tree was absent from the baseline. | `EV-02`; `EV-228`; `EV-229`; `CA-01`–`CA-22` | `resolved` | The F10–F13 implementation replaces the superseded baseline with the request-scoped Core, Server and Web lifecycle. Remaining fresh ceremony evidence is separately recorded as accepted risk under `EV-235`. |

| `FND-003` | implementation | `builder_core` handoffs | `EV-10`; `EV-11`; `EV-27`; `EV-28`; F1-T1 exit; `CA-01`–`CA-06`, `CA-09`–`CA-15`, `CA-17`–`CA-22` | `resolved` | Lifecycle behavior coverage was strengthened for confirmation, provisioning, reconciliation, webhook ordering, cancellation, submission/result and OTP races; current F1 exit passes. Artifact storage/download remains mapped to Server. |
| `FND-004` | implementation/orchestration | Repeated `builder_core` correction attempt | `EV-12`; `EV-25`; `EV-28`; F1-T1 exit; `CA-01`–`CA-06`, `CA-09`–`CA-15`, `CA-17`–`CA-22` | `resolved` | Explicit hasher/verifier/credential/metadata boundaries and non-empty contract checks are now present; current Core security scans and sensors pass. |
| `FND-005` | implementation | Current-candidate Orchestrator sensor run | `EV-13`; `EV-15`; F1-T1 exit; `CA-03` | `resolved` | `builder_fix_core` corrected the callable verifier mock and guard-update test setup; all current Core sensors pass. |
| `FND-006` | implementation/security | Current-candidate Core contract inspection | `EV-16`; `EV-25`; F1-T1 exit; `CA-01`–`CA-14`, `CA-18`–`CA-21` | `resolved` | Explicit hasher/credential/metadata boundaries are now wired; current scans show no raw hash fallback or empty provider/proxy/result placeholder, and Core type/tests pass. Remaining lifecycle evidence is tracked separately by FND-003. |
| `FND-007` | implementation/orchestration | Interrupted `builder_fix_core` correction | `EV-20`; `EV-22`; `EV-24`; F1-T1 exit; current Core type/test sensors | `resolved` | `builder_fix_core` completed the missing error import and corrected the artifact purpose; current Core sensors pass. |
| `FND-008` | implementation/integration | Server `FormalizationSignatureSourceReader` was behind the Core contract and its fixture mock omitted the new authentication-source methods. | `EV-32`; `EV-36`; F3-T1 exit | `resolved` | The consumer methods and fixture mocks now implement the current contract; inactive/ineligible actors and clients without e-mail consent fail closed, and current Server sensors pass. |
| `FND-009` | validation/integration | The historical broad Server test run reached unrelated Formalization controller regressions and database bootstrap notices. | `EV-32`; `EV-228`; `EV-236` | `accepted_risk` | The current delivery uses focused Server/provider/controller/job evidence and the package static gates; the remaining broad-suite/environment noise is retained as accepted risk under the explicit revision-12 waiver and is not presented as a pass. |
| `FND-010` | validation/orchestration | The persistence layer cannot be evidenced through direct repository/adapter implementation tests under the database rules. | `EV-30`; `EV-228`; `EV-235` | `accepted_risk` | Indirect persistence effects were exercised by focused controller/job/disposable-database evidence; the remaining fresh full-flow proof is waived under `EV-235`, with no direct repository test added. |
| `FND-012` | validation/orchestration | The historical F3 wording requested direct repository/transaction tests even though the database rules prohibit them. | `EV-45`; `documentation/rules/database-layer-rules.md`; `EV-228`; `EV-235` | `resolved` | The Plan is reconciled to indirect controller/complete-flow evidence through real bindings and PostgreSQL; the conclusion waiver covers only the remaining fresh integrated ceremony. |
| `FND-011` | database/orchestration | The initial generated F4 migration contained PostgreSQL identifiers longer than 63 bytes whose truncation collided with unique constraints, so forward migration apply failed. | `EV-39`; `EV-42`; `EV-43`; F4-T1 exit | `resolved` | Source model constraints now use explicit globally unique names; migration `0042_signing_gateway` was regenerated and applied successfully. Legacy drop notices remain non-blocking. |
| `FND-013` | contract/integration | The first F5 activation exposed missing status, metadata and cancellation composition. | `EV-47`; `EV-228`; `EV-229`; `EV-235` | `resolved` | Status, review, metadata, confirm/cancel composition, typed Web surface, Documenso provider, provisioning/cancellation jobs, public Gateway, webhook, proxy and Communication delivery are integrated. Fresh browser/provider-certificate/staging evidence remains explicitly waived and is not claimed as passed. |
| `FND-014` | messaging/integration | Versioned signing events were named `.v1` and validated with `version: 1`, but their domain constructors omitted the transport version. Inngest rejected provisioning events before the job handler, so no provider call occurred. | `EV-88`; `EV-86`; CA-18–CA-20 | `resolved` | All ten versioned signing event constructors now add `version: 1` and the provisioning job has an integration regression covering trigger acceptance. |
| `FND-015` | provider/integration | Confirmation stored source DOCX metadata in immutable request documents instead of the ready PDF preview metadata. Documenso reached `envelope.create` but correctly rejected the payload as `INVALID_DOCUMENT_FILE`; the existing request cannot be repaired by code because its snapshot is immutable. | `EV-88`; `EV-87`; CA-08, CA-18–CA-20 | `resolved` | The metadata reader now resolves `findReadyPreviewFileId(formalizationId, previewId)` and the Core confirmation/review contracts pass the preview ID. The affected local request must be cancelled and recreated. |
| `FND-016` | database/integration | The seed cleanup deleted `formalizations` while Signing Gateway request records still referenced them through `RESTRICT` foreign keys, causing `db:seed` to exit 1 without a useful application error. | `EV-89`; F3/F4 seed reset exit | `resolved` | Added `removeAll()` to the Spec-listed `FormalizationSignatureGatewayTransaction`, deleting Signing Gateway records in dependency order before the existing Formalization seeder cleanup. Seed observability now prints the underlying exception when a future run fails. |
| `FND-017` | specification/tooling | The initial Signing Gateway Spec used an affected-path ledger header that the repository structural checker did not recognize. | `EV-90`; `PATH-01`; `PATH-03`; `EV-236` | `resolved` | Revision 13 uses the checker-recognized `Path | Change` ledger and the current gate reports every current path declared and passing. Historical failures remain in the evidence trail only. |
| `FND-018` | delivery/integration | Provisioning successfully created provider resources, but no Communication Inngest consumer existed for the published invitation/OTP delivery commands, leaving send attempts pending and producing no relevant delivery run. | `EV-91`; `EV-92`; CA-20 | `resolved` | Added the Resend/Mailpit EmailProvider, identity/consent revalidation, encrypted payload handling, dedicated Communication send jobs, Formalization outcome jobs and scheduled pending-delivery reconciliation. |
| `FND-019` | implementation/packaging | Static HTML templates placed below the Biome-scanned Server source tree were parsed as code and the production build did not copy them as runtime assets. | `EV-94`; `EV-95`; `EV-96` | `resolved` | Moved the templates to `volumes/communication/templates` beside the existing Auth templates, added a deterministic local/runtime path resolver, and copied the directory into the Server image at `/app/templates/communication`. |
| `FND-020` | implementation/integration | A collaborator invitation was incorrectly sent through the client channel-selection state, so the recipient saw “Confirme seu e-mail” despite the delivery address being the collaborator account. | `EV-98`; `EV-99`; CA-04, CA-06, CA-15 | `resolved` | Gateway context now resolves the recipient source before selecting the authentication branch; collaborators go through HMS login with an allow-listed return target, while only consented natural-person clients use e-mail OTP. Session establishment also verifies the authenticated collaborator matches the invited recipient. |
| `FND-021` | implementation/concurrency/authorization | Internal cancel-all sends the Formalization version to a use case that compared it with the request version, then used the request version for the Formalization CAS; the Server boundary also rejected administrators even though review/status allow them. | `EV-100`; `EV-101`; `EV-103`; `EV-105`; `EV-106`; `EV-108`; RF-36–RF-38; CA-13, CA-17, CA-18 | `resolved` | Core now reads the request version and passes it separately from the command's Formalization version. The REST boundary passes collaborator profile and permits only the assigned lawyer or an administrator. Cancellation attempts use their own IDs, provider `DRAFT`/missing-resource cases converge safely, and focused Core/Server/Web tests plus authenticated REST/Inngest/Database evidence pass. |
| `FND-022` | implementation/database | Resetting a configuration after cancellation deleted all current signatory rows even though historical request recipients retain restrictive foreign keys to those rows. The UI action appeared available but the real reset transaction failed at PostgreSQL. | `EV-111`; `EV-112`; `EV-113`; `EV-114`; RF-36–RF-38; CA-21 | `resolved` | The reset persistence path now preserves signatory rows referenced by historical recipients, deletes only unreferenced rows, and updates/reuses the default signatories supplied by the reset use case. The authenticated local reset returned `200`, preserved the cancelled request/recipient history and produced a clean editable configuration; the widget regression verifies the action remains available in the cancelled state. |
| `FND-023` | implementation/integration | After reset/edit mutations, the cancelled request remained in the sending review cache and the server also returned terminal requests from `findCurrentByFormalizationId`, so the UI continued to show `Envio cancelado` and treated the historical request as active. | `EV-117`; `EV-118`; `EV-119`; RF-36–RF-38; CA-21–CA-22 | `resolved` | Terminal request statuses are excluded from the active-request lookup, while configuration mutations refresh the sending review/status queries. The regression now covers cancelled request → reset → refreshed editable send state. |
| `FND-024` | implementation/integration | A collaborator returning through `/login?returnTo=/assinaturas/acesso` could be redirected to `/home` whenever any HMS session was already present, preventing the Gateway from completing the collaborator session flow. | `EV-120`; `EV-121`; RF-01–RF-08; RF-26–RF-27; CA-01–CA-08; CA-14–CA-16 | `resolved` | The login middleware now permits the explicit Signing Gateway return flow, preserves the safe return target through sign-in, and retains the normal redirect for ordinary login. |
| `FND-025` | implementation/integration | Client and collaborator invitation exchanges persisted active flow sessions and valid browser cookies, but CORS did not expose the synchronizer CSRF response header. The next client OTP or collaborator-session mutation therefore omitted CSRF; one shared `required` message misreported it as a missing Gateway session. A raw collaborator login anchor also risked discarding the required in-memory token. | `EV-122`–`EV-129`; `EV-144`; `MV-02`; `MV-03`; `CA-01`–`CA-08`; `CA-14`–`CA-16` | `resolved` | Exact credentialed CORS exposure, distinct transport diagnostics, Gateway-wide credential defaults and SPA login navigation are implemented. Real collaborator establishment returned `200 reading`; real client OTP verification now returns `200 reading`. Previously consumed invitations remain intentionally non-replayable. |
| `FND-026` | implementation/architecture | A prior cancelled-state action-hook test imports REST context and React Query directly from a widget test, violating both Web dependency rules. | `EV-130`; `EV-131`; `EV-133`; Web architecture exit | `resolved` | Moved the unchanged action-hook behavior test to `apps/web/src/ui/formalization/hooks/tests`; focused test, architecture, Biome and TypeScript pass without production changes. |
| `FND-027` | implementation/integration | The real client flow reaches a valid `choose_channel` context but OTP issue fails closed because `RequestSignatureOtpUseCase` rereads Identity consent using the Gateway recipient row ID instead of the recipient's Identity person ID. The same OTP request/verify lifecycle also treats that recipient ID as the consumed invitation ID even though guards, challenges and rate reservations are invitation-bound foreign keys. After correcting those identifiers, real replay exposed that no path creates the first OTP guard and the locked transaction rejects a missing guard unconditionally. The security-generic error made these failures appear equivalent to the earlier CSRF/session failure. | `EV-134`–`EV-136`; `EV-144`; `MV-02`; `CA-02`–`CA-03` | `resolved` | OTP issue resolves the recipient's Identity person and consumed invitation, initializes the first guard atomically, and real issue/delivery/verification completes through `reading`. |
| `FND-028` | implementation/integration | The OTP e-mail is delivered and its send attempt becomes `delivered`, but the delivery-outcome use case looks up the current challenge by passing `attempt.challengeId` to an invitation-ID repository method. The challenge therefore remains `pending_delivery` and verification must reject it. | `EV-139`–`EV-144`; `MV-02`; `CA-02`–`CA-03` | `resolved` | Exact challenge-ID lookup is implemented at Core and Drizzle boundaries; the real Inngest outcome run completed and moved the challenge to `active` before verification consumed it. |
| `FND-029` | implementation/database | OTP resend counts reservations with `reserved_at > rolling_window_started_at`, excluding the first reservation because first-use guard initialization deliberately gives both timestamps the same instant. The guard records one send while the repository returns zero, so the locked resend CAS fails closed and surfaces as the generic unavailable state. | `EV-142`; `EV-144`; `MV-02`; `CA-02`–`CA-03` | `resolved` | Invitation and source-IP reservations are counted inclusively from the boundary; real resend returned `200` and preserved guard/send consistency. |
| `FND-030` | implementation/concurrency | The valid-code verification branch hardcodes OTP guard CAS version `1`; first issue initializes version 1 and subsequent issue updates make the current version greater than 1, so otherwise valid delivered OTPs conflict and surface as unavailable. | `EV-143`; `EV-144`; `MV-02`; `CA-02`–`CA-03` | `resolved` | Successful verification now requires and uses the current guard version; a real non-initial guard verified and rotated the flow session to one active authenticated session. |
| `FND-031` | implementation/UI lifecycle | The page initialization effect was coupled to the mutable HMS access token through `resolveCollaboratorContext` and retained the first resolved Gateway context Promise. Supabase token refresh when a hidden tab became visible changed the dependency, reran initialization and reapplied the stale initial invitation context over client OTP or collaborator reading state. | User screenshots; `EV-146`; `VIS-24`; `MV-02`–`MV-03`; `CA-03`–`CA-05`, `CA-15` | `resolved` | Bootstrap context is applied at most once and collaborator establishment is isolated in its own state-driven effect. Client OTP input and collaborator reading survive auth-token refresh, visibility change and rerender; focused tests and Web gates pass. |
| `FND-032` | implementation/integration | Exact-lawyer authentication establishes an authenticated Gateway session and returns `reading`, but private document metadata/content fail because `GetSignatureDocumentUseCase` passes the signature request ID where `FormalizationSignatureSourceReader.findDocumentVersion` requires the owning Formalization ID. The context response also exposes the source document UUID as its title and hardcodes request version `1` instead of reading the immutable source/request records. | `EV-147`–`EV-150`; `MV-03`; `CA-04`, `CA-06`–`CA-07`, `CA-15` | `resolved` | The use cases now resolve the request, use its `formalizationId`, return immutable source title and actual request version, and the real exact-lawyer metadata/PDF endpoints return `200`. The remaining false reader error is isolated as FND-033. |
| `FND-033` | implementation/integration | The authenticated document metadata and PDF stream both return `200`, but Web validates the metadata with the unrelated strict context-summary schema. The valid `{ privateFileId, title, mediaType, byteCount, sha256 }` response is converted into an in-memory 422, so the reader reports a storage failure despite successfully downloading the PDF. Core's REST service interface also incorrectly types `getDocument()` as the context-summary shape, encouraging unsafe coercion. | `EV-151`–`EV-155`, `EV-158`; `MV-03`; `CA-04`, `CA-06`–`CA-07`, `CA-15` | `resolved` | Added the exact strict authenticated metadata response schema and Core service type, consumed them in Web without casts, and proved the real reader renders the PDF without the false error at desktop and narrow viewports. |
| `FND-034` | implementation/UI state machine | The private-reading widget's “Li o documento” action is wired to the no-op callback created by `contextToState`; no page transition carries the authenticated document and request version into acknowledgement, so the lawyer cannot reach “Abrir assinatura” even after the PDF loads. | `EV-154`, `EV-156`, `EV-158`; `MV-03`; `CA-06`–`CA-08`, `CA-15` | `resolved` | The page hook now owns reading→acknowledgement with the immutable document/current request version; the real lawyer flow reaches acknowledgement and provider entry. |
| `FND-035` | implementation/local integration | The Spec requires local same-origin parity by forwarding `/assinaturas/provedor/**` from Vite to Nest before the SPA fallback, but the current Vite config has no proxy rule. The safe relative provider path therefore cannot reach the Server proxy during local lawyer/client flows. | `EV-157`–`EV-158`; `G-05`; `MV-03`; `CA-08`, `CA-14`–`CA-15` | `resolved` | Added the Nitro dev prefix proxy to the configured Nest origin. A nonexistent alias now returns Nest's generic Gateway 404 while `/assinaturas/acesso` remains on Web; the real provider alias reaches the Server proxy without exposing the credential. |
| `FND-036` | implementation/provider lifecycle | Provisioning neither carries the configured per-recipient signature fields into Documenso nor calls the existing provider distribution operation. Documenso keeps the envelope `DRAFT`, its recipients `NOT_SENT`; distribution rejects the fieldless signers, and `/sign/{token}` returns 404, so every otherwise-valid lawyer or client fails at the final provider transition. | `EV-158`–`EV-161`; `RF-32`; `MV-03`; `CA-07`–`CA-08`, `CA-19` | `resolved` | Provisioning now recovers the exact locked configuration/version, converts each recipient's assigned fields and distributes before persistence with provider-native e-mail disabled. Core/Server gates pass, and targeted local provider proof returns an actionable signer state with zero provider e-mails; remaining interactivity is isolated as FND-037. |
| `FND-037` | implementation/provider proxy | The content-aware proxy originally left Documenso root assets at Web and replaced its nonce CSP. Its first correction still mapped real browser assets beneath the signer route, discarded query strings and let the provider's client router see an unknown HMS pathname. Nitro's public-asset middleware also intercepts extension-bearing requests before `devProxy`, while Documenso's Vite preload helper reconstructs lazy assets from the root. | `EV-161`–`EV-164`; `RF-13`; `MV-03`, `MV-05`; `CA-07`, `CA-09`, `CA-14`–`CA-15` | `resolved` | The public alias now carries a virtual `/sign/{alias}` route under an alias-scoped React Router base; bidirectional rewriting covers the bootstrap base/env, nonce CSP, root resources, Vite lazy-preload base, path/query alias translation and PDF API. Local parity combines Nitro and Vite prefix proxies so both extensionless routes and fingerprinted assets reach Nest. Real narrow/desktop flows render the PDF with no HTTP errors/provider-origin leak, and opening the assigned field displays the signature dialog. |
| `FND-038` | implementation/concurrency/configuration | Provisioning compares the request's locked pre-confirmation configuration version with the current Formalization-backed configuration projection after confirmation has already incremented that aggregate version. Every newly confirmed request therefore fails before provider creation; cancelled historical requests can also continue into provisioning because the use case does not reject their terminal request state. | `EV-166`; `EV-171`–`EV-173`; `RF-32`, `RF-36`–`RF-37`; `CA-19`, `CA-21` | `resolved` | Provisioning now requires the actual current nonterminal request and current configuration presence instead of comparing against the mutable Formalization aggregate version. Canonical terminal states, stale/cancelled requests, first-document and post-aggregate-advance second-document cases are covered; both real envelopes completed. |
| `FND-039` | implementation/provider lifecycle | HMS can retain a provider resource after the local Documenso database is recreated. API V2 then returns `NOT_FOUND`, but cancellation maps every non-2xx response to provider downtime and retries a cancellation that has no remaining provider-side access to revoke. | `EV-167`; `EV-172`; `EV-174`; `RF-36`–`RF-37`; `CA-21` | `resolved` | Cancellation treats an authoritative envelope lookup 404 as `already_terminal` without reading the provider body; non-404 failures remain unavailable/retriable. The real stale attempt completed once. |
| `FND-040` | implementation/messaging | The provisioning job applies its five-retry policy to known deterministic `AppError` failures, producing six identical Inngest step errors that cannot succeed without a new event or domain-state change. | `EV-168`–`EV-170`; `EV-172`; Messaging Rule Pack; `RF-32`; `CA-19` | `resolved` | The job translates only `FormalizationSignatureProvisioningFailedError` to Inngest `NonRetriableError`; all unexpected failures remain retriable. The real Dockerized job test passed 3/3 without repository mocks. |
| `FND-041` | implementation/security/integration | A page refresh preserves the valid HttpOnly Gateway session/device cookies but necessarily clears the Web-only synchronizer token. Although revision 5 explicitly requires `GET /context` to rotate and return a fresh session-bound CSRF value, the Core context use case did not rotate it and the controller emitted no CSRF response header. The next OTP or collaborator-session mutation therefore omitted `X-HMS-Signing-CSRF` and failed with the visible diagnostic shown by the user. | User screenshot; `EV-175`–`EV-181`; `RF-25`; `CA-01`, `CA-04`, `CA-14`; `MV-02`–`MV-03` | `resolved` | Context bootstrap now validates session/device, rotates the persisted CSRF hash optimistically and returns the raw value only through the exposed header. Web replaces its in-memory value and EV-181 proves reload through the next protected mutation. |
| `FND-042` | implementation/security/REST integration | After OTP verification and collaborator authentication, the controller obtained a newly rotated context but returned Core's internal `{ context, csrfToken }` envelope as JSON while leaving the preceding session token in the response header. Web correctly rejected the extra fields, and the raw synchronizer token crossed the body boundary. | `EV-180`–`EV-182`; `RF-25`; `CA-01`, `CA-04`, `CA-14`; REST/Validation Rule Packs | `resolved` | Both authenticated transition handlers now set the header from the final context result and return only `result.context`; exact controller tests cover OTP and collaborator responses, and the rebuilt runtime returns strict context bodies. |
| `FND-043` | implementation/provider localization | The pinned Documenso completion/claim-account UI renders in English for Brazilian signers. v2.17.0 ships `pt-BR` translations, but its `Accept-Language` parser reduces `pt-BR` to unsupported `pt` and falls back to English. HMS intentionally strips browser cookies at the provider boundary, so the provider cannot retain its own language cookie through the proxy. | User screenshot; upstream v2.17.0 locale source and issue 3090; `EV-183`–`EV-184`; `RF-13`; `CA-07`, `CA-14` | `resolved` | Every proxied provider request now receives the narrowly synthesized Documenso `lang=pt-BR` preference. Incoming browser cookies and authorization remain blocked, with focused assertions covering HMS flow/device credential isolation. A fresh live provider capture remains part of the broader F7 browser evidence rather than a source-correction blocker. |
| `FND-044` | implementation/provider configuration | The provider completion page promotes creating a Documenso account and requires a password even though HMS recipients need no provider account and RF-13 excludes provider account paths from the signing surface. | User screenshot; Documenso v2.17.0 completion-route/auth source; `EV-185`; `RF-13`; `CA-07` | `resolved` | Documenso email/password signup is disabled through its supported environment switch, which makes `canSignUp` false and removes the complete claim-account panel. Recipient signing and existing administrator sign-in remain enabled; the local provider was recreated healthy without replacing its database or volumes. |
| `FND-045` | implementation/provider UI | Documenso's completed-recipient page still exposes a signing-card Share action. This promotional action is outside the HMS signing workflow and can create a provider sharing route that RF-13 excludes from the proxied recipient surface. | User screenshot; Documenso v2.17.0 completion-route/share-component source; `EV-186`–`EV-188`; `RF-13`; `CA-07`, `CA-14` | `resolved` | The HMS proxy injects a nonce-authorized, completion-only style that suppresses the signing-card Share button after hydration without weakening CSP or altering signing/download markup. It fails safely when a valid provider style nonce or complete HTML head is unavailable. Fresh live visual confirmation is queued for the current user's reload/new completed flow. |
| `FND-046` | implementation/cross-layer contract | The revision-5 implementation created a separate provider lifecycle per request document, while revision 6 requires one request envelope and package recipient lifecycle. | Direct 2026-09-03 decision; Spec revision 6; `EV-189`–`EV-191`; `EV-228`; `EV-229`; RF-06–RF-25, RF-31–RF-38 | `resolved` | F10–F13 replace the superseded cardinality with one shared envelope, immutable assignments, independent acknowledgements and one provider ceremony. Remaining fresh integrated/visual evidence is separately waived under `EV-235`. |
| `FND-047` | implementation/Core contract and tests | The first F10 candidate compiles but did not provide fresh integrated proof for all revision-6 package behaviors. | `EV-193`–`EV-194`; revision-12 waiver `EV-235` | `accepted_risk` | Fresh integrated closure evidence deferred by explicit user decision; no technical-resolution claim. |
| `FND-048` | specification/Core/Database atomicity | Revision 6 described a shared multi-recipient envelope but exposed a singular provider-observation transaction. The first revision-7 draft coupled one receipt to every recipient, allowed independently timed recipient reads to supply conflicting envelope states and proved only the Core transaction call count; its first correction then assigned receipt rollback to the receipt-free scheduled reconciliation flow. These defects could fabricate receipts, suppress newer provider state, partially advance the request graph or create an unexecutable test contract. | Spec revision 6 and revision-7 transaction/provider/validation revisions; direct multi-document-package decision; `EV-195`–`EV-198`; RF-16, RF-18–RF-19; CA-11–CA-12 | `resolved` | Revision 7 uses one coherent `findEnvelopeState` response, independent `receiptUpdates: 0..n` with mixed-duplicate semantics and a batch-only transaction result. Real-PostgreSQL receipt rollback/replay belongs to the webhook job; two-recipient/two-item recipient/document/request/Formalization rollback with no receipt mutation belongs to scheduled reconciliation. The same Reviewer passed the final split. |
| `FND-049` | specification/Core/Database webhook concurrency | Fresh integrated proof for webhook claim ownership, retries and stale-attempt isolation remains absent. | `EV-201`; `EV-202`; revision-12 waiver `EV-235` | `accepted_risk` | Fresh integrated closure evidence deferred by explicit user decision; no technical-resolution claim. |
| `FND-050` | implementation/Core lifecycle and tests | Fresh integrated proof for all revised lifecycle, provider-observation and confirmation invariants remains absent. | `EV-201`; `EV-206`; revision-12 waiver `EV-235` | `accepted_risk` | Fresh integrated closure evidence deferred by explicit user decision; no technical-resolution claim. |
| `FND-051` | specification/Core/Server webhook completion | Revision 8 requires unknown valid provider events to trigger request-level reconciliation without speculative state, but exposes no exact-token transaction that can complete only the owned receipt. The first revision-9 draft also retained raw provider metadata in Core/persistence, did not declare the Server normalizer boundary and lacked complete cross-layer test ownership. | `EV-206`; `EV-207`; `EV-208`; `EV-209`; RF-16–RF-18; CA-10–CA-12 | `resolved` | Revision 9 adds `completeWebhookReceiptClaim`; replaces Core/persistence receipt metadata with neutral `hintKind`/`encryptedHint`; declares the exact Server `DocumensoWebhookNormalizer` path, operation and composition; and assigns raw-to-neutral plus complete reconciliation-only behavior to focused controller/normalizer and real-PostgreSQL job tests. The same Reviewer passed the corrected contract with no finding. |
| `FND-052` | implementation/Core access, result and reconciliation safety | Corrections exist, but fresh integrated proof for all submission, provider-entry, acknowledgement and aggregate-observation guarantees remains absent. | `EV-212`–`EV-226`; revision-12 waiver `EV-235` | `accepted_risk` | Fresh integrated closure evidence and reviewer recheck deferred by explicit user decision; no technical-resolution claim. |
| `FND-053` | environment/browser integration | Documenso was configured with `NEXT_PUBLIC_WEBAPP_URL=http://127.0.0.1:3004` while the local browser accessed `http://localhost:3004`. The sign-in page therefore sent its session request cross-origin to `127.0.0.1`; the browser rejected it because the response did not allow the `localhost` origin, producing the generic sign-in error before credentials were evaluated. | `EV-230`; user screenshot; `RF-13`; local Compose configuration | `resolved` | Local public-origin defaults now use `http://localhost:3004` in Compose, `.env.example` and the active `.env`; the Documenso container was recreated without replacing its database/volumes. The session endpoint returns `200` from the browser origin with no CORS or console error. Deployment environments must continue to set their own canonical public origin explicitly. |
| `FND-054` | implementation/UI state machine | After the internal cancel endpoint returned `202 scheduled`, the sending controller refetched the still-active request before the asynchronous cancellation job completed. The page therefore continued to render “Envio de assinaturas em andamento” and kept reset disabled until a full reload observed the later terminal projection. The first polling correction then failed to clear when the review endpoint correctly omitted its terminal current request while a stale status response still said `sending`. | User screenshot/report; `EV-231`; RF-36–RF-37; CA-21 | `resolved` | After a successful cancellation command, the page enters an explicit cancellation-pending state and polls authoritative review/status responses until terminal. A loaded review with no current request now also ends the pending state, preventing indefinite updates when terminal requests are intentionally omitted from the current-request projection. Reset remains unavailable during the pending window and becomes available after reconciliation. |
| `FND-055` | implementation/Core and UI state machine | When one recipient submits first, the request correctly advances to `partially_submitted`. The remaining recipient can retrieve the private PDF because document access admits that request state, but acknowledgement rejects every state except `in_progress`. The Web page then passes the acknowledgement failure through the PDF viewer's `error` prop, replacing already loaded content with “Não foi possível carregar o documento privado.” | User screenshots; live request `efd006a3-63cd-4b06-8495-8ce0da996219`; `EV-233`; RF-08–RF-11; CA-05, CA-09, CA-18; MV-02 | `resolved` | Acknowledgement and provider start now accept `in_progress` and `partially_submitted`; document-load and action errors are independent, and focused plus real-browser regressions preserve the loaded viewer. |
| `FND-056` | implementation/OTP recovery | Reopening a consumed but recoverable client invitation creates a new flow, but OTP resend derives the next generation from `findCurrentByInvitationId`. A previously consumed challenge is intentionally not current, so the lookup returns null, generation restarts at `1`, and PostgreSQL rejects the duplicate invitation/generation pair with a 500 before any delivery attempt is created. | Exact local exchange/context/OTP replay; live PostgreSQL challenge/guard rows; `EV-233`; RF-01–RF-03; CA-02–CA-03; MV-02 | `resolved` | OTP issue reads the latest historical challenge for monotonic generation and the current challenge only for supersession. Consumed-history and real replay pass. |
| `FND-057` | implementation/OTP rate-window rollover | After the 30-minute rolling window expires, the reservation query correctly returns zero current-window sends, but OTP issue uses that zero as the optimistic expectation for a guard that still stores the previous window's send count. The transaction rejects the valid rollover before inserting the new challenge. | Exact local replay on the rebuilt port-5555 server; live PostgreSQL guard state; `EV-233`; RF-01–RF-03; CA-02–CA-03; MV-02 | `resolved` | Rate policy and new guard state use the current-window count, while the transaction compares against the persisted pre-rollover guard count. Focused rollover and real replay pass. |

## Lessons learned

| Lesson | Source finding | Authority disposition |
| --- | --- | --- |
| Authenticated-browser evidence requires healthy Compose dependencies and a booted real Nest/Web stack before feature validation. | `FND-001` | No authority change; already explicit in `AGENTS.md` and `implement-spec`. |
| A persisted server-side Gateway session is not browser-session evidence; validation must prove the credentialed exchange response stores cookies and that the next request resends them for every actor branch. | `FND-025` | No authority change; the Spec's cookie-bound session and MV-02/MV-03 contracts already require this end-to-end proof. |
| A reverse-proxied provider SPA must receive a browser-visible route compatible with its client-router base, and its bootstrap, lazy chunks, API/PDF paths and CSP must be validated as one browser flow. Rewriting only the initial HTML or server requests leaves extension-bearing assets and client navigation outside the proxy boundary. | `FND-037`; `EV-161`–`EV-164` | No authority change; RF-13 and MV-03/MV-05 already require an opaque same-origin provider experience. Local parity requires both Nitro routing and Vite prefix proxying because their middleware handles extensionless routes and static-looking assets differently. |
| A request's frozen configuration version cannot be compared with a mutable aggregate version after asynchronous per-document transitions. Provisioning must bind to the current nonterminal request and immutable request/document/recipient snapshots while allowing aggregate version advances between sibling documents. | `FND-038`; `EV-173` | No authority change; RF-32 and CA-19 already require multi-document provisioning and stale-work rejection. |
| Inngest retries are for failures that may recover; deterministic application-state failures must be translated to `NonRetriableError` at the job boundary and proven through the real Inngest fixture. | `FND-040`; `EV-172` | No authority change; already explicit in the Messaging and Job Testing Rule Packs. |
| An in-memory synchronizer CSRF design requires every cookie-authenticated bootstrap endpoint to rotate and return a fresh token; preserving only HttpOnly session cookies across refresh otherwise creates a valid but unusable session. | `FND-041`; `EV-175` | No authority change; revision-5 `GET /context` and Cookies/CSRF sections already require this recovery behavior. |
| When a Core use case returns transport metadata alongside a public DTO, every REST composition path must explicitly project the DTO and carry the metadata through headers; returning the internal envelope leaks implementation data and breaks strict consumers. | `FND-042`; `EV-180` | No authority change; the Spec already forbids CSRF values in public bodies and Validation already rejects unknown keys. |
| A reverse proxy that intentionally strips browser cookies may still need to synthesize a narrowly allow-listed provider preference cookie; forwarding the browser cookie jar to preserve localization would violate the security boundary. | `FND-043`; `EV-183` | No authority change; RF-13 already requires unsafe-header stripping and complete provider UI functionality under the HMS origin. |
| A missing implementation tree is baseline scope, not permission to weaken the revision-5 contract. | `FND-002` | No authority change; the Spec and Plan remain authoritative. |
| The Core declaration surface is not sufficient evidence for a lifecycle contract; every use case and concurrency/security rule needs behavior-focused tests before dependent boundaries activate. | `FND-003` | No authority change; applied in Core tests and satisfied at F1 exit. |
| A security-sensitive Core contract needs an explicit secret/randomness verifier port; placeholder bytes and plaintext fallbacks cannot be accepted as test scaffolding. | `FND-004` | No authority change; applied in Core ports/use cases and satisfied at F1 exit. |
| Current-candidate sensor results supersede earlier Builder-reported passes; test setup and dependency contracts must be verified on the integrated worktree. | `FND-005` | No authority change; already required by `implement-spec` living evidence rules. |
| Security placeholders that cross a Core boundary are implementation blockers even when types and unit counts pass; explicit ports and non-empty contract assertions are required. | `FND-006` | No authority change; already required by the Spec threat model and Core Rule Pack. |
| Interrupted ownership work must be completed or reverted by a replacement within the same boundary before validation; partial required dependencies cannot be accepted as a candidate. | `FND-007` | No authority change; follows the implement-spec correction and Builder ownership gates. |
| A Core interface change requires immediate consumer and fixture updates in the owning application boundary; package-local success is insufficient. | `FND-008` | No authority change; current Server consumer compilation is the required follow-up sensor. |
| A broad integration suite with database bootstrap noise must be decomposed before it can support a phase exit; failed scenarios remain findings until isolated evidence exists. | `FND-009` | No authority change; F3 exit remains focused real-PostgreSQL persistence and transaction evidence. |
| Optimistic concurrency must keep the request version and aggregate projection version separate; an event consumer must resolve cancellation attempts by attempt ID, not by the related document ID. | `FND-021`; `EV-105`; `EV-106`; `EV-108` | No authority change; the Spec's versioned transaction and cancellation-attempt contracts remain authoritative. |
| Historical signing recipients must keep their referenced signatory identity after cancellation; reset must reuse or retain those rows instead of deleting through a restrictive FK. | `FND-022`; `EV-113`; `EV-114` | No authority change; RF-36–RF-38 and the existing restrictive history FK require preserved evidence while allowing the current configuration to be reset. |
| F3 persistence tests depend on the canonical migration owned by F4; test-only schema duplication would invalidate the evidence. | `FND-010` | Execution ordering is adjusted so Orchestrator migration generation unblocks, rather than weakens, F3 verification. |
| Direct repository tests violate the repository's mandatory database rule; real HTTP/application flows are the valid evidence path for persistence behavior. | `FND-012` | Plan wording is being reconciled to route F3 behavior evidence through F5 integration without changing the revision-5 contract. |
| PostgreSQL identifier limits must be handled in source model names before migration generation; accepting automatic truncation can make a migration non-executable. | `FND-011` | No authority change; the migration remains generated/reviewed by the Orchestrator after the Server model correction. |
| REST endpoint coverage must be checked against the complete Core use-case inventory before activating controllers; missing status/lookup ports are upstream contract gaps, not controller responsibilities. | `FND-013` | No authority change; the Spec remains authoritative and the correction must preserve thin controllers. |
| Seed reset order must follow the request graph's restrictive foreign keys; aggregate cleanup belongs behind an existing database contract rather than ad hoc SQL in the central seed script. | `FND-016` | No authority change; the Database and Server App rules already require module-owned cleanup through contracts. |

## PR CI quality gate

<!-- Populate during conclude-spec. The head SHA identifies the PR revision checked by CI. -->

| ID | Workflow | Head SHA | Result | Run |
| --- | --- | --- | --- | --- |
| `CI-01` | applicable repository PR workflow | — | `pending` | — |

## History

| Date/Time | Event |
| --- | --- |
| `2026-09-03` | EV-210 reconciles Plan/Evaluation to revision 9; EV-211 records the same Core Builder's exact correction scope before source work resumes. Spec and Plan are `in_progress` at F10. |
| `2026-09-03` | EV-212 records the green 144-focused/505-full Core checkpoint. EV-213 independently rejects that false-green result on result-state, live access, dedupe, per-item terminal, artifact and provider-entry recovery defects; FND-052 opens before a bounded Core correction. |
| `2026-09-03` | EV-214 activates bounded `builder_fix_core` agent `01a069a6-ece6-7e22-a4f6-74bcbe710777` (`Lovelace`) for FND-052, with Server/PostgreSQL atomicity explicitly retained for F12. |
| `2026-09-03` | EV-214 produced no patch through bounded progress windows and was shut down. EV-215–EV-218 split FND-052 across four disjoint Core write sets before replacement assignments. |
| `2026-09-03` | EV-219 integrates the four disjoint corrections and passes 199 focused/541 full Core tests plus type, architecture, lint and whitespace gates. The same reviewer is rechecking every EV-213 finding before F10 acceptance. |
| `2026-09-03` | EV-220's same-reviewer recheck rejects F10 on unsafe submission persistence, unrecoverable committed provider-entry bindings, incomplete acknowledgement authority and hard-coded webhook aggregate state. Revision 10 returns to `draft`; implementation remains paused pending the mandatory Spec audit. |
| `2026-09-03` | EV-221 records revision-10 draft hash `717e2b38…` and the mandatory same Spec Reviewer audit assignment. No source correction resumes while the Contract is draft. |
| `2026-09-03` | EV-221 fails on hash drift plus R10-F02–R10-F07. EV-222 corrects refresh-safe CSRF, post-submit lost-response recovery, Validation parity, aggregate ownership, PostgreSQL test ownership and alias-CAS persistence, then freezes hash `f053e8d2…` for same-reviewer recheck. |
| `2026-09-03` | EV-222 resolves six findings but leaves R10-F03's durable duplicate proof and stale independent-result language. EV-223 adds the exact observation column/evidence, removes the bearer contradiction and freezes hash `d94fba83…` for final same-reviewer recheck. |
| `2026-09-03` | EV-223 passes the final revision-10 audit with no finding. EV-224 reconciles the Plan and resumes the existing F10 Core owner under the reviewed transaction/recovery contract. |
| `2026-09-03` | EV-209 records the clean same-reviewer revision-9 recheck at hash `1574b726…`; R9-F01–R9-F03 and FND-051 are resolved and the Spec returned to `open` before Plan reconciliation. |
| `2026-09-03` | EV-208 records three accepted high revision-9 findings. The correction removes raw provider receipt metadata from Core/persistence, declares the exact Server normalizer and adds complete cross-layer reconciliation-only evidence before same-reviewer recheck. |
| `2026-09-03` | EV-207/FND-051 open draft revision 9 for exact-token reconciliation-only receipt completion and provider-neutral hint classification. F10 source work is paused before mandatory review. |
| `2026-09-03` | EV-206 rejects the first revision-8 Core checkpoint despite 61 focused passes. Unknown-event mapping, parser fail-closed behavior, literal acknowledgement, signable-state guards, fresh confirmation-state guards and deep acknowledgement/start tests return to the same F10 owner. |
| `2026-09-03` | EV-204 reconciles Plan/Evaluation to revision 8; EV-205 records the same Core Builder's exact correction scope before source work resumes. Spec and Plan are `in_progress` at F10. |
| `2026-09-03` | EV-203 records the same Reviewer's clean final revision-8 recheck at hash `42ef5808…`; all R8 findings are resolved and the Spec returned to `open` before Plan reconciliation. |
| `2026-09-03` | EV-202 records the first revision-8 Reviewer failure. The corrected draft moves receipt claims from the repository into the focused transaction, replaces reusable worker ownership with a unique per-attempt claim token, defines exact attempt accounting and maps `retry_required` to five retriable Inngest retries with independent request-level recovery. F10 remains paused pending same-reviewer recheck. |
| `2026-09-03` | EV-201 recorded a false-green revision-7 Core checkpoint. FND-049 opened a narrow revision-8 receipt identity/lease Contract amendment; FND-050 records remaining in-Contract lifecycle corrections. F10 is paused before review. |
| `2026-09-03` | Recorded revision-7 F10 reassignment EV-200 before resuming the same Core Builder; Server/database rollback remains explicitly outside its write scope. |
| `2026-09-03` | Reconciled Plan/Evaluation to revision 7 as EV-199 and returned the Spec/Plan to `in_progress` before resuming the same F10 Core Builder. |
| `2026-09-03` | Final same-reviewer recheck passed revision 7 with no finding. EV-198 resolves FND-048 and the Spec returned to `open`; Plan reconciliation precedes resuming the same F10 Core Builder. |
| `2026-09-03` | Focused recheck resolved R7-F01/R7-F02 and narrowed R7-F03 to test ownership. Receipt-bearing rollback/replay moved to the webhook job test; receipt-free multi-recipient rollback remains in the reconciliation job test. A final same-reviewer recheck is pending. |
| `2026-09-03` | The same Spec Reviewer returned R7-F01 blocking and R7-F02/R7-F03 high. All were verified and accepted: revision 7 now decouples receipt cardinality, requires one coherent envelope-scoped provider read and assigns real-PostgreSQL late-conflict rollback proof to the existing reconciliation job test; same-reviewer recheck is pending. |
| `2026-09-03` | Paused F10 after proving revision 6's singular provider-observation transaction cannot atomically reconcile a multi-recipient envelope. Revision 7 is draft, EV-193 is stale, EV-195/FND-048 record the contract amendment, and no feature source work resumes before the mandatory same-reviewer audit and Plan reconciliation. |
| `2026-09-03` | Activated F10 `builder_core` `01a068d8-cddd-7530-95e2-c806a27fd737` (`Volta`) under EV-192 with a read-only hold; recorded revision, sources, RF/CA, paths, Rules and exits before authorizing source edits. |
| `2026-09-03` | Spec revision 6 opened after the direct multi-document-package decision and mandatory same-reviewer audit/rechecks passed. EV-189–EV-191 preserve the revision-5 baseline, record FND-046 and reconcile Plan F10–F13 before any revision-6 source edit. |
| `2026-09-01 18:40` | Evaluation created for Spec revision `5`; preflight health passed, baseline recorded, and F1 entered `in_progress`. |
| `2026-09-01 18:41` | Activated `builder_core` agent `01a05eec-6278-75a0-88d2-993d7d4375a3` for F1-T1 before feature-source edits. |
| `2026-09-01 19:00` | `builder_core` reported declarations and baseline sensors passing, but F1 remained incomplete; `FND-003` opened and the same Builder was resumed. |
| `2026-09-01 19:25` | `builder_core` reported all 202 Core paths and 23 use-case tests present, but contract-depth gaps remained; `EV-11` recorded as failed and F1 stayed in progress. |
| `2026-09-01 19:45` | Resumed `builder_core` produced no further changes; `EV-12` failed and `FND-004` recorded the concrete security/behavior blocker. |
| `2026-09-01 19:15` | Current-candidate Core sensors invalidated the earlier pass: type-check failed and 3 OTP tests failed; `EV-13` and `FND-005` recorded. |
| `2026-09-01 19:20` | Activated independent `builder_fix_core` agent `01a05f0b-c869-7d40-8370-e4825ed92d63` for FND-005 after the original Builder could not progress. |
| `2026-09-01 19:35` | `builder_fix_core` resolved FND-005; `EV-15` passed current Core sensors, while FND-003/FND-004 remain active. |
| `2026-09-01 19:50` | Current Core inspection found raw hash fallbacks and empty provider/proxy values; `EV-16` failed and `FND-006` opened. |
| `2026-09-01 19:51` | Activated fresh `builder_fix_core` agent `01a05f10-2946-79d0-81bd-92a701f82c05` for FND-006. |
| `2026-09-01 20:15` | FND-006 correction made no changes; `EV-18` failed, the correction Builder was stopped, and F1 remained blocked. |
| `2026-09-01 20:20` | Activated fresh `builder_fix_core` agent `01a05f1c-f098-7ff3-a57e-dfd3a5cc48c7` to resume F1 security/contract correction. |
| `2026-09-01 20:00` | Current integrated Core candidate passed type-check and 122-file/326-test suite with no prohibited hash/placeholder patterns; `EV-25` passed and FND-006 resolved. |
| `2026-09-01 20:01` | Activated fresh `builder_fix_core` agent `01a05f34-67e4-7ed2-8145-8a439c465c2d` for lifecycle behavior evidence correction. |
| `2026-09-01 20:10` | OTP behavior tests and session-expiry guard landed; `EV-27` recorded the remaining smoke-level lifecycle test gap and F1 stayed in progress. |
| `2026-09-01 20:20` | Orchestrator reran the integrated Core exit; `EV-28` passed and FND-003/FND-004 resolved. F2 Validation and F3 Server foundation became dependency-ready. |
| `2026-09-01 20:21` | Activated parallel F2 `builder_validation` agent `01a05f45-8a3c-7d60-b5b3-63af766fb2ee` and F3 `builder_server` agent `01a05f45-80c0-7220-9f7d-0f0b8e8529e6`. |
| `2026-09-01 20:40` | F2 Validation completed with package sensors passing; `EV-31` passed and F3 remained in progress. |
| `2026-09-01 21:30` | Orchestrator ran the corrected Server sensors: code and architecture passed, Server type-check failed on the source-reader/fixture Core contract, and the broad Server suite reported 10 Formalization controller failures before interruption; `EV-32`, `FND-008` and `FND-009` recorded. |
| `2026-09-01 21:45` | First `builder_fix_server` correction timed out twice without a handoff; `EV-33` failed and a replacement same-boundary Builder was required. |
| `2026-09-01 22:00` | Replacement `builder_fix_server` also timed out twice without a handoff; `EV-34` failed and the correction was narrowed to the two consumer methods plus fixture update. |
| `2026-09-01 22:20` | Current Server sensors passed in `EV-36`; focused F3 persistence testing was then blocked because canonical migrations are absent and test-only DDL is prohibited; `EV-37`, `EV-38` and `FND-010` recorded. |
| `2026-09-01 22:35` | F4 migration generation completed, but forward apply failed on PostgreSQL identifier truncation/collision risk; `EV-39` and `FND-011` recorded and the source-model correction was routed to `builder_fix_server`. |
| `2026-09-01 22:50` | `builder_fix_server` stopped without correcting the model names; `EV-40` failed and the correction was rerouted with an explicit model-file checklist. |
| `2026-09-01 23:20` | F5 `builder_server` activation `01a05f91-9e1c-7752-a2b6-bc6e01512159` completed its preflight but found missing Core status/lookup contracts and no Server metadata-reader binding; `EV-47` passed activation, `EV-48` failed implementation readiness, and `FND-013` opened. |
| `2026-09-01 23:35` | `builder_fix_core` activation `01a05f97-c406-7c62-99bb-d3befff1a762` produced no handoff and was stopped; `EV-49` failed. A narrower Core replacement was activated as `EV-50`. |
| `2026-09-01 23:55` | Core activation `01a05f9c-117a-7162-81aa-c574d14473d5` also produced no handoff and was stopped; `EV-50` failed. The correction was reduced to one use case, one test and one barrel as `EV-51`. |
| `2026-09-02 00:15` | Core activation `01a05f9f-e85e-7072-95eb-ad3b0224b52f` produced no new use-case file and was stopped; `EV-51` failed. A final one-file Builder activation was opened as `EV-52`. |
| `2026-09-02 00:35` | Three Core Builder activations produced no handoff; the Orchestrator applied the minimal status use case/test fallback. `EV-52` failed as an activation, `EV-53` passed focused Core validation, and FND-013 remains active only for Server metadata/composition. |
| `2026-09-02 01:00` | Server activation `01a05fa5-1ef7-7630-bc10-6f0bd35c57f2` produced no handoff or feature changes and was stopped; `EV-54` failed. F5 remains incomplete at the Server composition boundary. |
| `2026-09-02 01:20` | Orchestrator fallback added the metadata reader and status route/composition after `EV-54` failed; Server sensors and focused Nest/Supertest status-route coverage passed in `EV-55`. Review/confirm/cancel remain open. |
| `2026-09-02 02:00` | Orchestrator fallback completed review/confirm/cancel composition and routes, HMAC secret hashing and current focused Server checks; `EV-56` passed. F5 remains open for provider/delivery and real lifecycle integration. |
| `2026-09-02 02:20` | Final integrated Core/Server sensors passed in `EV-57`; the Spec remains `in_progress` because provider/delivery, public Gateway/Web, full persistence-flow and release-gate evidence are not implemented. |
| `2026-09-02 03:00` | F5/F6 Builder review confirmed no changes: Server provider/delivery/Gateway remains absent and Web is blocked by the missing Core REST service methods; `EV-60` failed. Core correction activation `EV-61` opened. |
| `2026-09-02 03:30` | Core activation `01a061e1-9e8b-74b1-b95f-9436eb02e58a` produced no handoff; `EV-61` was resolved through a minimal Orchestrator fallback as `EV-62`. Web reactivation `EV-63` opened against the now-complete service contract. |
| `2026-09-02 04:00` | Web reactivation `01a061e4-2a9f-7c33-a066-a385896d7329` also produced no handoff. The Orchestrator applied the bounded REST adapter and internal review/progress/cancel UI fallback; `EV-64` and `EV-65` passed. A new narrow Server provider activation `01a061ee-dde2-74c1-9f22-bbcc2e6bc1bd` opened as `EV-66`. |
| `2026-09-02 04:30` | Provider activation `01a061ee-dde2-74c1-9f22-bbcc2e6bc1bd` produced no handoff and was closed. The Orchestrator added the isolated public Gateway client/CSRF boundary and request-options support; `EV-67` passed all current package sensors and focused client tests. |
| `2026-09-02 05:00` | Full Web regression after the public client/context integration passed in `EV-68` (111 files, 407 tests). |
| `2026-09-02 05:15` | Final local boundary checks for this turn passed in `EV-69`: Validation types/architecture, focused Server provision/controller tests (7 tests), and `git diff --check`. |
| `2026-09-02 05:30` | Public Gateway channel validation was promoted from an adapter-local schema to the named Validation export; `EV-70` passed. |
| `2026-09-02 06:00` | Activated `builder_server` `01a06205-558d-7841-9735-ee8feddca263` and `builder_web` `01a06205-6c74-7d42-9e40-4b1a8cd863fc` for the missing server and public Gateway integration slices; `EV-71` and `EV-72` opened. |
| `2026-09-02 06:20` | The prior Server Builder delivered the dedicated internal sending composition; the Orchestrator integrated it into all four controllers, corrected stable conflict errors and passed current Server checks in `EV-73`. The Web Builder handed off no public route files (`EV-72`), and the required Nest runtime preflight failed at port `5555` (`EV-74`). |
| `2026-09-02 06:25` | Activated Server Builder `01a0620d-6d08-73c0-a3e7-ea061815d671` for the next bounded provider/jobs/public-Gateway slice as `EV-75`; Web remains queued behind its Server endpoints. |
| `2026-09-02 06:45` | The replacement Server Builder produced no handoff or verifiable changes after three progress windows and was shut down; `EV-76` records the failed activation. Same-boundary local fallback is now permitted for the provider/jobs/Gateway implementation. |
| `2026-09-02 09:40` | The fallback Server slice passed current sensors, booted Nest successfully and exposed the Gateway/webhook/proxy routes; `EV-77` records the result. Web Builder `01a06222-6370-70e3-8fca-a5e67abe71f5` was activated for the public route/UI as `EV-78`. |
| `2026-09-02 10:00` | Web Builder `01a06222-6370-70e3-8fca-a5e67abe71f5` produced no handoff and was shut down; the Orchestrator applied the bounded public route/UI fallback, regenerated `routeTree.gen.ts`, and recorded `EV-79`. |
| `2026-09-02 10:10` | The focused public Gateway Playwright test passed at 390×844 with keyboard focus evidence after fixing Strict-mode initialization re-entry; current Server/Web/Validation boundary sensors and `git diff --check` passed in `EV-80`. The broad Web integration run remains non-gating because existing client fixture scenarios failed before Gateway coverage. |
| `2026-09-02 10:20` | Reopened F7 with scoped `builder_web` `01a06257-a458-7611-aff7-0cabd10413c8` to replace the compact fallback with the complete Spec-required widget tree; `EV-81` records the assignment and F7 remains in progress. |
| `2026-09-02 10:50` | `builder_web` produced no handoff after three bounded progress windows and was shut down; `EV-82` records the failed activation. Same-boundary local fallback is now permitted for the exact widget-tree correction. |
| `2026-09-02 11:05` | The same-boundary Web fallback completed the literal Spec widget tree and Gateway-only REST client boundary. Web types/code/architecture passed, focused widget/service coverage passed (27 files, 29 tests), Nest health was `UP`, and the focused 390×844 keyboard Playwright flow passed; `EV-83` records the result. Full lifecycle, authenticated, visual and release gates remain open. |
| `2026-09-02 11:30` | Replaced all placeholder Gateway widget-hook tests with behavior-driven `renderHook` cases and corrected two production state-machine defects found by those tests. Focused widget coverage passed at 26 files/41 tests; full Web Vitest, type/code/architecture checks and `git diff --check` passed. `EV-84` records the current evidence; prior focused browser evidence remains valid because no route rendering contract changed. |
| `2026-09-02 11:35` | Recaptured the focused Chromium Gateway route after the page-hook fixes; the 390×844 fragment/keyboard scenario passed with Docker/Auth and Nest health confirmed. `EV-85` records the fresh browser evidence. |
| `2026-09-02 16:00` | Investigation of the failed Inngest provisioning runs found strict event-schema rejection because `.v1` signing event payloads omitted `version: 1`; all versioned signing event constructors were corrected and focused Core/Server sensors passed (`EV-86`). |
| `2026-09-02 16:10` | Live Inngest validation accepted a corrected provisioning event. Replaying the user’s pending request reached Documenso and exposed the immutable-snapshot DOCX/PDF mismatch; the metadata reader was corrected to use ready PDF previews and regression-tested (`EV-87`, `EV-88`, `FND-014`, `FND-015`). |
| `2026-09-02 16:30` | `db:seed` investigation exposed a restrictive Signing Gateway FK during Formalization cleanup. Cleanup was integrated into the existing gateway transaction contract, error logging was added to the seed entrypoint, and three consecutive reset/repopulate runs plus current package sensors passed (`EV-89`, `FND-016`). |
| `2026-09-02 17:52` | The provisioning failure was corrected to reconcile Documenso's delayed recipient population. The missing Communication consumers were then implemented with local Mailpit/production Resend routing, delivery outcome jobs, OTP activation and scheduled reconciliation; live Inngest and Mailpit evidence passed (`EV-91`, `EV-92`, `FND-018`). |
| `2026-09-02 19:08` | Added HMS-styled static HTML invitation/OTP templates, propagated the optional HTML body through Mailpit and Resend, renamed the delivery adapter to `FormalizationSignatureEmailDeliveryProvider`, and corrected runtime packaging after the first asset-placement/build sensor failed (`EV-94`–`EV-97`, `FND-019`). |
| `2026-09-02 19:20` | Investigated the invitation addressed to `lawyer@hmsadvogados.com.br`; database evidence confirmed a collaborator recipient while the Gateway was still entering the client e-mail channel state. Added collaborator-aware context resolution, exact recipient/session binding, safe login return routing and focused Core/Web/browser evidence (`EV-98`–`EV-99`, `FND-020`). |
| `2026-09-02` | Investigated the failed internal cancel-all action. The UI command carries the Formalization version, but cancellation conflates it with the request version, producing a deterministic CAS conflict after an active request exists. Opened `EV-100` and `FND-021`; the fix is routed to the Core Builder with a separate regression test. |
| `2026-09-02` | The scoped `builder_core` activation `01a06439-2adb-7e90-8747-55209c255aa5` produced no verifiable handoff after three bounded progress windows and was shut down. Same-boundary local fallback was authorized by the implementation workflow; `EV-101` records the failed activation before the correction. |
| `2026-09-02` | Read-only identity/database inspection showed the active request belongs to `lawyer@hmsadvogados.com.br`, while the local seed also exposes an administrator path that can read the Formalization. The cancellation service did not pass or honor collaborator profile, so the administrator path remained forbidden. Activated `builder_server` for this boundary as `EV-103`. |
| `2026-09-02` | The scoped `builder_server` activation `01a06442-7d48-7de0-93b9-2511e098af1e` produced no verifiable handoff after three bounded progress windows and was shut down. Same-boundary local fallback was authorized; `EV-104` records the failed activation before the Server correction. |
| `2026-09-02` | Local fallback completed the cancellation correction: separate request/Formalization optimistic versions, administrator authorization, attempt-ID processing, safe no-resource handling and Documenso `DRAFT` terminal handling. Core/Server focused sensors passed in `EV-105`. |
| `2026-09-02` | Authenticated local REST cancellation returned `200 scheduled`; the Formalization/request projections and both cancellation attempts converged to `cancelled`, and the two replayed Inngest jobs completed without errors. `EV-106` passed and `FND-021` was resolved. |
| `2026-09-02` | The internal Web widget regression suite passed 23/23 tests, proving the cancel action sends the current Formalization version, preserves the visible error state when the mutation fails and renders a successful cancellation as terminal. `EV-108` added UI evidence for the resolved cancellation finding. |
| `2026-09-02` | Investigated the cancelled-state reset failure. PostgreSQL rejected deletion of signatories referenced by historical recipients; the Server fallback made reset FK-safe, the Web regression kept the action enabled, and the authenticated reset advanced the configuration to a clean editable version while preserving request history. `EV-111`/`EV-112` were superseded by `EV-113`/`EV-114`; `FND-022` was resolved. The existing route fixture still fails before the route during mock authentication (`EV-115`). |
| `2026-09-02` | Ran the root coverage gate. Core and Web coverage completed, while the Server coverage task failed in six unrelated setup/teardown suites (`EV-116`); no reset-specific coverage failure was reported. |
| `2026-09-03` | Corrected the remaining stale-cache path: successful signature configuration mutations now immediately refetch the sending review and status queries. The focused Web suite passed 32/32 tests, including the cancelled-request → reset → editable-state regression. |
| `2026-09-03` | Investigated collaborator access: local Auth and `/auth/complete-sign-in` succeed for the seeded collaborator, while the consumed invitation had an active flow session but the browser route could redirect an existing user away from the required login return path. The middleware/return-target correction passed isolated Playwright login coverage 6/6 and focused Web coverage 52/52. |
| `2026-09-03` | Fresh client and collaborator reports reproduced the common post-exchange failure: an active flow session is persisted, but the next browser context request has no usable Gateway cookie. `EV-122` failed, `FND-025` opened, and scoped `builder_server`/`builder_web` corrections were activated as `EV-123`/`EV-124`. |
| `2026-09-03` | The FND-025 investigation corrected the initial cookie hypothesis: real Chromium stored both cookies, while missing CORS exposure made the next actor mutation omit the in-memory CSRF token. Server/Web corrections and desktop/narrow isolated browser flows pass in `EV-127`–`EV-129`; fresh real invitations remain required for final MV-02/MV-03 confirmation. Web architecture then exposed independent prior-test violations, opening `EV-130`/`EV-131` and `FND-026`. |
| `2026-09-03` | Moved the misplaced action-hook test to its owning feature-hook boundary; `EV-133` passes focused behavior, Web architecture/code/types and whitespace checks, resolving `FND-026`. Inspected fresh 1200×900 and 390×844 collaborator-login captures as `VIS-23`. |
| `2026-09-03` | A fresh real client replay passed exchange, cookies, CSRF and `choose_channel` but failed OTP issue. `EV-134` isolated the recipient-ID/person-ID mismatch in the Core OTP use case; `FND-027` opened and scoped `builder_fix_core` `01a06732-766a-7e60-bf9d-954a426ca795` was activated as `EV-135`. |
| `2026-09-03` | FND-027 inspection expanded the correction to invitation-bound issue/verify and first-use guard initialization. The original Server Builder left a type-invalid partial transaction patch after three bounded waits; replacement `builder_fix_server` `01a06745-3750-7681-aaf8-32cb2a4d982d` was activated as `EV-137`. |
| `2026-09-03` | Clean-server OTP replay exposed the first resend boundary defect: the reservation timestamp equals the rolling-window start but strict-after counting returns zero against a guard count of one. `FND-029` opened and scoped Server correction `EV-142` was activated before implementation. |
| `2026-09-03` | Pre-verification source inspection exposed a second deterministic CAS defect: valid OTP verification hardcodes guard version 1 although issue advances it. `FND-030` opened and the existing Core Builder was resumed as `EV-143` before correction. |
| `2026-09-03` | EV-144 completed the real client path through OTP issue, Inngest/Mailpit delivery, verification and authenticated `reading`; PostgreSQL confirms consumed challenge and session rotation. EV-145 passed the current Core/Server/Web and isolated Chromium gates, resolving FND-025 and FND-027–FND-030. |
| `2026-09-03` | User tab-switch evidence exposed stale Gateway bootstrap replay for both client and collaborator flows. FND-031 opened and the existing Web Builder was resumed as EV-146 before implementation; affected state-continuity visual evidence is stale as VIS-24. |
| `2026-09-03` | Resolved FND-031 after user tab-switch evidence: one-time Gateway bootstrap is now independent from refreshable HMS auth state, collaborator establishment is a separate guarded transition, and client OTP plus collaborator reading survive blur/focus and token-refresh rerenders. Focused hook tests, Web gates and narrow/desktop isolated Chromium route checks pass in `EV-146`; `VIS-24` is current. |
| `2026-09-03` | Exact-lawyer desktop/narrow replay resolved FND-033–FND-035 through private PDF reading, acknowledgement and the same-origin Nest proxy. The provider page then returned Documenso's 404 because the stored envelopes were still `DRAFT`/`NOT_SENT`; FND-036 opened before correcting the missing provider distribution step. |
| `2026-09-03` | Targeted local field/distribution repair proved Documenso can serve the correct signer state without provider e-mail. The 390×844 proxy replay then exposed root asset 404s and nonce CSP blocking, so FND-037 opened before proxy source changes. |
| `2026-09-03` | EV-162 passed isolated proxy tests, but EV-163 real exact-lawyer replay showed wildcard-param path extraction still nested every rewritten asset under the signer route and dropped query strings. FND-037 remains active with a real URL-shaped correction scope. |
| `2026-09-03` | EV-164 resolved FND-037: an alias-scoped virtual provider base/sign route plus dual Nitro/Vite local prefix handling now renders the real assigned PDF and opens the signature dialog at narrow/desktop viewports without provider-origin leakage or HTTP errors. |
| `2026-09-03` | EV-165 retried current-code provisioning after a non-destructive Inngest restart; the dev executor still left the event run without a step/lease, so real async provisioning remains an environment/integration gate despite green use-case/provider tests. |
| `2026-09-03` | The user-provided Inngest stacks plus live database/provider evidence split the apparent outage into FND-038 (deterministic post-confirmation configuration-version mismatch) and FND-039 (missing provider envelope during cancellation). Scoped Core and Server Builders were activated as EV-166/EV-167 before source changes. |
| `2026-09-03` | The repeated provisioning stack also exposed FND-040: deterministic application failures bypass the required non-retriable job translation. The existing Server Builder was resumed as EV-168 before expanding into the provisioning job/test boundary. |
| `2026-09-03` | The resumed Server Builder produced no FND-040 job change after bounded waits and was closed as EV-169; the completed provider candidate remains integrated while a narrow replacement is required for the job boundary. |
| `2026-09-03` | Replacement `builder_fix_server` `01a0681d-958f-7030-873b-af4dafca9864` (`Locke`) was activated as EV-170 for the two-file FND-040 job correction before source changes. |
| `2026-09-03` | EV-171 official sensors rejected the first canonical-status cleanup on a TypeScript narrow-union error despite passing behavior tests; the same Core Builder was resumed and affected type evidence remains stale. |
| `2026-09-03` | EV-172 passed current Core/Server/provider/real-Inngest/static gates. EV-173 then provisioned both current request documents through real Inngest and Documenso after the sibling aggregate advance, persisted all mappings and delivered all four HMS invitations. EV-174 converged the stale missing-envelope cancellation in one run, resolving FND-038–FND-040. |
| `2026-09-03` | User refresh evidence reproduced `Signing Gateway CSRF token is required.` Source comparison found that the implemented `GET /context` omitted the fresh synchronizer-token rotation/response explicitly required by revision 5. FND-041 opened; healthy local Auth/Server/Compose preflight and scoped Core/Server/Web activations were recorded as EV-175–EV-178 before source changes. |
| `2026-09-03` | EV-179 passed the focused/static FND-041 candidate. EV-180 then proved real refresh recovery and CSRF propagation, while exposing the authenticated-transition envelope leak as FND-042; the same Server owner was resumed before correcting both OTP and collaborator paths. |
| `2026-09-03` | The resumed Server owner projected strict public bodies and final rotated headers for both authenticated transitions. EV-181 then passed the real refreshed collaborator session through private PDF retrieval and the next CSRF-protected acknowledgement with only 200 responses and no Chromium console errors, resolving FND-041/FND-042. |
| `2026-09-03` | User evidence showed the real proxied Documenso completion UI in English. Upstream v2.17.0 source confirms bundled `pt-BR` translations and broken automatic negotiation; FND-043 and scoped Server activation EV-183 were recorded before proxy edits. |
| `2026-09-03` | FND-043 was corrected at the provider boundary: only Documenso's serialized `pt-BR` preference is synthesized upstream, incoming HMS cookies and authorization remain stripped, and EV-183–EV-184 pass focused proxy plus Server static/architecture sensors. |
| `2026-09-03` | Disabled only Documenso email/password signup through the supported provider environment switch. The application container was recreated healthy with its database/volumes preserved, removing the irrelevant post-signing account-claim/password panel while retaining administrator sign-in. |
| `2026-09-03` | Removed the provider signing-card Share action at the HMS completion proxy boundary. The first Builder produced no patch; its replacement was corrected from `/completed` to the real `/complete` path, and EV-187–EV-188 pass 12 focused tests plus Server static/architecture gates without weakening nonce CSP. |
| `2026-09-01 23:05` | The rerouted Builder left a partial explicit-name conversion; `EV-41` failed and the remaining model declarations were isolated for a narrow replacement. |
| `2026-09-01 20:45` | The correction was interrupted after partial hasher/verifier edits; `EV-20` failed, `FND-007` opened, and a fresh same-boundary correction is required. |
| `2026-09-01 20:46` | Activated fresh `builder_fix_core` agent `01a05f24-1337-7ae2-b484-21e26cfeb07a` for FND-007. |
| `2026-09-01 21:00` | Current-candidate sensors found two remaining type errors after the interrupted correction; `EV-22` failed and FND-007 remained active. |
| `2026-09-01 21:01` | Activated narrow `builder_fix_core` agent `01a05f2e-01de-7a71-afc6-19bcbbe62e7c` for the two FND-007 type errors. |
| `2026-09-01 21:20` | FND-007 resolved; `EV-24` passed all current Core sensors, while the security-contract findings remained active. |
| `2026-09-04` | Investigated the Documenso sign-in screenshot. Chromium reproduced a CORS failure because the browser origin was `localhost:3004` while Documenso generated its session request for `127.0.0.1:3004`. Updated the local public-origin defaults, recreated only the Documenso app container, and verified the session request returns `200` with no console errors in `EV-230`; database/volumes were preserved. |
| `2026-09-04` | User cancellation evidence reproduced a stale active sending card after the cancel command returned before asynchronous provider reconciliation. FND-054 opened and the Web controller correction was recorded as `EV-231`; it now renders cancellation pending immediately, polls authoritative status, and unlocks reset only after terminal cancellation. Focused and full Web evidence passed; the separate route Playwright preflight remained blocked in its shared login fixture as `EV-232`. |
| `2026-09-04` | User client-reading evidence and live PostgreSQL state isolated FND-055: after the collaborator submitted, the request was `partially_submitted`; private retrieval remained authorized, acknowledgement rejected that valid aggregate state, and the UI rendered the action failure as a PDF-load failure. EV-233 activated a direct F13 Core/Web correction without reviewer agents per explicit user direction. |
| `2026-09-04` | EV-233 resolved FND-055–FND-057. Cross-signer acknowledgement/start now admit `partially_submitted`, action failures no longer replace loaded PDFs, and recoverable client invitations preserve monotonic OTP generations across consumed challenges and rolling-window rollover. Focused tests, package type/architecture gates, desktop replay and 390×844 keyboard replay passed against real PostgreSQL, Mailpit and Gateway REST. |
