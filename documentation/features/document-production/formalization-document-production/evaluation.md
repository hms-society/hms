---
feature: "document-production/formalization-document-production"
spec: ./spec.md
plan: ./plan.md
spec_revision: 8
status: ready
prd: https://plataformahms.atlassian.net/wiki/spaces/~712020e69febeaca304dffb2d8d156ea17d2c4/pages/24051713/PRD+M+dulo+de+Formaliza+o
jira_tickets:
  - SCRUM-139
updated_at: 2026-08-25
---

# Evaluation

Evaluation of Spec revision `8` against the current implementation.

Current result: Revision 8 makes Intake legal area/topic inheritance explicit on the Formalization projection and uses it for contract-form discovery. Revision 7 replacement behavior remains implemented, including answer clearing and two temporary seeded forms. The four reviewer evidence findings are now corrected: the rollback path runs through the real transaction after insertion, the seed was executed twice, all required PNGs are retained with dimensions, and the post-seed admin/paralegal/dark-mode browser matrix is green. The same Reviewer completed the final read-only recheck with no blockers. The local generation worker limitation remains accepted as `FND-011`, and the contracted Pencil `WNe1f` state remains a deferred supplemental state because no approved read-by-Intake API exists. Final status awaits the PR CI quality gate.

## Execution assignments

| Name | Agent ID | Spec revision | RF/CA scope | Allowed paths | Prohibited paths | Phase/tasks | Validation exits | Evidence location |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `builder_core` | `01a035f7-ffc3-7562-927f-ba31f2d9cbd7` | `5` | `RF-01`–`RF-10`; `CA-01`–`CA-11` | `packages/core/**` | Spec/Plan/Evaluation, Rules/PRD, migrations, `apps/server/**`, `apps/web/**`, generated artifacts | F1-T1/F1-T2 | Core focused unit/use-case tests, `pnpm --filter @hms/core check-types`, public export review | `EV-03`, handoff report and integrated verification |
| `builder_validation` | `01a03610-5c7d-7542-81ff-48f3dfb7700d` | `5` | `RF-04`–`RF-05`; `CA-04`–`CA-05`, `CA-10`–`CA-11` transport shape | `packages/validation/**` | Spec/Plan/Evaluation, Rules/PRD, `packages/core/**`, `apps/server/**`, `apps/web/**`, generated artifacts | F2-T1 | Validation tests, code check and typecheck | `EV-04`, handoff report and integrated verification |
| `builder_web` | `01a03610-4ea8-7313-8821-38c351a9e3bf` | `5` | `RF-07`; `CA-07`–`CA-09` plus shared UI regressions | `apps/web/**` limited to Plan F3 paths | Spec/Plan/Evaluation, Rules/PRD, `packages/**`, `apps/server/**`, migrations, `routeTree.gen.ts`, dependencies | F3-T1/F3-T2 | Focused widget/route tests, web code/types and Consultation regression; mocked transport labeled | `EV-07`, `EV-08`, handoff report and integrated verification |
| `builder_server` | `01a03627-3518-7730-aaf1-e7514852ab0c` | `5` | `RF-01`–`RF-11`; `CA-01`–`CA-11`, `CA-13` | `apps/server/**` and `apps/server/rest-client/formalization/**` limited to Plan F4 paths | Spec/Plan/Evaluation, Rules/PRD, `packages/**`, `apps/web/**`, generated migrations/meta, lockfiles | F4-T1/F4-T2/F4-T3 | Real DB/seed/server/controller tests, code/types/build, auth/authorization/persistence/source/event/convergence and logs | `EV-05`, `EV-06`, handoff report and integrated verification |

Assignment context: mapped PRD Formalization page and Jira `SCRUM-139`; required Core file tree and contracts from Spec revision 5; Rule Pack includes code conventions, core package, use-case testing and messaging rules; Architecture/Modules/Tooling and design bundle were read at kickoff. The Builder was instructed to wait until this activation record existed before editing.

F2/F3 activation context: F1 is completed with `EV-03`; the Validation and Web assignments are dependency-ready and may work in parallel on disjoint paths. The Web Builder must preserve Consultation behavior and may use mocked browser transport only for isolated UI contract evidence; real REST/Auth/persistence remains an Orchestrator-integrated exit.

F4/F5 activation context: F2 and F3 are completed with `EV-04`, `EV-07` and `EV-08`; `builder_server` is activated for F4 and the same `builder_web` agent is resumed for F5. Their paths are disjoint; migrations/meta, generated route tree and official Evaluation remain Orchestrator-owned.

Correction activation: the original `builder_core` (`01a035f7-ffc3-7562-927f-ba31f2d9cbd7`) was resumed for the in-contract F1-T2 type inference correction exposed by the integrated Server typecheck. Its allowed path remains `packages/core/**`; F1-T2 and F4 remain `in_progress` until this correction and migration-backed runtime evidence pass.

Correction activation: `builder_server` (`01a038fe-e3ae-70d1-9c00-3403e056779a`) was activated for the user-requested seed correction under F4-T1. Its scope is limited to `apps/server/src/intake/database/intake-seeder.ts` and colocated seed tests; it removed the additional non-document-production `in_formalization` Intake and preserved the document-production Intake as the sole record with that status.

Authorization correction activation: `builder_core` (`01a035f7-ffc3-7562-927f-ba31f2d9cbd7`) and `builder_server` (`01a038fe-e3ae-70d1-9c00-3403e056779a`) were resumed for the explicit product decision that administrators may operate Formalizations. Core owns role-aware authorization and focused tests; Server owns forwarding the authenticated collaborator profile through every Formalization controller. The assigned lawyer projection and audit actor identity remain separate.

Routing correction activation: the Orchestrator owns the workspace dependency override and lockfile for the client-side Formalization preload crash. The existing `builder_web` assignment remains the owner of focused route/browser regression coverage under `apps/web/**`; it may not edit dependencies, generated route artifacts, or this Evaluation.

Revision 8 evidence-refresh activation: `builder_server` (`01a038fe-e3ae-70d1-9c00-3403e056779a`) owns current seed/database/runtime verification under the existing F4-T1/F4-T3 paths, and `builder_web` (`01a03610-4ea8-7313-8821-38c351a9e3bf`) owns current route, responsive, keyboard and affected visual capture checks under the existing F5-T3 paths. Both assignments are limited to in-contract corrections if a current sensor exposes one; neither Builder edits Spec, Plan or Evaluation. The Orchestrator owns generated artifacts, dependency/lockfile reconciliation, official evidence and the final conformance record. The same Reviewer (`01a0368c-9323-7dc1-a5d4-06aff0ce1fb9`) must recheck the current candidate after refreshed evidence.

## Reviewer gate

- Initial integrated reviewer: `01a0368c-9323-7dc1-a5d4-06aff0ce1fb9` (`reviewer-agent`), completed read-only review. Its valid blocking findings were routed to the owning Core, Server and Web Builders and corrected; the initial stale-evidence findings were refreshed by the Orchestrator.
- Follow-up: the previous same-reviewer re-review reopened `FND-028`–`FND-031`; those corrections are now complete. The same Reviewer completed the final read-only recheck of the current candidate with no blocking findings. Historical `stale` rows are explicitly superseded by current EV-50–EV-52 evidence.

## Acceptance matrix

| Criterion | Evidence | Status |
| --- | --- | --- |
| `CA-01` | `EV-02`; `EV-05`; `EV-44`; `EV-51`; `EV-52` | `passed` |
| `CA-02` | `EV-05`; `EV-51`; `EV-52` | `passed` |
| `CA-03` | `EV-32`; `EV-46`; `EV-51`; `MV-02` | `passed` |
| `CA-04` | `EV-02`; `EV-07`; `MV-01` | `passed` |
| `CA-05` | `EV-02`; `EV-07`; `MV-01` | `passed` |
| `CA-05A` | `EV-40`; `EV-45`; `EV-50` | `passed` |
| `CA-05B` | `EV-41`; `EV-44`; `EV-45`; `EV-50` | `passed` |
| `CA-06` | `EV-02`; `EV-05`; `EV-07`; `MV-01` | `passed` |
| `CA-07` | `EV-02`; `EV-07`; `MV-01` | `passed` |
| `CA-08` | `EV-02`; `EV-05`; `EV-07`; `MV-01` | `passed` |
| `CA-09` | `EV-02`; `EV-07`; `EV-08` | `passed` |
| `CA-10` | `EV-02`; `EV-05`; `EV-07` | `passed` |
| `CA-11` | `EV-02`; `EV-05`; `EV-07`; `MV-01` | `passed` |
| `CA-12` | `EV-07`; `EV-33`; `EV-43`; `EV-46`; `EV-47`; `EV-51`; `EV-52` | `passed` |
| `CA-13` | `EV-06`; `EV-10`; `EV-44`; `EV-50`; `EV-52` | `passed` |

## Automated and runtime evidence

| ID | Layer | Command or scenario | Result | Status |
| --- | --- | --- | --- | --- |
| `EV-01` | Cross-layer | `docker compose ps -a`; `curl -fsS http://localhost:8000/auth/v1/health`; `curl -fsS http://localhost:3333/health`; `pnpm --filter server dev`; `pnpm --filter web dev` | Initial run failed because Supabase containers were stopped and `supabase-db` had an OCI file/directory mount error. After restarting `supabase-db` and `docker compose up -d`, DB/Auth/dependencies became healthy; persistent Server/Web sessions reached readiness. `FND-001` resolved; preflight current. | passed |
| `EV-02` | Cross-layer | Baseline conformance comparison against Spec revision 5, Plan, required tree, contracts, exclusions and seven design references | Existing code had the contracted Formalization gap; no feature paths existed at kickoff. Required implementation paths, runtime contracts and exclusions are mapped in Plan; all seven PNGs exist with verified dimensions. F1 now adds only the Core-owned paths. | passed |
| `EV-03` | Core | `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core test`; `pnpm --filter @hms/core lint`; `git diff --check -- packages/core` | Current integrated Core candidate: typecheck passed; 83 test files/260 tests passed after lifecycle/CAS/document-lock corrections; lint exit 0 with pre-existing Consultation warnings; diff check clean. | passed |
| `EV-04` | Validation | `pnpm --filter @hms/validation test`; `pnpm --filter @hms/validation lint`; `pnpm --filter @hms/validation check-types`; `git diff --check -- packages/validation` | Current F2 candidate: 9 test files/32 tests passed; lint and typecheck passed; diff check clean. Public export and strict payload review found no actor/source snapshot fields. | passed |
| `EV-05` | Server | Focused Formalization controller, persistence, authorization, source/event and convergence tests against real infrastructure | `pnpm --filter server test -- src/formalization --no-file-parallelism`: 1 file/4 tests passed, including lifecycle/CAS rollback regression; server type/code/build checks passed. | passed |
| `EV-06` | Database | Drizzle migration generation/apply and deterministic seed | Migration `0038_sharp_captain_flint.sql` and the historical seed run passed before the duplicate-Intake correction; runtime observations are stale for the corrected seed source. | stale |
| `EV-07` | Web | `pnpm --filter web check:code`; `pnpm --filter web check:types`; focused Vitest for neutral package/review components; focused Playwright CLI route tests | Current F3 candidate: code check and typecheck passed; 3 focused Vitest files/6 tests passed; 4 Playwright CLI tests passed. Route transport is mocked and remains only UI/REST contract evidence. Console warnings were repository route-file/`NO_COLOR` warnings, not feature failures. | passed |
| `EV-08` | Web | Consultation source-neutral regression coverage via `tests/routes/document-production/consultation-documents.index.test.tsx` and `consultation-document-version.test.tsx` | Consultation adapters preserve individual selection/generation/review/current/history behavior; 4 route tests passed including narrow keyboard layout. | passed |
| `EV-09` | Static | Server/Web code, types, builds and generated route tree on integrated candidate | Server build/types/code passed; Web build/types/code passed; route generation passed with only the pre-existing `modelos-de-documentos/index.test.ts` route warning. | passed |
| `EV-10` | Cross-layer | Real request/response/persistence/authorization replay for `MV-01` and `MV-02`, with console, failed-request and network classification | Historical seed reset plus real Auth/Server/Web CLI passed before the duplicate-Intake correction; runtime evidence is stale for the corrected seed source. | stale |
| `EV-11` | Cross-layer | `pnpm --filter server check:types` after F4 handoff | Failed with TS2345 in `packages/core/src/formalization/use-cases/replace-formalization-document-selection-use-case.ts:116,125`: an untyped `[]` inferred as `never[]` when pushing existing/new package documents. | failed |
| `EV-12` | Server | `pnpm --filter server test -- src/formalization/rest/controllers/tests/formalization-controller.regressions.test.ts --no-file-parallelism` | 1 scenario passed (ineligible Intake returns 400/no event); not-found scenario returned 500 because `formalizations` table is absent until Orchestrator generates/applies the migration. | failed |
| `EV-13` | Cross-layer | `pnpm --filter server check:types` after FND-003 correction | Passed after the original Core Builder typed the `PackageDocument[]` accumulator; no `UnknownDependenciesException` or TypeScript errors. | passed |
| `EV-14` | Web | `pnpm --filter web exec playwright test tests/routes/formalization/formalization.index.test.tsx tests/routes/document-production/consultation-documents.index.test.tsx --workers=1` (first integrated rerun) | Failed before assertions after 120s because a stale broad `tests/routes` Playwright CLI process occupied the test environment and no `3100` server became ready. | failed |
| `EV-15` | Web | Same focused Playwright CLI command after stopping the stale broad process | 3 tests passed in 21.8s, including Formalization journey and Consultation narrow/keyboard coverage. Warnings were limited to existing route-file/`NO_COLOR` notices; no feature assertion or failed-request error. | passed |
| `EV-16` | Database | `pnpm --filter server db:migration:generate`; reviewed `0038_sharp_captain_flint.sql`; `pnpm --filter server db:migration:apply` | Generated schema contained 33 tables and `formalizations`; the new SQL was reconciled to omit duplicate pre-existing Case Management DDL caused by historical snapshot drift, preserving only the new Formalization table/indexes; migration applied successfully. | passed |
| `EV-17` | Server | `pnpm --filter server check:types`; `pnpm --filter server test -- src/formalization`; real fixture assertions | Server typecheck passed; 1 server test file/3 tests passed: stable 404 without protected snapshot, ineligible Intake 400 without event, and real draft persistence with stale CAS 409 preserving version 2. | passed |
| `EV-18` | Static | `pnpm --filter server check:code` after F4 handoff | Blocked only by formatting diagnostics in Orchestrator-owned generated `migrations/meta/0038_snapshot.json` and `_journal.json`; feature Server source has no reported code issue. | failed |
| `EV-19` | Cross-layer | `docker compose ps -a`; `curl http://localhost:8000/auth/v1/health`; `curl http://localhost:3333/health` | DB/Auth healthy and Auth/Server returned HTTP 200; `supabase-storage` remains unhealthy but is not used by current Formalization controller scenarios and must be classified during final gate. | passed |
| `EV-20` | Static | `pnpm --filter server check:code` after formatting generated meta artifacts | Passed; Biome checked 589 Server files with no diagnostics. | passed |
| `EV-21` | Database | `pnpm --filter server db:seed`; PostgreSQL verification of `formalizations`, Formalization package/document/specification | Historical seed passed before the duplicate-Intake correction; its database observations are not current for the corrected seed source. | stale |
| `EV-22` | Server | Official post-seed rerun: `pnpm --filter server check:types`; `pnpm --filter server test -- src/formalization --no-file-parallelism`; `pnpm --filter server check:code` | Typecheck, 1 file/7 real tests, build and code check all passed after lifecycle/confirmation guard corrections; no SDD or generated-artifact edits by Builder. | passed |
| `EV-23` | Cross-layer | `pnpm --filter web generate-routes`; integrated Server/Web build, code/type checks and Validation tests | Route tree generated with Formalization list/detail/review routes. Server build, Web build, Web code/types, Validation tests and focused real-browser infrastructure preflight passed. The existing route-file warning for `modelos-de-documentos/index.test.ts` and `NO_COLOR` notices are classified as non-feature warnings. | passed |
| `EV-24` | Cross-layer | Historical repository Playwright CLI authorization replay | Administrator login succeeded before the administrator-access rule change; the observed 403 is superseded by `EV-32` and is retained only as historical evidence. | stale |
| `EV-25` | Cross-layer | Seed reset followed by repository Playwright CLI `real-formalization.test.ts`; PostgreSQL verification of Formalization/package/generation | Historical browser run used the prior seed source; the focused Formalization behavior remains covered, but this evidence is stale for the corrected seed dataset. | stale |
| `EV-26` | Cross-layer | Focused mocked route/Consultation suite after Web contract correction | 5 route tests passed: Formalization selection/generation/review/approval/confirmation/closure contract, close/reopen dialogs, and Consultation narrow/keyboard regression. | passed |
| `EV-27` | Static | Post-correction integrated sensors | Historical integrated sensors passed before the requested seed correction. | stale |
| `EV-28` | Server | `pnpm --filter server check:code`; `pnpm --filter server check:types`; `pnpm --filter server test -- src/formalization`; `git diff --check -- apps/server/src/intake/database/intake-seeder.ts` | Seed correction passed Server code/types checks, 7 Formalization tests, and whitespace validation. No colocated seeder tests exist. `db:seed` was not rerun because it destructively resets the development dataset. | passed |
| `EV-29` | Web | `pnpm --filter web check:code`; `pnpm --filter web check:types`; `pnpm --filter web test -- src/ui/intake/widgets/pages/intake-details-page/tests/intake-details-page.test.tsx`; `git diff --check -- apps/web/src/ui/intake/widgets/pages/intake-details-page` | Intake in-progress Formalization card matches the `qxkrh` state: compact header/status, `Abrir formalização` action, no direct contracting action; 1 file/6 tests passed and Web code/types checks passed. | passed |
| `EV-30` | Database | Targeted PostgreSQL verification and repair of seeded Consultation `00000000-0000-4000-8000-000000000101` | The linked Consultation was confirmed `pending` despite finalized attendance, then updated narrowly to `completed`; the row remains linked to the sole seeded `in_formalization` Intake. No full `db:seed` reset was performed. | passed |
| `EV-31` | Database | Targeted PostgreSQL verification and repair of the `Condições comerciais da formalização` dynamic-form context | The formalization form was confirmed with `legal` context, then updated narrowly to `formalization`; other legal forms were not changed. | passed |
| `EV-32` | Core/Server | `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core test -- src/formalization --no-file-parallelism`; `pnpm --filter server check:code`; `pnpm --filter server check:types`; `pnpm --filter server test -- src/formalization --no-file-parallelism`; `git diff --check` | Role-aware authorization allows `CollaboratorProfile.Admin` to start, read and operate any Formalization, including cancellation of another collaborator’s pending generation. All 19 Server controller calls forward `actorProfile`; assigned-lawyer ownership and audit actor IDs remain distinct. Core: 16 files/31 tests; Server: 1 file/7 tests; code/types/diff checks passed. | passed |
| `EV-33` | Web | `pnpm --filter web check:code`; `pnpm --filter web check:types`; `pnpm --filter web test -- src/ui/formalization --no-file-parallelism`; `git diff --check -- apps/web/src/ui/formalization` | Removed page-owned horizontal centering and maximum-width utilities, including `mx-auto` and `max-w-6xl`, from Formalization page and review roots so the app layout owns horizontal geometry. Two focused UI test files passed; Web code/types and diff checks passed. Existing visual evidence affected by the root geometry change is marked stale pending recapture. | passed |
| `EV-34` | Web | `pnpm --filter web check:code`; `pnpm --filter web check:types`; `pnpm --filter web test -- src/ui/formalization` | Removed the Formalization page contract-flow banner and `Ativa`/`Encerrada` badge while preserving the disabled future `Confirmar contratação` control. Focused Formalization UI tests (2 tests), Web code check, and Web typecheck passed. | passed |
| `EV-35` | Web routing | Pin only `@tanstack/router-core@1.171.16`; regenerate routes; run Web code/types, focused Formalization Vitest, and focused Formalization Playwright CLI coverage | Route generation, code check, typecheck and 2 focused UI tests passed, but Playwright SSR repeatedly failed in `@tanstack/react-router@1.170.16` `MatchesInner` because the forced core patch was not paired with its matching React Router patch. This candidate was stopped and superseded. | failed |
| `EV-36` | Web routing | Pin the matched `@tanstack/react-router@1.170.19` / `@tanstack/router-core@1.171.16` pair; regenerate routes; run Web code/types and focused Formalization Playwright CLI coverage with client-console classification | Server rendering recovered, but client hydration failed in the older `@tanstack/start-client-core@1.170.12` because the partial override did not align the full Router/Start graph. The focused Playwright setup could not reach Login and this candidate was superseded. | failed |
| `EV-37` | Web routing | `pnpm --filter web update` for the declared TanStack Router/Start packages; `pnpm --filter web list @tanstack/react-router @tanstack/router-core @tanstack/react-start @tanstack/start-client-core --depth 3`; `pnpm --filter web generate-routes`; `pnpm --filter web check:code`; `pnpm --filter web check:types`; `pnpm --filter web build`; `pnpm --filter web test -- src/ui/formalization --no-file-parallelism`; `pnpm --filter web exec playwright test tests/routes/formalization/formalization.index.test.tsx --grep "intent-preloads" --workers=1` | The coherent graph resolves React Router `1.170.32`, Router Core `1.171.27`, React Start `1.168.49`, and Start Client Core `1.170.27`. Route generation, code/types/build, 2 focused UI tests, and the focused mocked Playwright intent-preload sensor passed; the sensor hovered an actual `Intakes` link and found no `_nonReactive`, `loadRouteMatch`, `preloadRoute`, or undefined-property router errors. Initial sensor drafts exposed fixture activation, role-specific navigation and unrelated fake-host resource-error assumptions before the final focused scenario passed. Existing route-file and `NO_COLOR` warnings remain non-feature warnings. | passed |
| `EV-38` | Documentation | Inspect `documentation/prompts/implement-spec-prompt.md` and `.codex/skills/implement-spec/SKILL.md`; `git diff --check` | The source prompt and generated skill now explicitly reject page-root horizontal margins, `mx-auto`, `max-w-6xl`, and equivalent `max-w-*` containers unless a governing design/rule assigns them to the page; component-local constraints remain allowed. | passed |
| `EV-39` | Web | `pnpm --filter web check:code`; `pnpm --filter web check:types`; `pnpm --filter web test -- src/ui/formalization`; `pnpm --filter web exec playwright test tests/routes/formalization/formalization.index.test.tsx --grep "runs the formalization form and individual document contract" --workers=1`; `git diff --check -- apps/web/src/ui/formalization/widgets/pages/formalization-page/index.tsx` | Restyled the `Visualizar` document action as a token-based outlined pill with an eye icon and clearer hover affordance; `Revisar` keeps its existing default treatment and navigation. Focused Formalization UI tests (2 tests), Web code/types checks, whitespace validation, and the focused browser journey (1 test) passed. Existing `NO_COLOR` and route-file warnings remain non-feature warnings. | passed |
| `EV-40` | Core/Validation/Server/Web | `pnpm --filter @hms/core test -- src/formalization/use-cases/tests/replace-formalization-contract-form-use-case.test.ts src/shared/use-cases/tests/list-dynamic-forms-use-case.test.ts`; `pnpm --filter @hms/validation test -- src/formalization/schemas/tests/formalization-schemas.test.ts`; `pnpm --filter server test -- src/formalization --no-file-parallelism`; package code/type checks | Revision 7 replacement use case, context-filtered form discovery, validation exports, REST controller composition, UI selector integration and two temporary seed definitions are implemented. Focused Core (2 files/5 tests), Validation (1 file/9 tests), Server Formalization (1 file/7 tests), Server/Web code checks and all package typechecks passed. Clean-seed uniqueness and the canonical three-definition catalog are confirmed by `EV-50`; replacement persistence remains covered by the isolated real-controller regression in `EV-45`. | passed |
| `EV-41` | Core/Server/Database/Web | `pnpm --filter server db:migration:generate`; `pnpm --filter server db:migration:apply`; Core/Server/Web type/code checks; focused Core tests; PostgreSQL inheritance query; `git diff --check` | Formalization now copies `legalAreaId`/`legalTopicId` from Intake at start/seed, exposes them in REST, and uses the inherited values for replacement filtering. Migration `0039_pink_spacker_dave.sql` and its snapshot were generated/applied successfully; `EV-50` confirms the clean-seed projection and all three matching definitions. | passed |
| `EV-42` | Web static preflight | `pnpm --filter web check:types`; `pnpm --filter web check:code`; focused Vitest; `git diff --check` | `builder_web` removed the unused `PageTitle` import from the source-neutral document package. Web code/types checks, focused Vitest (4 files/6 tests) and whitespace validation pass. | passed |
| `EV-43` | Web mocked route preflight | `pnpm --filter web exec playwright test tests/routes/formalization/formalization.index.test.tsx tests/routes/document-production/consultation-documents.index.test.tsx --workers=1` | After stopping a stale reused `3100` Vite server, the CLI reached the fixture-backed page and exposed two stale accessible-name assertions (`Ficha de contratação` and `Encerrar sem contrato`). The owned test assertions now follow the current snapshot-driven UI (`Condições comerciais` and `Encerrar sem contratação`); the focused suite passed 6 tests in 40.2s. Fixture diagnostics include the known mocked-host `GET http://hms-api.test/clients?page=1&limit=50` name-resolution error; no scenario failed and no MCP evidence was used. | passed |
| `EV-44` | Cross-layer current runtime | Authenticated CLI `curl` against local Auth/Server; two `POST /formalizations/by-intake/:intakeId/start` calls; filtered `GET /dynamic-forms`; PostgreSQL seed queries | Current local services are healthy (`Auth v2.189.0`, Server `status=ok`). Administrator authentication succeeded; the existing Formalization returned the Intake-inherited legal area/topic and both start calls returned the same ID/version (`17`). The inherited-context filter returned the current `Condições comerciais da formalização` definition. No full `db:seed` was run, so this is current targeted runtime evidence rather than a clean-seed proof. | passed |
| `EV-45` | Server replacement/transaction regression | `pnpm --filter server check:code`; `pnpm --filter server check:types`; `pnpm --filter server test -- src/formalization`; isolated PostgreSQL-backed controller regression | Replacement persistence passes through the real controller composition, proving legal context preservation, selected-definition persistence, answer clearing, form-revision reset and optimistic-version increment. The corrected rollback scenario now exercises the real HTTP-to-transaction path, forces the post-insert Intake CAS conflict, and proves HTTP 409, no Formalization row, unchanged Intake and no generation event. Server code/types and the Formalization suite pass with 1 file/8 tests. | passed |
| `EV-46` | Cross-layer real browser | Temporary repository Playwright CLI config against the already-running real Web/Server/Auth; assigned-lawyer and unauthorized-collaborator scenarios; dark-mode replay | Real Auth login and Server-backed navigation passed for the administrator: `/login` reached `/home`, the seeded Intake opened the existing Formalization, and the current page rendered without console errors or failed requests. The same replay passed at `390 × 844` with keyboard focus on `Reabrir formulário`; a paralegal session received the expected 403 and rendered the access error state. A separate dark-mode replay passed without console errors or failed requests. `NO_COLOR` was the only runner warning. | passed |
| `EV-47` | Web visual refresh | Final repository Playwright CLI screenshots inspected with local image tooling | Final captures are saved under `apps/web/test-results/final-visual/`: `F2GBfU.png` (`1200 × 1485`), `zetNe.png` (`957 × 398`) and `Z3Ll2j.png` (`945 × 401`). The page preserves the supplied hierarchy while using database-driven form labels/values; the package has the updated outlined `Visualizar` pill/eye affordance and the sending configuration remains a disabled explanatory state. The shorter page/package content reflects the canonical one-document seed and is recorded as a state adaptation, not a contract gap. | passed |
| `EV-48` | Intake visual refresh | Current repository Playwright CLI screenshot inspected with local image tooling | Fresh capture `apps/web/test-results/real-intake-formalization.png` (`1280 × 1633`) shows the in-progress `Formalização iniciada` card with `Abrir formalização`, matching the scoped `qxkrh` state. The supplemental contracted `WNe1f` state remains deferred because no approved read-by-Intake API exists. | passed |
| `EV-49` | Web dialog visual refresh | Final repository Playwright CLI capture; local image inspection | Final dialog captures are saved at `apps/web/test-results/final-visual/b2f2jS.png` (`673 × 392`), `nFKJE.png` (`673 × 412`), `ZLBTF.png` (`673 × 453`) and `USNIG.png` (`673 × 505`). The four dialog states were inspected; the package confirmation preserves the no-signature/no-message guard and close-without-contract preserves the required reason/notes controls. | passed |
| `EV-50` | Database/Server | `pnpm --filter server db:seed` twice; PostgreSQL uniqueness, projection and dynamic-form queries after each run | Two complete authorized clear/run executions passed. The final PostgreSQL observation reports exactly one `in_formalization` Intake, one Formalization, one unique Formalization/Intake relation, three `formalization`-context definitions and two temporary replacement definitions. The final lifecycle state is a single open Formalization with six persisted answers, one package and one package document; repeated browser close/reopen increments are expected state transitions, not duplicate seed rows. | passed |
| `EV-51` | Cross-layer real browser | Final temporary repository Playwright CLI matrix against the repeated clean seed: admin access, paralegal denial, dark mode, assigned-lawyer Intake/Formalization, close/reopen, `390 × 844` dialog captures and request diagnostics | Final `pnpm exec playwright test --config playwright.closure.config.ts --workers=1` passed 4/4 in 1m. Admin access passed; paralegal received the expected Formalization 403; dark mode passed; assigned-lawyer close/reopen passed with the optional collaborator 403 classified by `FND-027`. No request failed or page error occurred. Final PNGs were inspected and retained under `apps/web/test-results/final-visual/`; temporary config/test sources are removed after capture. | passed |
| `EV-52` | Cross-layer closure | Corrected real transaction regression, repeated clean seed, final browser matrix and final saved visual artifacts | All four reviewer blockers are resolved without a Contract change: rollback is exercised after the real insert, seed repeatability has two runs, all required current PNGs exist with recorded dimensions, and post-reseed role/theme/browser evidence is green. | passed |

## Manual evidence

| ID | Scenario | Criteria | Expected | Observed | Status |
| --- | --- | --- | --- | --- | --- |
| `MV-01` | Assigned lawyer completes Formalization journey | `CA-01`–`CA-13` | Real login reaches protected Intake/Formalization flow; idempotent start, draft/close/reopen, document generation/review/freshness, confirmation and close-without-contract persist with no signature/contract side effect; desktop/narrow, light/dark and keyboard paths are usable. | Clean-seed lifecycle and final browser evidence pass (`EV-50`, `EV-51`, `EV-52`). Review/approval/confirmation of a freshly generated version remain covered by the accepted local-worker limitation in `FND-011` and the focused real-controller/mocked route evidence; no signing or contract side effect was introduced. | passed |
| `MV-02` | Another lawyer or administrator attempts access | `CA-03` | The other lawyer receives 403; an administrator can read and operate without changing ownership, and audit fields use the administrator’s ID. | Final post-reseed browser matrix proves administrator access and paralegal denial; Core/Server tests retain alternate-collaborator authorization and audit identity coverage (`EV-32`, `EV-51`). | passed |

## Visual evidence

| ID | Surface and state | Viewport | Reference | Implementation | Differences | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `VIS-01` | Formalization page hierarchy | `1200 × 1828` | `design/F2GBfU.png` | `apps/web/test-results/final-visual/F2GBfU.png` (`1200 × 1485`) | Fresh real-service capture inspected. The page keeps the Client/Intake context, inherited responsible lawyer, generic commercial form, document package, sending placeholder and terminal actions; the shorter height reflects the canonical one-document seed and data-driven fields. | passed |
| `VIS-02` | Commercial conditions dynamic-form card | `957 × 398` | `design/zetNe.png` | `apps/web/test-results/final-visual/zetNe.png` (`957 × 398`) | Independent cropped capture inspected. Snapshot-driven labels, required markers, persisted values and completion indicator are present; field count/options are canonical seed data rather than the reference fixture. | passed |
| `VIS-03` | Source-neutral document package | `945 × 401` | `design/Z3Ll2j.png` | `apps/web/test-results/final-visual/Z3Ll2j.png` (`945 × 401`) | Independent cropped capture inspected. The package preserves selection/confirmation controls, document status and sending configuration; the one-document seed leaves the locked sending panel visible in the crop, and `Visualizar` retains its token-based outlined pill/eye affordance. | passed |
| `VIS-04` | Close-form dialog | `673 × 392` | `design/b2f2jS.png` | `apps/web/test-results/final-visual/b2f2jS.png` (`673 × 392`) | Fresh capture inspected: same modal hierarchy, explanatory callout and cancel/destructive action grouping; copy and spacing are adapted to HMS tokens. | passed |
| `VIS-05` | Reopen-form dialog | `673 × 412` | `design/nFKJE.png` | `apps/web/test-results/final-visual/nFKJE.png` (`673 × 412`) | Fresh capture inspected: same warning hierarchy and explicit reopen/cancel controls; copy explains document lock while the form is open. | passed |
| `VIS-06` | Package-confirmation dialog | `673 × 453` | `design/ZLBTF.png` | `apps/web/test-results/final-visual/ZLBTF.png` (`673 × 453`) | Fresh fixture-backed capture inspected: same confirmation warning and no-signature/no-message explanation; the production guard remains explicit. | passed |
| `VIS-07` | Close-without-contract dialog | `673 × 505` | `design/USNIG.png` | `apps/web/test-results/final-visual/USNIG.png` (`673 × 505`) | Fresh fixture-backed capture inspected: definitive warning treatment, required reason select, optional notes textarea and explicit destructive action. | passed |
| `VIS-08` | Intake Formalization in-progress card | `Pencil node qxkrh` | `/Ubuntu-22.04/home/petros/projects/hms/design/hms.pen#qxkrh` | `apps/web/test-results/real-intake-formalization.png` | Fresh real-service capture inspected. The `Formalização iniciada` card exposes `Abrir formalização`, preserves the in-progress status and does not expose a direct contracting action. | passed |
| `VIS-09` | Intake Formalization completed/contracted card | `Pencil node WNe1f` | `/Ubuntu-22.04/home/petros/projects/hms/design/hms.pen#WNe1f` | — | Deferred supplemental state: current Intake details data does not expose an existing Formalization ID for contracted Intakes, and the approved Spec does not add a read-by-Intake API. No client-side ID was invented. | accepted non-blocking |

## Rule and documentation compliance

| Authority | Reference | Result | Notes |
| --- | --- | --- | --- |
| Rule Pack | `documentation/rules/code-conventions-rules.md` | passed | Applied by all Builders; final code checks pass. |
| Rule Pack | `documentation/rules/core-package-rules.md` | passed | Core contracts/use cases and lifecycle/CAS correction reviewed; Core checks pass. |
| Rule Pack | `documentation/rules/use-case-testing-rules.md` | passed | Focused behavior suites cover the new use cases and lifecycle regressions. |
| Rule Pack | `documentation/rules/rest-layer-rules.md` | passed | Canonical controller/client methods and failure mapping verified. |
| Rule Pack | `documentation/rules/controllers-testing-rules.md` | passed | Real infrastructure controller tests cover authorization, persistence and CAS rollback. |
| Rule Pack | `documentation/rules/database-layer-rules.md` | passed | Migration reviewed/applied; module-owned deterministic seed passes. |
| Rule Pack | `documentation/rules/server-app-layer-rules.md` | passed | Nest composition, transaction adapter and server build/type checks pass. |
| Rule Pack | `documentation/rules/messaging-layer-rules.md` | passed | Generation event carries server-built Formalization source; no browser source accepted. |
| Rule Pack | `documentation/rules/ui-layer-rules.md` | passed | Source-neutral widgets, REST context, error propagation and terminal read-only behavior verified. |
| Rule Pack | `documentation/rules/web-app-routing-rules.md` | passed | Protected routes, canonical paths and generated route tree pass. |
| Rule Pack | `documentation/rules/widget-testing-rules.md` | passed | Dynamic form, document package/review and dialog route coverage passes. |
| SDD | `documentation/rules/sdd-rules.md` | passed | Plan-backed ownership, living Evaluation, correction routing and the same-reviewer follow-up are complete. |
| Design | `documentation/design.md` and `design/manifest.md` | passed | Seven supplied references compared against fresh CLI captures; differences recorded above. |
| Architecture/Modules/Tooling | `documentation/architecture.md`; `documentation/modules.md`; `documentation/tooling.md` | passed | Preflight and documented pnpm/Drizzle/TanStack/Biome workflows applied. |

## Findings

| ID | Classification | Source | Affected evidence | Status | Resolution |
| --- | --- | --- | --- | --- | --- |
| `FND-001` | environment | Authenticated-browser preflight; `docker inspect hms-supabase-db`; `docker logs hms-supabase-db` | `EV-01`, all future `MV-*`/`VIS-*` | resolved | Restarted the existing `supabase-db` container after confirming `volumes/db/jwt.sql` and `roles.sql` are regular files, then ran `docker compose up -d`; DB/Auth/Server/Web are healthy/ready. No data volume was removed. |
| `FND-002` | Rule/environment | Web Builder initially detected Playwright MCP processes; root `AGENTS.md` requires repository Playwright CLI and prohibits MCP | `EV-07`, future `MV-*`/`VIS-*` | resolved | Invalid MCP evidence was discarded; the process was stopped and affected Web validation was rerun with `pnpm --filter web exec playwright test ... --workers=1`, yielding 4 passed tests. No MCP evidence is accepted. |
| `FND-003` | implementation | Integrated Server typecheck exposed `TS2345` in Core selection use case lines 116/125 | `EV-11`, `EV-13`, F1-T2/F4 | resolved | Original `builder_core` typed the `PackageDocument[]` accumulator and corrected the Core barrel import; Core tests/types/lint and the integrated Server typecheck now pass. |
| `FND-004` | generated artifact/database | Real Formalization controller regression attempted GET against a database without the new `formalizations` table | `EV-12`, `EV-05`, `EV-06` | resolved | Orchestrator generated/reviewed/applied the Formalization migration; `builder_server` must rerun the controller suite for current runtime evidence. |
| `FND-005` | generated artifact/rule | Drizzle snapshots omit Case Management tables even though tracked migration `0023_case_management.sql` and the live DB already contain them; fresh generation attempted duplicate Case DDL | `EV-16`, `EV-06` | resolved | Reviewed the generated SQL and reconciled only the newly generated migration to omit duplicate Case DDL while retaining the schema-derived Formalization table/indexes. No second feature schema was introduced; the drift is preserved as historical context. |
| `FND-006` | environment | First integrated Web Playwright run timed out waiting for `127.0.0.1:3100` while a stale broad CLI process was running | `EV-14`, `EV-15` | resolved | Stopped only the stale task-owned broad Playwright process and reran the focused CLI scenario successfully; no browser MCP evidence was used. |
| `FND-007` | implementation | F4 handoff inspection: `DocumentProductionSeedReferences.formalizationId` is declared but `DocumentProductionSeeder` does not consume it to create the Formalization-compatible package/document fixture | `EV-06`, `EV-17`, `EV-21`, `EV-22`, `CA-08`, `CA-13` | resolved | `builder_server` wired `formalizationId` through seed orchestration and created deterministic Formalization specification/package/document; official seed, PostgreSQL verification, code/types/tests passed. |
| `FND-008` | implementation/fixture | First real `MV-01` replay: the canonical Formalization dynamic-form seed uses non-UUID ids (`service-type`, `fixed-fee`, etc.) while the frozen Validation Contract requires UUID `fieldId` values | `EV-10`, `MV-01`, `CA-04`, `CA-05`, `CA-13` | resolved | `builder_server` replaced the eight Formalization seed field ids with deterministic UUIDs, reran Server checks/seed, and the real draft request now returns 200 with persisted answers. |
| `FND-009` | implementation/UI | Manifest comparison after F5 integration: required close/reopen/package confirmation dialogs are absent, and close-without-contract hardcodes `reason: 'other'` without reason/notes controls | `EV-25`, `VIS-04`–`VIS-07`, `MV-01`, `CA-06`, `CA-10`, `CA-11` | resolved | `builder_web` implemented the four dialogs, reason/notes controls, disabled signing/contract placeholders and completion indicator; focused route and real-browser CLI evidence now pass. |
| `FND-010` | implementation/integration | Initial reviewer found lifecycle eligibility/atomicity, Web REST path/body mismatches, missing source-neutral review actions, conditional field-key lookup, missing RestResponse error propagation, stale-version selection and terminal editability gaps | `CA-01`–`CA-11`, `EV-05`, `EV-07`, `EV-25` | resolved | Findings were routed to the owning Core, Server and Web Builders. Core now enforces eligible Intake lifecycle, atomic start and open/cancelled document guards; Server adds transactional CAS/rollback, canonical REST guards and Formalization-owned confirmation projection; Web aligns transport, propagates failures, resolves conditional dependencies by field ID, selects newest versions, supports selection/review/edit/decision/current/history, and makes terminal state read-only. Integrated code/types/build/tests pass. |
| `FND-011` | environment/evidence | The local real generation worker leaves the newly requested generation in `running`, so a single real-browser pass cannot advance that version through review/approval/confirmation | `MV-01`, `CA-08`–`CA-10` | accepted non-blocking | The real pass proves server-built source persistence, pending/confirmation lock, keyboard/narrow behavior, and no side effects; the successful review/approval/confirmation path is covered by the 5-test mocked route suite plus real Server/Core controller/use-case tests. The UI correctly prevents confirmation until the worker produces a fresh reviewed version. |
| `FND-012` | SDD evidence | Final reviewer found superseded test counts and old screenshot references in the Evaluation ledger | `EV-03`, `EV-22`, `EV-27`, `VIS-01`–`VIS-07` | resolved | Reconciled counts to Core 260 and Server 7 Formalization tests, recorded Web 5-file/16-test coverage, and replaced visual references with exact-dimension `vis-01`–`vis-07` artifacts plus dark-mode capture. |
| `FND-013` | implementation/seed | User review identified a second seeded Intake with `status = in_formalization` in `apps/server/src/intake/database/intake-seeder.ts` | `CA-13`, `EV-06`, `EV-10`, `EV-21`, `EV-25`, `EV-27`, `MV-01` | resolved | Removed the additional divorce Intake from the seed. The document-production Intake remains the sole seeded `in_formalization` record; database reseeding is still pending because the command clears the dev dataset. |
| `FND-014` | Contract/API | Pencil `WNe1f` requires a functional `Abrir formalização` action for the contracted Intake state, but `IntakeDetailsData` has no Formalization ID and the current start REST action only accepts eligible `viability_registered`/`in_formalization` Intakes | `VIS-09`, `CA-01`, `CA-13` | accepted non-blocking | `qxkrh` is implemented and covered by current real capture. `WNe1f` is explicitly supplemental/deferred in the Plan and requires an approved server/API read contract for an existing Formalization by Intake; no client-side ID was invented and no in-scope acceptance criterion is blocked. |
| `FND-015` | implementation/seed | The Formalization Intake’s seeded Consultation was `pending` even though its attendance was finalized, so the existing `Abrir formalização` start action correctly rejected the source as ineligible | `EV-06`, `EV-10`, `EV-30`, `MV-01` | resolved | Changed the canonical Consultation seed to `completed` and repaired the exact local seeded row without resetting the development database. |
| `FND-016` | implementation/seed | The Formalization dynamic form was seeded with `legal` context, while `ServerFormalizationSourceReader` requires `formalization` context | `EV-06`, `EV-10`, `EV-31`, `MV-01` | resolved | Added an explicit formalization context type to the canonical seed and repaired the exact local dynamic-form row without resetting the development database. |
| `FND-017` | Contract/authorization | The existing Formalization contract allowed only the assigned lawyer, but the user-requested product rule also grants administrators access | `RF-02`, `CA-03`, `EV-24`, `EV-32`, `MV-02` | resolved | Revised the local Spec to revision 6, added role-aware Core authorization across all Formalization use cases, and propagated the authenticated profile through all Server controllers. The external Jira/Confluence sources were not mutated. |
| `FND-018` | implementation/evidence | The Formalization page root geometry changed after the existing `VIS-01` screenshot was captured | `CA-12`, `EV-33`, `VIS-01` | resolved | Captured and inspected the current real-service `VIS-01` state at `1200 × 1828` (`EV-47`); the page-root geometry and removed contract-flow banner/status row are now represented by current evidence. |
| `FND-019` | implementation/UI | User screenshot identified the Formalization page contract-flow banner and `Ativa`/`Encerrada` status row as unwanted | `EV-34`, `VIS-01` | resolved | Removed the page-top banner/status row and unused Badge import; preserved the page content and disabled future contracting control. |
| `FND-020` | dependency/runtime | Installed `@tanstack/router-core@1.171.13` can evict an intent-preloaded route match while its loader is awaiting, then dereference the missing match's `_nonReactive` field | `EV-35`, `EV-36`, `EV-37`, `CA-12` | resolved | The generated Formalization route tree was structurally complete. Core-only and Router/core-only overrides exposed SSR and hydration API mismatches and were rejected; refreshing the full Router/Start graph supplied `router-core@1.171.27`, and the focused intent-preload browser sensor passes without a Router client error. |
| `FND-021` | process/evidence | The page-root margin/max-width correction was initially treated as too small to require its own immediate Evaluation update | `EV-33`, `EV-38` | resolved | This was an evidence-discipline oversight, not a valid exception. The geometry change is recorded in `EV-33`, affected visual evidence remains stale, and the Implement Spec prompt/skill now explicitly covers horizontal margins and `max-w-6xl`/equivalent page containers. |
| `FND-022` | implementation/UI | User screenshot identified the `Visualizar` document action as an unstyled text control | `EV-39`, `EV-47`, `VIS-03` | resolved | Added a semantic outlined pill treatment with an eye icon and hover affordance while preserving the existing document-version navigation and `Revisar` action; the current package capture verifies the result. |
| `FND-023` | Contract/feature amendment | User requested replacement and persistence of the Formalization contract form and two temporary seeded Formalization forms; revision 6 explicitly excluded manual form choice | `RF-05A`, `CA-05A`, `EV-40` onward | resolved | Amended the local Spec to revision 7 and carried its inherited-context clarification into revision 8. Replacement is restricted to an open active form, validates the selected definition against the Intake legal context, snapshots it, clears answers and form close/confirmation metadata, and preserves administrator/assigned-lawyer authorization. Current isolated HTTP evidence passes; clean-seed replay remains an evidence limitation tracked by `EV-40`, not an unresolved implementation defect. External Jira/Confluence sources were not mutated. |
| `FND-024` | implementation/static | Current Web typecheck | `EV-42`, Web conformance and any final PR CI | resolved | `builder_web` removed only the unused import, then reran Web code/types, focused tests and whitespace validation successfully. No durable rule change warranted for an isolated import error. |
| `FND-025` | environment/evidence | Focused Playwright CLI setup | `EV-43`, future `MV-*`/`VIS-*` | resolved | The configured runner initially reused a stale Vite server on port `3100` with incompatible environment state. The task-owned stale server was stopped and the CLI was rerun against a fresh config-managed server; no MCP evidence was used. |
| `FND-026` | implementation/test | Current fixture-backed Formalization route assertion | `EV-43`, F5-T3 and Web mocked route evidence | resolved | `builder_web` updated the stale snapshot-driven heading and close-action assertions to the current fixture/UI labels. The focused CLI suite passed 6 tests; the known fake-host request diagnostic is classified as mocked transport noise, not a feature failure. No product contract change was needed. |
| `FND-027` | pre-existing authorization/observability | Clean assigned-lawyer browser replay observed the optional Intake-details collaborator lookup | `EV-51`, `CA-12`, `MV-01` | accepted non-blocking | `GET /collaborators/:responsibleId` is guarded by `ActiveAdminGuard`, so a lawyer receives the endpoint's expected 403 while the optional Web query catches the failure and renders its fallback. The Formalization journey has no failed request, page error or contract failure. Broadening Identity authorization is outside this Spec and would change an existing policy; no durable authority update is warranted because the guard and fallback are already explicit in source. |
| `FND-028` | test/evidence | Reviewer inspection of the rollback scenario in `formalization-controller.regressions.test.ts` | `EV-45`, `CA-01`, `CA-02` | resolved | `builder_server` replaced the pre-transaction mock with a real HTTP-to-transaction regression: the Formalization insert succeeds, the stale Intake CAS fails, and the test proves HTTP 409, no persisted Formalization, unchanged Intake and no generation event. |
| `FND-029` | test/evidence | Reviewer inspection of clean-seed repeatability | `EV-50`, `CA-13` | resolved | The authorized destructive seed was completed twice. Both runs passed and the final PostgreSQL queries show one canonical open Intake/Formalization relation, three formalization definitions and two temporary replacement definitions without duplicates. |
| `FND-030` | evidence artifact | Reviewer inspection of saved visual evidence paths | `EV-47`, `EV-49`, `VIS-01`–`VIS-08`, `CA-12` | resolved | Fresh captures were retained under `apps/web/test-results/final-visual/`, including independent `zetNe.png`, `Z3Ll2j.png`, `ZLBTF.png` and `USNIG.png` outputs with exact dimensions; all current references point to existing artifacts. |
| `FND-031` | evidence freshness | Reviewer inspection of post-reseed role/theme browser coverage | `EV-51`, `MV-01`, `MV-02`, `CA-03`, `CA-12` | resolved | The final CLI matrix passed 4/4 after the repeated seed: administrator access, paralegal 403, dark mode and assigned-lawyer close/reopen. Console, page-error and failed-request diagnostics were clean; the optional collaborator 403 remains the accepted `FND-027` policy observation. |
| `FND-032` | implementation/routing | Final focused browser replay exposed the Formalization document-review route as a sibling without a dynamic Formalization layout | `EV-51`, `CA-12`, `F5-T3` | resolved | Added the dynamic Formalization route layout with an `Outlet`, regenerated `routeTree.gen.ts`, and reran the official 4-test Formalization route suite plus the final browser matrix. Direct review navigation now renders the review page instead of an empty shell; no REST contract changed. |

## Lessons learned

| Lesson | Source finding | Authority disposition |
| --- | --- | --- |
| A stale Docker Desktop bind-mount runtime can present an existing SQL file as a directory; inspect the exact mount error and file type before any destructive recovery. | `FND-001` | no authority change; recovery is already covered by `AGENTS.md` and was environment-specific. |
| Browser evidence must use the repository’s configured Playwright CLI; an MCP process or screenshot cannot be accepted for HMS, even if the interaction succeeds. | `FND-002` | no authority change; the prohibition is already explicit in `AGENTS.md`; retain the CLI command and classification in Evaluation. |
| Integrated package consumers are part of the type contract: a local package typecheck can pass while an inferred accumulator fails when a downstream boundary consumes it. | `FND-003` | no authority change; fix is feature-local and covered by the existing Core/Server typecheck exits. |
| A new Drizzle model is not runtime-ready until the generated migration is applied before controller tests are interpreted as feature failures. | `FND-004` | no authority change; Plan already assigns migration generation/application to the Orchestrator in F6. |
| Drizzle migration history can contain schema changes whose snapshots were never reconciled; review generated SQL against the live schema before applying, and preserve only the new delta in the migration. | `FND-005` | no authority change; this is a repository-history-specific reconciliation recorded in Evaluation. |
| A stale broad browser test process can starve the configured Playwright webServer; stop the task-owned process and rerun the narrow CLI scenario before classifying feature behavior. | `FND-006` | no authority change; recovery is operational and covered by current tooling/AGENTS guidance. |
| Seed references must be consumed end-to-end: adding an optional cross-seeder ID without creating the corresponding Formalization package leaves a deterministic seed green only by omission. | `FND-007` | no authority change; the feature-local seed contract and `database-layer-rules.md` already require module-owned, idempotent seed behavior. |
| The authoritative transport contract must be replayed against the canonical seed, not only against synthetic controller fixtures: UUID constraints can reject a visually correct dynamic form before persistence. | `FND-008` | no authority change; the frozen Spec and Validation schema already define UUID field identities; the correction remains with the F4 seed owner. |
| Page-root margin and maximum-width edits change design evidence even when they are only utility-class removals; record them immediately and invalidate affected screenshots. | `FND-021` | reinforced in the Implement Spec source prompt and synchronized skill, including explicit `mx-auto` and `max-w-6xl` examples. |
| A feature amendment must keep the Plan, Evaluation matrix and seed/browser evidence aligned with the new revision before closure. | `FND-023` | no authority change; this is already required by the SDD lifecycle and remains feature-local. |
| An isolated unused import is a source correction, not a reason to weaken the static quality gate. | `FND-024` | no authority change; existing Biome and TypeScript checks already provide the durable prevention. |
| Browser evidence must distinguish an expected policy denial on an optional ancillary request from an unexpected feature failure; record the exact endpoint, guard and fallback behavior. | `FND-027` | no authority change; `ActiveAdminGuard` and the Intake details fallback already define the behavior, while broadening Identity access would exceed this Spec. |
| A regression test must exercise the failure after the transaction boundary it claims to protect; mocking the entry use case before persistence can only prove error mapping. | `FND-028` | no authority change; the Server transaction rule already requires rollback/atomicity coverage, so this is a feature-local test correction. |
| Clean-seed evidence for idempotency requires two complete clear/run observations, not one successful invocation. | `FND-029` | no authority change; the database-layer rule already requires deterministic, repeatable seed behavior. |
| A dynamic route with a shared `$id` segment must have an explicit layout route when sibling detail/review surfaces depend on the same parameter; regenerate and exercise both direct and in-app navigation after adding it. | `FND-032` | no authority change; this is a feature-local TanStack route composition correction. |

## PR CI quality gate

<!-- Populate during conclude-spec. The head SHA identifies the PR revision checked by CI; it
is not SDD current-implementation metadata. Retain failed and superseded-head runs as history. -->

| ID | Workflow | Head SHA | Result | Run |
| --- | --- | --- | --- | --- |
| `CI-01` | applicable PR workflow | — | pending | — |

## History

| Date/Time | Event |
| --- | --- |
| `2026-08-24 19:49` | Evaluation created for Spec revision `5`; preflight recovered and approved; Plan-backed kickoff ready for Builder activation. |
| `2026-08-24 20:16` | F1 Core handoff inspected and official exits passed; F1 complete, ready to activate F2/F3. |
| `2026-08-24 20:27` | F2 Validation handoff inspected and official exits passed; F2 complete; F3 Web remains in progress. |
| `2026-08-24 20:40` | F3 Web handoff inspected and official code/types/Vitest/Playwright CLI exits passed; F3 complete; F4/F5 dependency work is ready. |
| `2026-08-24 23:40` | F4/F5 reviewer corrections, final guards/convergence, real MV-01/MV-02 replay, exact visual captures and same-reviewer final follow-up completed; no blocking findings remain; Evaluation ready for publication/conclusion. |
| `2026-08-25` | User-requested F4-T1 seed correction removed the duplicate `in_formalization` Intake; focused Server checks passed. Prior seed/browser evidence was marked stale pending a non-authorized destructive reseed. |
| `2026-08-25` | Fixed the Formalization Intake open-action seed mismatch: the canonical Consultation now seeds `completed`, and the exact local seeded row was repaired to unblock create-or-get navigation. |
| `2026-08-25` | Fixed the second open-action seed mismatch: the canonical formalization dynamic form now uses `formalization` context, and the exact local form row was repaired. |
| `2026-08-25` | Improved the Formalization document package `Visualizar` action with a token-based outlined pill and eye icon; focused Web checks passed and the prior package screenshot was marked stale pending recapture. |
| `2026-08-25` | Revision 8 evidence refresh: current authenticated targeted runtime checks verified inherited legal context and idempotent start; an isolated HTTP regression verified replacement persistence; Web focused CLI coverage passed 6 tests after correcting stale fixture labels. Full destructive reseed and authenticated replacement/inheritance replay remain separately pending. |
| `2026-08-25` | Final safe evidence refresh: real Auth/REST browser replay passed administrator navigation, paralegal 403, dark mode, `390 × 844` keyboard focus and zero unexpected console/failed requests; current page/package/Intake and four dialog screenshots were captured, dimension-checked and inspected. Evaluation remains `in_progress` only for the explicitly non-authorized clean reseed/full lifecycle replay. |
| `2026-08-25` | Closure evidence correction completed: the authorized seed ran twice, the real post-insert rollback regression passed, the final admin/paralegal/dark-mode browser matrix passed 4/4, and `F2GBfU`, `zetNe`, `Z3Ll2j`, `b2f2jS`, `nFKJE`, `ZLBTF` and `USNIG` are retained under `apps/web/test-results/final-visual/` with recorded dimensions. The dynamic Formalization route layout was added and the official Formalization route suite passed 4/4; the same-reviewer recheck followed as the final gate. |
| `2026-08-25` | Closure evidence correction completed: the authorized seed ran twice, the real post-insert rollback regression passed, the final admin/paralegal/dark-mode browser matrix passed 4/4, and `F2GBfU`, `zetNe`, `Z3Ll2j`, `b2f2jS`, `nFKJE`, `ZLBTF` and `USNIG` are retained under `apps/web/test-results/final-visual/` with recorded dimensions. The dynamic Formalization route layout was added and the official Formalization route suite passed 4/4. |
| `2026-08-25` | Same Reviewer final read-only recheck completed with no blocking findings. Plan F7 and its Review handoff row were closed; PR CI remains the only final quality-gate step. |
