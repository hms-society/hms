---
title: Dynamic forms administration page — implementation plan
status: in_progress
spec: ./spec.md
spec_revision: 19
evaluation: ./evaluation.md
jira_tickets:
  - SCRUM-141
prd: https://plataformahms.atlassian.net/wiki/spaces/~712020e69febeaca304dffb2d8d156ea17d2c4/pages/2654209/PRD+M+dulo+de+Cat+logo+Jur+dico
updated_at: 2026-09-14
---

# Execution status

- **Spec:** [`./spec.md`](./spec.md), revision `19`, `in_progress`.
- **Rationale:** Plan-backed execution is required for the cross-package Core/Validation
  contract, Server persistence and REST boundaries, Web surface, migration risk, module
  composition, real integration tests and authenticated browser validation.
- **Current phase:** `F5` — structural gate and integrated validation.
- **Next action:** Re-audit FND-027 on the published Web head, complete the Web PR CI gate, then
  invoke `conclude-spec` if no blocking finding remains.
- **Active blockers:** FND-027 remains open pending the same Reviewer’s re-audit and the final
  Web PR CI gate. The current branch is the
  canonical delivery line; unrelated worktree changes remain outside scoped commits and no
  `origin/develop` merge is planned.
- **Builders:** `builder_core` owns Core + Validation contracts; `builder_server` owns Server;
  `builder_web` owns Web. They use non-overlapping paths.
- **Parallelism:** `builder_server` and `builder_web` start only after `builder_core` hands off
  stable contracts. Orchestrator owns generated outputs, shared integration, Evaluation and
  final validation.

## Execution ledger

| Wave | Builder | Phase | Name | Depends on | Parallel with | Status | Exit condition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | Orchestrator | F0 | Kickoff and assignment activation | Spec Reviewer PASS | — | `completed` | Canonical Evaluation exists; exact Builder assignments and baseline conformance are recorded. |
| 1 | `builder_core` | F1 | Core administration contracts and behavior | F0 | — | `completed` | Core types, ports, six use-case tests pass Core checks; Validation schemas/barrels pass lint and type-check with no Validation tests. |
| 2 | `builder_server` | F2 | Server persistence, compatibility, REST and module wiring | F1 | `builder_web` | `completed` | Real Server source, migration inputs, controllers, six mirrored controller integrations, provider adapters and REST examples pass focused checks and real HTTP/database scenarios. |
| 2 | `builder_web` | F3 | Administration routes, hooks, widgets and route suites | F1 | `builder_server` | `completed` | Required Web tree and widget/controller-hook pairs exist; focused Vitest and Playwright CLI behavior, keyboard/narrow states, console/network checks and design captures pass. |
| 3 | Orchestrator | F4 | Generated artifacts and cross-boundary integration | F2, F3 | — | `completed` | Route tree and Drizzle migration artifacts are generated by repository commands; all shared contracts, exports, registrations and changed paths are integrated. |
| 4 | Orchestrator + `reviewer` | F5 | Structural gate, integrated sensors and review | F4 | — | `in_progress` | Current PATH-01 passes after correction; integrated tests, coverage, builds, real services, MV scenarios and exactly one current Reviewer pass with no blocking findings. |

### F0 — Kickoff and assignment activation

#### F0-T1 — Materialize Evaluation and activate bounded Builders

- **Status/owner:** `completed` — Orchestrator
- **Depends/parallel:** Spec revision 19 is `in_progress`; independent Spec Reviewer completed with PASS for the prior revision. The shared cross-module composition correction remains subject to the same review workflow.
- **Paths:** `documentation/features/legal-catalog/dynamic-forms-page/evaluation.md`; Plan and Spec metadata/status only.
- **Contract:** All FR/AC; assignment gate and canonical evidence ledger requirements from `documentation/sdd.md` and `implement-spec`.
- **Outcome:** Evaluation records the revision, assignments, exact allowed/prohibited paths, Rule Pack, design references, baseline conformance and validation exits; Spec/Plan move to `in_progress` before Builder work.
- **Rules:** `documentation/sdd.md`; `documentation/rules/rules.md`; all Rule Pack paths listed in the Spec.
- **Exit:** Inspect current worktree and untouched required tree; record exact assignment IDs, baseline result, service preflight requirements and pending evidence rows in Evaluation. Completed with the baseline integrity finding `FND-009` and authenticated preflight `EV-12`.

### F1 — Core administration contracts and behavior

#### F1-T1 — Implement Core contracts, use cases and Validation schemas

- **Status/owner:** `completed` — `builder_core`
- **Depends/parallel:** F0; blocks Server and Web implementation contracts. Core and Validation are grouped because Validation is a small contract-only package owned by the same confirmed Builder.
- **Paths:** All canonical `packages/core/**` rows in Spec revision 19, including the Legal Catalog domain declarations, administration repository/ports, consumer usage-provider ports, service contract, and `packages/core/package.json`; all canonical `packages/validation/**` rows, excluding the separately Orchestrator-owned shared-error integration path if applicable.
- **Contract:** FR-04–FR-09, FR-11; AC-03–AC-08 and AC-11.
- **Outcome:** Framework-independent Core owns the canonical administration contract and behavior; the existing consumer contract remains compatible; Validation exposes only transport schemas and stays test-free.
- **Rules:** `documentation/rules/core-package-rules.md`; `documentation/rules/use-case-testing-rules.md`; `documentation/rules/validation-package-rules.md`; `documentation/rules/code-conventions-rules.md`.
- **Exit:** `pnpm --filter @hms/core test -- src/legal-catalog/use-cases/tests`; `pnpm --filter @hms/core check-types`; `pnpm --filter @hms/core check:architecture`; `pnpm --filter @hms/validation lint`; `pnpm --filter @hms/validation check-types`; inspect exact tree, one exported declaration per Core file, and no Validation tests. Completed with `EV-18`–`EV-21` passed; repository-wide integrity remains `FND-009`.

### F2 — Server persistence, compatibility, REST and module wiring

#### F2-T1 — Implement canonical persistence and consumer compatibility

- **Status/owner:** `completed` — `builder_server`
- **Depends/parallel:** F1; parallel with F3 after stable Core/Validation contracts.
- **Paths:** `apps/server/src/legal-catalog/database/**`, legal-catalog constants, database/provision modules, consultation/formalization usage-provider adapters and module bindings, Identity database cycle-breaking path, shared database compatibility adapter/seeder/schema/type paths, and the non-generated shared seed paths named in the Spec.
- **Contract:** FR-04, FR-06–FR-09, FR-11; AC-04, AC-06–AC-08, AC-11.
- **Outcome:** Canonical Legal Catalog persistence owns definitions, topics, audit and replay; consumer flows continue through the legacy adapter; usage providers cross module boundaries only through Core ports; module wiring has no cycle.
- **Rules:** `documentation/rules/database-layer-rules.md`; `documentation/rules/server-app-layer-rules.md`; `documentation/rules/provision-layer-rules.md`; `documentation/rules/core-package-rules.md`; `documentation/rules/code-conventions-rules.md`.
- **Exit:** Focused Server lint/type-check and integration sensors; real database transaction, replay, historical-preservation and usage-impact scenarios; inspect migration inputs before Orchestrator generation; record HTTP/persistence evidence in Evaluation.

#### F2-T2 — Implement REST controllers, error mapping and examples

- **Status/owner:** `completed` — `builder_server`
- **Depends/parallel:** F1 and F2-T1; parallel with F3 once the Core contract is stable.
- **Paths:** Six Legal Catalog controllers, DTO/barrels/module registration, six mirrored controller test files, shared global error handler/DTO, Legal Catalog and consumer `.rest` files, and related Server REST paths named in the Spec.
- **Contract:** FR-01, FR-04–FR-09, FR-11; AC-01, AC-04–AC-08, AC-11.
- **Outcome:** Each action has one controller and one real Nest/database integration test file; authorization, named conflict metadata, idempotency, status, deletion and compatibility responses are observable over HTTP.
- **Rules:** `documentation/rules/rest-layer-rules.md`; `documentation/rules/controllers-testing-rules.md`; `documentation/rules/database-layer-rules.md`.
- **Exit:** Focused Server Biome/type checks; all six controller test files pass against real module/database wiring; real `curl` scenarios cover status/body, validation, auth/authorization, persistence, replay and side effects; no combined controller suite.

## Execution log

- **2026-09-11 — F1 handoff completed**
  - **Finding/result:** `EV-18`–`EV-21` passed; `FND-009` remains a pre-existing repository sensor failure.
  - **Next action:** Continue `builder_server` and `builder_web` in parallel for F2/F3.
- **2026-09-11 — F2/F3 safe checkpoints**
  - **Finding/result:** `FND-011` and `FND-012`; both Builder slices are incomplete despite partial focused checks passing.
  - **Next action:** Resume the same ownership Builders, complete their remaining paths and validations, then run Orchestrator integration.
- **2026-09-11 — F2/F3/F4 handoffs completed**
- **Finding/result:** Server six controller suites passed (6 files/20 tests), provider aggregation was covered through the usage-impact/controller boundary, migration compatibility passed, Web focused checks passed (12 files/19 tests), all three route suites passed, route tree and migration artifacts generated/applied; `FND-011`–`FND-013` resolved.
  - **Next action:** Complete integrated validation, then run the single Implementation Reviewer.
- **2026-09-11 — Integrated validation and runtime correction**
  - **Finding/result:** PATH-01 passed 164/164; scoped Core/Server/Web coverage, architecture, types, scripts and production build passed. Fresh admin Auth/REST/browser validation passed at 390×844 with a screenshot and keyboard route check. A fresh Nest bootstrap initially exposed `UnknownDependenciesException` for `DrizzleClient`; the Server Builder added `SharedDatabaseModule` to `FormalizationProvisionModule`, after which bootstrap and route registration passed.
- **Next action:** Complete the single Implementation Reviewer and resolve any findings; finish or classify the remaining manual and exact-viewport visual evidence.

- **2026-09-14 — Correction and evidence refresh completed**
  - **Finding/result:** The owning correction Builders restored the exact Core repository contract and normalized-name conflict mapping. The Formalization replacement fixture now creates its referenced legal area/topic through the Legal Catalog repositories. Core coverage passed 135/664, Server coverage passed 114/240, Web coverage passed 170/549, current Web UI tests passed 12/19, the focused UUID route passed 1/1, PATH-01 passed 164/164, and `/tmp/dynamic-forms-admin.png` was recreated and inspected.
  - **Next action:** Resume the same Implementation Reviewer for the current canonical branch; on `PASS`, route directly to `conclude-spec`.

### F3 — Administration routes, hooks, widgets and route suites

#### F3-T1 — Implement Web domain hooks, widgets and protected routes

- **Status/owner:** `completed` — `builder_web`
- **Depends/parallel:** F1; parallel with F2 after stable Core/Validation contracts. REST calls may be isolated during development but final evidence must use real Server/Auth flows.
- **Paths:** All canonical `apps/web/**` rows in the Spec except the Orchestrator-owned generated `apps/web/src/routeTree.gen.ts`.
- **Contract:** FR-01–FR-05, FR-07, FR-10–FR-11; AC-01–AC-05 and AC-07, AC-09–AC-11.
- **Outcome:** The exact six-widget tree, seven domain query/action hooks, protected list/new/detail routes, admin-only sidebar item, URL-owned search/filter/page state and consumer-facing recovery/focus behavior are implemented with no page-level geometry violations.
- **Rules:** `documentation/rules/ui-layer-rules.md`; `documentation/rules/web-app-routing-rules.md`; `documentation/rules/widget-testing-rules.md`; `documentation/rules/rest-layer-rules.md`; `documentation/design.md`.
- **Exit:** Required widget/component and controller-hook tests pass; affected Playwright CLI suites pass with accessible locators, keyboard and 320px coverage, console/failed-request inspection and fresh screenshots for each design state; inspect exact tree and manifest comparisons.

### F4 — Generated artifacts and cross-boundary integration

#### F4-T1 — Generate and integrate route/migration artifacts

- **Status/owner:** `completed` — Orchestrator
- **Depends/parallel:** F2 and F3 complete with focused exits.
- **Paths:** `apps/web/src/routeTree.gen.ts`; `apps/server/src/shared/database/drizzle/migrations/0044_harsh_romulus.sql`; `apps/server/src/shared/database/drizzle/migrations/meta/0044_snapshot.json`; `apps/server/src/shared/database/drizzle/migrations/meta/_journal.json`.
- **Contract:** AC-08, AC-10–AC-11; generated-artifact and migration requirements in the Spec.
- **Outcome:** Generated route and Drizzle artifacts reflect the integrated source and are reviewed for route registration, backfill, constraints, compatibility and historical safety.
- **Rules:** `documentation/rules/web-app-routing-rules.md`; `documentation/rules/database-layer-rules.md`; `documentation/tooling.md`.
- **Exit:** `pnpm --filter web generate-routes`; `pnpm --filter server db:migration:generate`; inspect generated bodies/diff; validate migration behavior through approved consuming boundaries and re-run focused type/architecture checks as needed.

### F5 — Structural gate, integrated sensors and review

#### F5-T1 — Run current path gate and integrated quality evidence

- **Status/owner:** `completed` — Orchestrator
- **Depends/parallel:** F4; no downstream review or readiness before PATH-01 passes.
- **Paths:** Integrated candidate across every Spec canonical row; no new implementation path may be introduced without a Spec correction.
- **Contract:** All FR/AC and every `MV-*`, visual reference and rule obligation.
- **Outcome:** Evaluation is a current ledger of structural, automated, runtime, manual, visual, rule and coverage evidence.
- **Rules:** `documentation/sdd.md`; full Spec Rule Pack; `documentation/tooling.md`; `AGENTS.md` authenticated-browser workflow.
- **Exit:** PATH-01 passed against `HEAD` with 164/164 declared paths; Core, Server and Web
  coverage passed (135/664, 114/240 and 170/549 tests respectively); architecture, types,
  scripts and build passed; fresh Nest/Auth/REST preflight, admin/attendant flows,
  mutation/retry evidence, narrow keyboard/focus evidence and exact-viewport visual captures
  passed. The current branch remains canonical and the baseline integrity result is reconciled
  as passing.

#### F5-T2 — Single integrated Implementation Reviewer and correction loop

- **Status/owner:** `in_progress` — Orchestrator + `reviewer` + `builder_fix_core`/`builder_fix_server`/`builder_fix_web`/`builder_fix_server_ci`/`builder_fix_web_ci`/`builder_fix_web_mobile`
- **Depends/parallel:** F5-T1 PATH-01 pass; may run alongside integrated sensors after the gate.
- **Paths:** Read-only review of all integrated Spec paths and current Evaluation evidence.
- **Contract:** Complete Spec revision 19 conformance, cross-Builder contracts, missing states/tests, design and evidence freshness.
- **Outcome:** Exactly one current Reviewer audited the integrated candidate; `FND-018`,
  `FND-019` and the transient `FND-023` were resolved through the owning correction Builders.
  The Formalization fixture was also aligned with the new legal-area/topic foreign keys, and
  the current full Server coverage rerun passed before the same Reviewer was resumed. That
  re-audit opened `FND-024` for the desktop table inventory; `builder_fix_web` then replaced the
  fixed widths with responsive columns, added a 1440×900 bounds regression and refreshed the
  catalog capture. Focused Web route/unit/code/type checks passed. The same Reviewer re-audited
  the corrected candidate and returned PASS with no findings.
  The first published Core slice then failed clean-PR type-check because its Formalization
  export edits depended on unrelated uncommitted provider-renaming files; `FND-025` routes the
  isolation correction through the Core owner.
  The corrected Core then passed, while the dependent clean Server/Web checks exposed the same
  dirty-tree dependency pattern in Formalization providers, pagination primitives and auth
  fixtures; `FND-026` was resolved by the published clean-slice corrections. The same Reviewer’s
  next re-audit opened `FND-027` because mobile cards lacked the explicit `Editar` operation;
  the Orchestrator-local Web correction now adds that action and a 320px keyboard assertion, and
  the focused checks pass. The final Web PR CI gate and same-Reviewer re-audit remain pending.
- **Rules:** `documentation/sdd.md`; full Spec Rule Pack; `documentation/agents/implementation-reviewer-agent.md`.
- **Exit:** The affected desktop/narrow browser and visual evidence is refreshed; the same Reviewer
  returned PASS with all findings resolved; every phase/task is complete; Evaluation is `ready`.
  Route to `conclude-spec` after the final PR CI gate passes.

## Validation and handoff

| Type | Scenario/surface | Criteria | Reference | Evidence target | Status |
| --- | --- | --- | --- | --- | --- |
| Runtime | Core administration use cases, repository transactions and compatibility adapter | AC-03–AC-08, AC-11 | Spec Technical Contract | `./evaluation.md` | `passed` |
| Runtime | Six real Legal Catalog controller HTTP/database boundaries | AC-01, AC-04–AC-08 | Spec REST Contract | `./evaluation.md` | `passed` |
| Runtime | Consultation/Formalization usage-provider aggregation | AC-06 | Spec usage-provider Contract | `./evaluation.md` | `passed` |
| Automated | Core/Validation/Server/Web focused checks, integrity, architecture, coverage and build | All applicable AC | Spec Validation Contract | `./evaluation.md` | `passed_with_non_blocking_baseline` |
| Manual | Admin and attendant fresh authenticated browser contexts, sidebar, routes and APIs | AC-01 | `MV-01` in Spec | `./evaluation.md` | `passed` |
| Manual | Search/filter/page URL flow and compatibility consumer flow | AC-03, AC-11 | `MV-02`, `MV-11` in Spec | `./evaluation.md` | `passed` |
| Manual | Duplicate/conflict/replay and status/deletion impact flows | AC-04–AC-08 | `MV-03`–`MV-08` in Spec | `./evaluation.md` | `passed_with_automated_replay` |
| Manual | Retry, focus, keyboard and 320 CSS px behavior | AC-09–AC-10 | `MV-09`, `MV-10` in Spec | `./evaluation.md` | `passed` |
| Visual | Populated administration page | AC-02 | `design/DWg64.png`, 1440×900, light/dark | `./evaluation.md` | `passed_with_authorized_difference` |
| Visual | Duplicate dialog | AC-04–AC-05 | `design/V7nH8.png`, exact manifest viewport | `./evaluation.md` | `passed_with_authorized_difference` |
| Visual | Impact dialog | AC-06 | `design/WjuC6.png`, exact manifest viewport | `./evaluation.md` | `passed_with_authorized_difference` |
| Visual | Availability states | AC-07 | `design/iu2Vg.png` and `design/tErvw.png`, exact manifest viewport | `./evaluation.md` | `passed_with_authorized_difference` |
| Visual | Deletion dialog | AC-08 | `design/aSbsz.png`, exact manifest viewport | `./evaluation.md` | `passed_with_authorized_difference` |

The final handoff requires every task and phase to be complete, generated artifacts and
migration bodies reviewed, services/accounts/fixtures ready, every `MV-*` executable, all
visual comparisons current, PATH-01 passing on the selected local baseline, the single
`reviewer` current and passing, no active blocking finding, and Evaluation set to `ready`.
Then invoke `conclude-spec` directly.
